# 下载格式验证指南

> **功能：** 确保下载的音频文件格式与用户选择的格式一致  
> **更新时间：** 2026-08-14 02:00

---

## 🎯 功能说明

### 核心逻辑
```
用户选择 MP3 格式
  ↓
生成语音（MP3 格式）
  ↓
保存 Blob 和格式信息
  ↓
点击"下载音频文件 (MP3)"按钮
  ↓
下载文件名：文档名.mp3 ✅
```

---

## 🔧 已添加的功能

### 1. 控制台调试信息
**生成语音时：**
```javascript
生成语音配置:
- 文件名: 测试文档
- 格式: mp3
- 采样率: 24000
```

**生成成功后：**
```javascript
音频生成成功:
- Blob 大小: 131560 bytes
- Blob 类型: audio/mpeg
- 保存的格式: mp3
```

**下载时：**
```javascript
开始下载音频...
文件名: 测试文档
格式: mp3
完整文件名: 测试文档.mp3
```

### 2. 下载按钮动态显示格式
**按钮文本变化：**
```
生成前： 📥 下载音频文件
         ↓
生成后： 📥 下载音频文件 (MP3)  ← 显示当前格式
```

### 3. 更清晰的错误提示
```
没有可下载的音频文件，请先生成语音 ← 更明确的提示
```

---

## ✅ 验证步骤

### 步骤1：打开浏览器开发者工具
```
按 F12 或右键 → 检查
切换到 "Console" 标签
```

### 步骤2：测试 MP3 格式
1. 上传 Word 文档
2. 选择音频格式：**MP3**
3. 选择采样率：**24000 Hz**
4. 点击"生成对话语音"
5. **查看控制台输出：**
   ```
   生成语音配置:
   - 文件名: xxx
   - 格式: mp3        ← 确认是 mp3
   - 采样率: 24000
   
   音频生成成功:
   - Blob 大小: xxx bytes
   - Blob 类型: audio/mpeg  ← 确认是 MP3
   - 保存的格式: mp3        ← 确认保存的是 mp3
   ```
6. **查看下载按钮：** 应该显示 `📥 下载音频文件 (MP3)`
7. 点击"下载音频文件 (MP3)"按钮
8. **查看控制台输出：**
   ```
   开始下载音频...
   文件名: xxx
   格式: mp3              ← 确认是 mp3
   完整文件名: xxx.mp3    ← 确认扩展名是 .mp3
   ```
9. **验证下载的文件：**
   - 文件名应该是 `xxx.mp3`
   - 文件可以正常播放

---

### 步骤3：测试 WAV 格式
1. 刷新页面（清除之前的状态）
2. 上传 Word 文档
3. 选择音频格式：**WAV**
4. 点击"生成对话语音"
5. **查看控制台输出：**
   ```
   生成语音配置:
   - 格式: wav        ← 确认是 wav
   
   音频生成成功:
   - Blob 类型: audio/wav  ← 确认是 WAV
   - 保存的格式: wav       ← 确认保存的是 wav
   ```
6. **查看下载按钮：** 应该显示 `📥 下载音频文件 (WAV)`
7. 点击下载按钮
8. **查看控制台输出：**
   ```
   格式: wav              ← 确认是 wav
   完整文件名: xxx.wav    ← 确认扩展名是 .wav
   ```
9. **验证下载的文件：** 文件名应该是 `xxx.wav`

---

### 步骤4：测试 OGG 格式
1. 刷新页面
2. 选择音频格式：**OGG**
3. 生成并下载
4. **验证：** 下载的文件扩展名应该是 `.ogg`

---

## 🐛 问题排查

### 问题1：下载的文件还是 .txt 格式
**可能原因：**
- 浏览器缓存未清除
- 使用了浏览器自带的下载按钮

**解决方案：**
1. 清除浏览器缓存（`Ctrl + Shift + R`）
2. 只使用页面上的"📥 下载音频文件"按钮（绿色按钮）
3. 不要使用音频播放器的下载功能

---

### 问题2：下载按钮显示的格式不对
**示例：** 选择了 WAV 格式，但按钮显示 `(MP3)`

**可能原因：**
- 生成语音时使用的是旧的配置
- JavaScript 变量未更新

**解决方案：**
1. 刷新页面
2. 重新上传文档
3. 重新选择格式
4. 重新生成语音

---

### 问题3：控制台没有调试信息
**可能原因：**
- 控制台过滤了信息
- JavaScript 未加载

**解决方案：**
1. 确认控制台显示 "全部" 或 "信息" 级别
2. 刷新页面
3. 重新生成语音

---

## 📊 测试矩阵

### 完整测试表格

| 测试编号 | 音频格式 | 采样率 | 预期下载文件名 | 实际下载文件名 | 测试结果 |
|---------|---------|--------|--------------|--------------|---------|
| 1 | MP3 | 8000 Hz | xxx.mp3 | | ☐ |
| 2 | MP3 | 16000 Hz | xxx.mp3 | | ☐ |
| 3 | MP3 | 24000 Hz | xxx.mp3 | | ☐ |
| 4 | WAV | 8000 Hz | xxx.wav | | ☐ |
| 5 | WAV | 16000 Hz | xxx.wav | | ☐ |
| 6 | WAV | 24000 Hz | xxx.wav | | ☐ |
| 7 | OGG | 8000 Hz | xxx.ogg | | ☐ |
| 8 | OGG | 16000 Hz | xxx.ogg | | ☐ |
| 9 | OGG | 24000 Hz | xxx.ogg | | ☐ |

---

### 测试用例详细说明

#### 测试用例 1-3：MP3 格式
**步骤：**
1. 选择 MP3 格式
2. 选择不同采样率（8000/16000/24000 Hz）
3. 生成并下载
4. **验证：** 所有下载文件扩展名都是 `.mp3`

#### 测试用例 4-6：WAV 格式
**步骤：**
1. 选择 WAV 格式
2. 选择不同采样率
3. 生成并下载
4. **验证：** 所有下载文件扩展名都是 `.wav`
5. **额外验证：** WAV 文件比 MP3 大 10 倍左右

#### 测试用例 7-9：OGG 格式
**步骤：**
1. 选择 OGG 格式
2. 选择不同采样率
3. 生成并下载
4. **验证：** 所有下载文件扩展名都是 `.ogg`

---

## 🎯 快速验证脚本

### 方法1：使用控制台
```javascript
// 在浏览器控制台执行以下命令，查看当前配置

console.log('当前音频格式:', currentAudioFormat);
console.log('当前文件名:', currentFileName);
console.log('Blob 是否存在:', !!currentAudioBlob);

// 如果看到：
// 当前音频格式: mp3
// 当前文件名: 测试文档
// Blob 是否存在: true
// 说明配置正确
```

### 方法2：模拟下载
```javascript
// 在控制台执行，查看完整文件名
console.log(`${currentFileName}.${currentAudioFormat}`);

// 应该输出：测试文档.mp3 (如果选择的是 MP3)
```

---

## 📝 验证报告模板

```
# 下载格式验证报告

## 测试信息
- 测试人：________
- 测试日期：________
- 浏览器：________ 版本：________

## 测试结果
### MP3 格式
- 8000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 16000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 24000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________

### WAV 格式
- 8000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 16000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 24000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________

### OGG 格式
- 8000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 16000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________
- 24000 Hz：☐ 通过 / ☐ 失败 - 下载文件名：________

## 控制台调试信息截图
[贴图]

## 发现的问题
1. [问题描述]
2. [问题描述]

## 结论
☐ 所有格式都能正确下载
☐ 部分格式有问题（请说明）
☐ 需要进一步调试
```

---

## 💡 使用建议

### 建议1：先看控制台
生成语音后，先查看控制台输出，确认格式是否正确保存。

### 建议2：清除缓存测试
每次测试新格式前，刷新页面清除状态。

### 建议3：保留调试信息
如果发现问题，截图控制台输出，方便排查。

---

**最后更新：** 2026-08-14 02:00  
**状态：** ✅ 已添加调试功能，等待验证

