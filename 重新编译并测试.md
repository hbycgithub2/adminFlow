# 重新编译并测试步骤

## ❗ 问题发现

从日志中看到，**没有输出"解析文本片段"的调试日志**，这说明：
1. 旧的class文件还在使用（没有重新加载）
2. 或者服务没有重启

## ✅ 解决方案：重新编译+重启

### 步骤1：停止当前服务

在运行服务的窗口按 `Ctrl+C` 停止服务

### 步骤2：重新编译

```powershell
cd d:\code\adminFlow\hm-service
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
mvn clean compile -DskipTests
```

**预期结果：**
```
[INFO] BUILD SUCCESS
```

### 步骤3：重新打包（可选，如果用java -jar运行）

```powershell
mvn clean package -DskipTests
```

### 步骤4：重启服务

#### 方法A：使用Maven运行（推荐）
```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
mvn spring-boot:run
```

#### 方法B：使用jar包运行
```powershell
java -jar target\hm-service-1.0.0.jar
```

#### 方法C：在IDE中运行（最简单）
1. 在IDEA中找到 `HmServiceApplication.java`
2. 右键 → Run（或点击绿色三角形）

### 步骤5：等待服务启动完成

看到以下日志说明启动成功：
```
Started HmServiceApplication in X.XXX seconds
```

### 步骤6：测试Word文档上传

1. 打开浏览器：`http://localhost:8080/document-tts-test.html`
2. 启用多音色模式
3. 选择6种不同音色
4. 上传Word文档
5. 点击"生成音频"

### 步骤7：查看日志（关键！）

**必须看到以下日志才说明修改生效：**

```log
DEBUG --- WordDocumentParser : 解析文本片段: text='xxx', isBold=true/false, colorRGB=0070C0/C00000/..., speaker=xxx, paragraphId=1
```

**关键字段：**
- `colorRGB=0070C0` ← 蓝色
- `colorRGB=C00000` ← 红色
- `colorRGB=00B050` ← 绿色
- `colorRGB=7030A0` ← 紫色
- `colorRGB=null` ← 黑色

**如果看不到这个日志，说明：**
1. 服务没有重启
2. 编译的class文件没有被加载
3. IDE缓存没有清理

---

## 🔍 如果还是不行，检查以下几点

### 检查1：确认class文件已更新

```powershell
cd d:\code\adminFlow\hm-service
dir target\classes\com\hmall\tts\volcengine\docx\WordDocumentParser.class
```

查看文件的修改时间，应该是刚才编译的时间。

### 检查2：确认代码已修改

打开 `WordDocumentParser.java`，检查第74行是否有：
```java
String colorRGB = run.getColor();
```

### 检查3：清理IDE缓存（如果使用IDE）

**IDEA：**
1. File → Invalidate Caches
2. 勾选 "Clear file system cache and Local History"
3. 点击 "Invalidate and Restart"

**Eclipse：**
1. Project → Clean
2. 选择项目
3. 点击OK

### 检查4：确认日志级别

打开 `application.yaml`，确认：
```yaml
logging:
  level:
    com.hmall: debug
```

### 检查5：查看完整启动日志

启动时应该看到类似的日志：
```
Mapped "{[/api/document-tts/generate]}" onto ...DocumentTTSController.generateDocumentSpeech
```

---

## 📋 预期的完整日志流程

```log
# 1. 接收请求
INFO --- DocumentTTSController : 接收到文档TTS生成请求...

# 2. 解析Word文档（关键！必须看到）
DEBUG --- WordDocumentParser : 解析文本片段: text='黑色加粗', isBold=true, colorRGB=null, speaker=zh_male_m191_uranus_bigtts, paragraphId=1
DEBUG --- WordDocumentParser : 解析文本片段: text='黑色普通', isBold=false, colorRGB=null, speaker=zh_female_vv_uranus_bigtts, paragraphId=2
DEBUG --- WordDocumentParser : 解析文本片段: text='蓝色文字', isBold=false, colorRGB=0070C0, speaker=zh_female_m191_uranus_bigtts, paragraphId=3
DEBUG --- WordDocumentParser : 解析文本片段: text='红色文字', isBold=false, colorRGB=C00000, speaker=zh_female_xiaolu_uranus_bigtts, paragraphId=4
DEBUG --- WordDocumentParser : 解析文本片段: text='绿色文字', isBold=false, colorRGB=00B050, speaker=ICL_uranus_zh_male_ruyacaijun_tob, paragraphId=5
DEBUG --- WordDocumentParser : 解析文本片段: text='紫色文字', isBold=false, colorRGB=7030A0, speaker=ICL_uranus_zh_female_wenrouwenya_tob, paragraphId=6

# 3. 合并片段
INFO --- TextSegmentMerger : 合并完成，共X个合并片段

# 4. 生成语音
INFO --- VolcengineTTSService : 调用TTS API...

# 5. 返回结果
INFO --- DocumentTTSController : 文档TTS生成成功
```

---

## ⚠️ 常见问题

### 问题1：看不到WordDocumentParser的日志

**原因：** 服务没有重启，还在使用旧的class文件

**解决：** 
1. 完全停止服务（Ctrl+C）
2. 重新编译
3. 重新启动

### 问题2：日志中colorRGB全是null

**原因：** Word文档的颜色设置不正确

**解决：** 
1. 检查Word文档，确保文字确实有颜色
2. 使用RGB值设置颜色，不要使用"主题颜色"
3. 蓝色RGB：R=0, G=112, B=192（十六进制：0070C0）
4. 红色RGB：R=192, G=0, B=0（十六进制：C00000）

### 问题3：日志中colorRGB有值，但speaker都一样

**原因：** VoiceConfig中的颜色音色字段为null

**解决：** 
1. 前端必须启用"多音色模式"
2. 前端必须选择4个颜色音色
3. 检查浏览器控制台，确认参数已发送

### 问题4：IDE中运行还是旧代码

**原因：** IDE缓存问题

**解决：** 
1. IDEA: File → Invalidate Caches → Invalidate and Restart
2. Eclipse: Project → Clean

---

## 🎯 快速验证方法

如果不想上传Word文档，可以用这个快速验证：

```java
// 在WordDocumentParser.java的parse方法开始处添加：
log.info("==========================================");
log.info("WordDocumentParser.parse() 被调用！");
log.info("multiVoiceMode={}", voiceConfig.isMultiVoiceMode());
log.info("blueVoice={}", voiceConfig.getBlueVoice());
log.info("redVoice={}", voiceConfig.getRedVoice());
log.info("==========================================");
```

重新编译并运行，上传任何Word文档，应该立即看到这个日志。

如果看到了，说明代码已更新。
如果看不到，说明服务还在用旧代码。

---

**创建时间：** 2026-08-21  
**状态：** 等待重新编译+重启+测试
