# 字幕编辑功能 - 方案分析

> **目标**：实现类似剪映的字幕编辑功能，支持可视化编辑字幕的样式、位置、时间轴等

---

## 🎬 剪映字幕编辑功能分析

### 剪映的字幕编辑特点

1. **所见即所得**：
   - 视频预览窗口中直接显示字幕
   - 拖动字幕可以改变位置
   - 实时预览字幕效果

2. **时间轴编辑**：
   - 字幕轨道显示所有字幕片段
   - 拖动字幕条可以调整时间
   - 拖动边缘可以调整持续时间

3. **样式编辑**：
   - 字体、大小、颜色
   - 边框、阴影、背景
   - 动画效果（渐入、飞入等）

4. **逐字编辑**：
   - 可以修改每句话的文本
   - 可以拆分或合并字幕
   - 支持逐字卡拉OK效果

---

## 📊 专业视频工具的字幕编辑方案对比

### 方案A：客户端编辑器（剪映、PR、AE）

**实现方式**：
- 使用桌面应用（Qt、Electron）
- 视频播放器 + 字幕轨道
- 实时渲染和预览

**优点**：
- ✅ 功能强大，体验流畅
- ✅ 支持复杂动画和特效
- ✅ 所见即所得

**缺点**：
- ❌ 需要开发桌面应用
- ❌ 开发成本高（3-6个月）
- ❌ 需要跨平台适配

---

### 方案B：Web可视化编辑器（我们的目标）

**实现方式**：
- 使用Web技术（Vue/React + Canvas/SVG）
- HTML5 Video + 字幕轨道UI
- 实时预览 + 后端重新生成

**优点**：
- ✅ 跨平台，无需安装
- ✅ 开发周期短（1-2周）
- ✅ 可以集成到现有系统

**缺点**：
- ⚠️ 性能不如原生应用
- ⚠️ 复杂特效可能有限制

---

### 方案C：ASS字幕文件编辑器

**实现方式**：
- 生成可编辑的ASS字幕文件
- 用户下载后用字幕编辑器修改
- 重新上传ASS文件生成视频

**优点**：
- ✅ 实现简单（1-2天）
- ✅ 专业用户可以精细调整
- ✅ 兼容现有工具（Aegisub）

**缺点**：
- ❌ 需要学习ASS语法
- ❌ 体验不够友好
- ❌ 不适合普通用户

---

## 🎯 推荐方案：Web可视化字幕编辑器

### 架构设计

```
┌─────────────────────────────────────────┐
│           前端编辑器                      │
│  ┌─────────────────────────────────┐   │
│  │    视频播放器 (HTML5 Video)      │   │
│  │    + 字幕覆盖层 (Canvas/SVG)     │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │       字幕时间轴编辑器            │   │
│  │  ┌───┐ ┌─────┐ ┌───┐ ┌─────┐   │   │
│  │  │字1│ │ 字2 │ │字3│ │ 字4 │   │   │
│  │  └───┘ └─────┘ └───┘ └─────┘   │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │       字幕样式编辑面板            │   │
│  │  字体 [微软雅黑 ▼]               │   │
│  │  大小 [======●==] 64px          │   │
│  │  颜色 [🎨 #FFFFFF]              │   │
│  │  动画 [渐入渐出 ▼]               │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
                  ↓ 保存
┌─────────────────────────────────────────┐
│           后端API                        │
│  POST /api/subtitle-editor/update       │
│  - 接收编辑后的字幕数据                   │
│  - 重新生成ASS文件                       │
│  - 调用FFmpeg重新合成视频                 │
└─────────────────────────────────────────┘
```

---

## 🛠️ 技术实现方案

### 阶段1：基础字幕编辑（1-2天）

**功能**：
- ✅ 显示当前字幕列表
- ✅ 编辑字幕文本内容
- ✅ 调整字幕时间（开始时间、持续时间）
- ✅ 调整字幕样式（字体、大小、颜色）

**实现**：
```javascript
// 前端数据结构
const subtitles = [
  {
    id: 1,
    text: "你好，我是云舟，很高兴认识你",
    startTime: 0.0,
    duration: 4.5,
    style: {
      fontName: "Microsoft YaHei",
      fontSize: 64,
      fontColor: "#FFFFFF",
      borderColor: "#000000",
      borderWidth: 3,
      position: 2,  // 底部居中
      animationType: "fade"
    }
  },
  // ...更多字幕
];

// 保存修改
async function saveSubtitles() {
  const response = await fetch('/api/subtitle-editor/update', {
    method: 'POST',
    body: JSON.stringify({
      taskId: 'xxx',
      subtitles: subtitles
    })
  });
  // 后端重新生成视频
}
```

**后端API**：
```java
@PostMapping("/update")
public VideoGenerateResponse updateSubtitles(@RequestBody SubtitleUpdateRequest request) {
    // 1. 重新生成ASS字幕文件
    String assContent = assGenerator.generateASS(request.getSubtitles(), ...);
    
    // 2. 调用FFmpeg重新合成视频（使用原音频 + 新字幕）
    ffmpegUtil.generateVideo(originalAudio, newAss, outputVideo, config);
    
    return response;
}
```

---

### 阶段2：可视化时间轴编辑（2-3天）

**功能**：
- ✅ 显示字幕时间轴（类似视频轨道）
- ✅ 拖动字幕条改变时间
- ✅ 拖动边缘改变持续时间
- ✅ 播放时高亮当前字幕

**实现**：
```javascript
// 使用Canvas或SVG绘制时间轴
<div class="timeline">
  <div class="timeline-track">
    <div 
      v-for="sub in subtitles" 
      :key="sub.id"
      class="subtitle-bar"
      :style="{
        left: (sub.startTime / totalDuration * 100) + '%',
        width: (sub.duration / totalDuration * 100) + '%'
      }"
      @mousedown="startDrag(sub)"
    >
      {{ sub.text.substring(0, 10) }}...
    </div>
  </div>
</div>

// 拖动逻辑
function startDrag(subtitle) {
  // 监听鼠标移动
  // 更新subtitle.startTime或subtitle.duration
  // 实时更新UI
}
```

---

### 阶段3：视频预览同步（1-2天）

**功能**：
- ✅ 播放视频时显示字幕
- ✅ 字幕样式实时预览
- ✅ 拖动进度条跳转

**实现**：
```javascript
// HTML5 Video + Canvas字幕层
<div class="video-preview">
  <video ref="videoPlayer" @timeupdate="onTimeUpdate">
    <source :src="videoUrl" type="video/mp4">
  </video>
  <canvas ref="subtitleCanvas" class="subtitle-overlay"></canvas>
</div>

// 实时渲染字幕
function onTimeUpdate(e) {
  const currentTime = e.target.currentTime;
  const currentSub = findSubtitleByTime(currentTime);
  
  if (currentSub) {
    drawSubtitle(currentSub);
  }
}

function drawSubtitle(subtitle) {
  const ctx = this.$refs.subtitleCanvas.getContext('2d');
  ctx.font = `${subtitle.style.fontSize}px ${subtitle.style.fontName}`;
  ctx.fillStyle = subtitle.style.fontColor;
  ctx.strokeStyle = subtitle.style.borderColor;
  ctx.lineWidth = subtitle.style.borderWidth;
  
  // 绘制字幕文本
  ctx.strokeText(subtitle.text, x, y);
  ctx.fillText(subtitle.text, x, y);
}
```

---

### 阶段4：高级特效编辑（2-3天，可选）

**功能**：
- ✅ 逐字卡拉OK效果
- ✅ 字幕位置拖动
- ✅ 多种动画效果切换
- ✅ 字幕分组和层级

---

## 💰 开发成本估算

### 方案对比

| 方案 | 开发时间 | 功能完整度 | 用户体验 | 维护成本 | 推荐度 |
|------|---------|----------|---------|---------|--------|
| **方案B-阶段1** | 1-2天 | ⭐⭐⭐ | ⭐⭐⭐ | 低 | ⭐⭐⭐⭐⭐ |
| 方案B-阶段1+2 | 3-5天 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中 | ⭐⭐⭐⭐ |
| 方案B-完整版 | 7-10天 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 高 | ⭐⭐⭐ |
| 方案C（ASS编辑器） | 1-2天 | ⭐⭐⭐⭐⭐ | ⭐⭐ | 低 | ⭐⭐⭐ |
| 方案A（桌面应用） | 90-180天 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 高 | ⭐ |

---

## 🎯 推荐实施路线

### 第一步：基础编辑功能（推荐立即实施）

**时间**：1-2天  
**功能**：
1. 字幕列表展示（表格形式）
2. 编辑字幕文本
3. 调整字幕时间
4. 调整字幕样式（字体、大小、颜色、动画）
5. 保存并重新生成视频

**优点**：
- ✅ 快速实现，立即可用
- ✅ 满足80%的编辑需求
- ✅ 可以后续迭代升级

---

### 第二步：时间轴可视化（可选）

**时间**：2-3天  
**功能**：
1. 时间轴UI展示
2. 拖动调整时间
3. 视频预览同步

**条件**：
- ⚠️ 如果用户反馈需要更直观的时间编辑
- ⚠️ 如果有充足的开发时间

---

### 第三步：高级特效（可选）

**时间**：2-3天  
**功能**：
1. 逐字卡拉OK
2. 字幕位置拖动
3. 更多动画效果

**条件**：
- ⚠️ 如果有专业视频制作需求
- ⚠️ 如果预算充足

---

## 📝 具体实现文件清单

### 阶段1需要创建的文件

#### 前端文件
```
hm-service/src/main/resources/static/
├─ subtitle-editor.html         # 字幕编辑器页面
└─ js/
   └─ subtitle-editor.js        # 字幕编辑逻辑
```

#### 后端文件
```
com.hmall.tts.video/
├─ controller/
│  └─ SubtitleEditorController.java      # 字幕编辑API
├─ service/
│  └─ SubtitleEditorService.java         # 字幕编辑服务
└─ dto/
   ├─ SubtitleUpdateRequest.java         # 更新请求DTO
   └─ SubtitleEditData.java               # 编辑数据DTO
```

---

## 🎨 界面设计预览

### 基础编辑界面（阶段1）

```
┌─────────────────────────────────────────────────────────┐
│  字幕编辑器                                   [保存] [预览] │
├─────────────────────────────────────────────────────────┤
│  视频信息                                                 │
│  视频：xxx.mp4    时长：28.6秒    字幕数：7条            │
├─────────────────────────────────────────────────────────┤
│  序号 │ 文本内容          │ 开始时间 │ 持续时间 │ 操作  │
│  ───  │ ───────────────── │ ──────── │ ──────── │ ───  │
│   1   │ 你好，我是云舟... │  0.0s    │  4.5s    │ 编辑 │
│   2   │ 云舟，我也很...   │  4.5s    │  3.8s    │ 编辑 │
│   3   │ 你来自哪里？      │  8.3s    │  2.1s    │ 编辑 │
│  ...  │ ...               │  ...     │  ...     │ ...  │
├─────────────────────────────────────────────────────────┤
│  编辑当前字幕 (序号 1)                                    │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 文本内容：                                         │ │
│  │ [你好，我是云舟，很高兴认识你                ____] │ │
│  │                                                    │ │
│  │ 开始时间：[0.0    __] 秒                          │ │
│  │ 持续时间：[4.5    __] 秒                          │ │
│  │                                                    │ │
│  │ 字体名称：[Microsoft YaHei ▼]                     │ │
│  │ 字体大小：[======●======] 64px                   │ │
│  │ 字体颜色：[🎨] #FFFFFF                            │ │
│  │ 边框颜色：[🎨] #000000                            │ │
│  │ 边框粗细：[===●========] 3px                     │ │
│  │ 字幕位置：[底部居中 ▼]                            │ │
│  │ 动画效果：[渐入渐出 ▼]                            │ │
│  │                                                    │ │
│  │                     [取消] [应用修改]              │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 实施建议

### 推荐：阶段1（基础编辑）

**理由**：
1. ✅ **快速交付**：1-2天即可完成
2. ✅ **满足需求**：覆盖大部分编辑场景
3. ✅ **易于维护**：代码简单，bug少
4. ✅ **可扩展**：后续可以升级到可视化编辑

**适合场景**：
- 修改字幕文本（错别字、内容调整）
- 调整字幕时间（同步问题）
- 更换字幕样式（字体、颜色、大小）
- 切换字幕动画效果

---

### 可选：阶段2（时间轴可视化）

**前提条件**：
- ⚠️ 用户反馈阶段1不够直观
- ⚠️ 有额外2-3天开发时间
- ⚠️ 预算允许

**增加的价值**：
- ✅ 更直观的时间调整
- ✅ 更好的用户体验
- ✅ 接近剪映的体验

---

## ❓ 需要确认的问题

### 问题1：实施范围
请选择实施方案：
- [ ] **方案1**：基础字幕编辑（1-2天，推荐⭐⭐⭐⭐⭐）
- [ ] **方案2**：基础 + 时间轴可视化（3-5天）
- [ ] **方案3**：完整版（7-10天）
- [ ] **方案4**：ASS文件编辑器（1-2天，专业用户）

### 问题2：优先级
最需要的编辑功能（多选）：
- [ ] 修改字幕文本内容
- [ ] 调整字幕显示时间
- [ ] 更换字幕样式（字体、颜色）
- [ ] 切换字幕动画效果
- [ ] 调整字幕位置（上、中、下）
- [ ] 拆分/合并字幕
- [ ] 添加/删除字幕

### 问题3：界面风格
倾向的编辑方式：
- [ ] 表格形式（Excel风格，简单直接）
- [ ] 时间轴形式（剪映风格，可视化）
- [ ] 混合形式（表格 + 时间轴）

---

**请告诉我你的选择，我会根据你的需求制定详细的实施方案！** 🎯

