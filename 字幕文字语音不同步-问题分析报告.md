# 🔴 严重问题：字幕文字与语音不同步

**发现时间：** 2026-08-14  
**问题严重度：** 🔴 P0（严重）  
**影响范围：** 简单版 + 专业版

---

## 🔍 问题现象

**用户反馈：** 文字和语音对应不上

**具体表现：**
1. 字幕显示的文本和音频播放的内容不一致
2. 字幕顺序和音频顺序可能错乱
3. 字幕时间和音频时间不同步

---

## 🔎 根本原因分析

### 原因分析（通过代码审查）

我检查了代码流程，发现了核心问题：

#### 问题1：字幕文本来源错误 ⭐

**代码位置：** `DocumentTTSServiceImpl.java` - `buildDialogSegments()` 方法

**问题描述：**
```java
// 当前实现（第170-270行）
private List<DialogSegment> buildDialogSegments(
    List<TextSegment> originalSegments,  // ← 这是原始文本片段
    List<AudioSegment> audioSegments,    // ← 这是TTS生成的音频片段
    VoiceConfig voiceConfig
) {
    // 问题：使用originalSegments作为字幕文本
    // 但音频是基于mergedSegments和split后的片段生成的
    // 两者不一致！
}
```

**流程对比：**

```
正确的音频生成流程：
originalSegments（原始文本）
  → merge（合并相同音色）
  → split（拆分过长片段）
  → finalSegments（最终片段）
  → TTS API调用
  → audioSegments（音频片段）

当前的字幕生成流程：
originalSegments（原始文本）← ❌ 直接使用
  → buildDialogSegments
  → DialogSegment（字幕片段）

问题：字幕用的是originalSegments，但音频用的是finalSegments！
```

---

#### 问题2：字幕和音频的映射关系错误 ⭐⭐

**代码位置：** `buildDialogSegments()` 第220-270行

**问题代码：**
```java
// 步骤2：根据AudioSegment的实际音频时长构建DialogSegment
int audioIndex = 0;
for (int lineIndex = 0; lineIndex < lines.size(); lineIndex++) {
    LineInfo line = lines.get(lineIndex);  // ← 来自originalSegments
    
    // 找到对应的AudioSegment（相同音色）
    while (audioIndex < audioSegments.size()) {
        AudioSegment audioSegment = audioSegments.get(audioIndex); // ← 来自finalSegments
        
        // ❌ 问题：line是基于originalSegments的
        // 但audioSegment是基于finalSegments的
        // 两者的数量和内容可能完全不一致！
    }
}
```

**为什么会不一致？**

1. **merge合并：** 连续相同音色的片段会被合并
   ```
   原始：[你好][，][我是][云舟]
   合并：[你好，我是云舟]
   结果：4个片段 → 1个片段
   ```

2. **split拆分：** 过长的片段会被拆分
   ```
   原始：[一段很长很长...的文本（1000字）]
   拆分：[前800字] + [后200字]
   结果：1个片段 → 2个片段
   ```

3. **映射错乱：**
   ```
   originalSegments: [A][B][C][D][E]  (5个片段)
   ↓ merge + split
   finalSegments:    [AB][C_part1][C_part2][DE]  (4个片段)
   ↓ TTS
   audioSegments:    [音频AB][音频C1][音频C2][音频DE]  (4个音频)
   
   ❌ 当前逻辑：尝试把5个文本片段映射到4个音频片段
   结果：映射关系完全错乱！
   ```

---

#### 问题3：字幕文本不是实际发音的文本 ⭐⭐⭐

**核心问题：** 字幕显示的是`originalSegments`的文本，但音频播放的是`finalSegments`的文本！

**示例场景：**

```
原始文档：
第1行：你好（加粗）
第2行：我是云舟（普通）
第3行：很高兴认识你（加粗）

originalSegments（5个片段）：
1. [你好]（speaker=BV001_streaming, isBold=true）
2. [我是云舟]（speaker=BV700_streaming, isBold=false）
3. [很高兴认识你]（speaker=BV001_streaming, isBold=true）

↓ merge（合并相同音色）

mergedSegments（2个片段）：
1. [你好很高兴认识你]（speaker=BV001_streaming）← 合并了第1和第3
2. [我是云舟]（speaker=BV700_streaming）

↓ TTS生成音频

audioSegments（2个音频）：
1. 音频1：播放"你好很高兴认识你"
2. 音频2：播放"我是云舟"

↓ 当前的buildDialogSegments逻辑

DialogSegments（字幕，基于originalSegments）：
1. 字幕1：显示"你好"（0-2秒）
2. 字幕2：显示"我是云舟"（2-4秒）
3. 字幕3：显示"很高兴认识你"（4-6秒）

❌ 结果：
- 0-2秒：字幕显示"你好"，但音频播放"你好很高兴认识你"
- 2-4秒：字幕显示"我是云舟"，但音频播放"我是云舟"（✅ 碰巧对上）
- 4-6秒：字幕显示"很高兴认识你"，但音频已经播放完了（❌ 完全错乱）
```

---

## 📊 问题影响分析

### 影响场景

| 场景 | 影响程度 | 说明 |
|------|---------|------|
| 简单文档（1-2段） | 🟡 中等 | 可能碰巧对上 |
| 多音色文档 | 🔴 严重 | 合并后完全错乱 |
| 长文档（拆分） | 🔴 严重 | 拆分后映射错误 |
| 专业版实时预览 | 🔴 严重 | 字幕和音频完全对不上 |

---

## 💡 解决方案

### 方案A：修复映射关系（推荐⭐⭐⭐）

**核心思路：** 字幕应该基于`finalSegments`（TTS实际发音的文本），而不是`originalSegments`

**实施步骤：**

1. **保存finalSegments到AudioSegment**
   ```java
   class AudioSegment {
       private MergedSegment mergedSegment;  // ← 已有
       private byte[] audioData;
       private Double duration;
       // ✅ 新增：保存原始文本信息（用于字幕显示）
       private String displayText;  // 显示的文本（可能被合并或拆分）
   }
   ```

2. **在buildDialogSegments中使用finalSegments**
   ```java
   private List<DialogSegment> buildDialogSegments(
       List<AudioSegment> audioSegments,  // ← 直接使用音频片段
       VoiceConfig voiceConfig
   ) {
       List<DialogSegment> dialogSegments = new ArrayList<>();
       double currentTime = 0.0;
       
       for (int i = 0; i < audioSegments.size(); i++) {
           AudioSegment audioSegment = audioSegments.get(i);
           
           // 使用音频片段的文本（这才是实际发音的文本）
           String text = audioSegment.getMergedSegment().getText();
           
           // 计算时长
           double duration = calculateAudioDuration(
               audioSegment.getAudioData(), 
               voiceConfig.getFormat(), 
               voiceConfig.getSampleRate()
           );
           
           // 添加停顿
           if (audioSegment.getNeedPause()) {
               duration += audioSegment.getPauseDuration() / 1000.0;
           }
           
           // 创建DialogSegment
           DialogSegment segment = DialogSegment.builder()
                   .index(i)
                   .text(text)  // ✅ 使用实际发音的文本
                   .startTime(currentTime)
                   .duration(duration)
                   .voiceId(audioSegment.getMergedSegment().getSpeaker())
                   .build();
           
           dialogSegments.add(segment);
           currentTime += duration;
       }
       
       return dialogSegments;
   }
   ```

3. **修改调用处**
   ```java
   // 原来
   List<DialogSegment> dialogSegments = buildDialogSegments(
       segments,        // ← ❌ 错误的文本
       audioSegments,
       voiceConfig
   );
   
   // 修改后
   List<DialogSegment> dialogSegments = buildDialogSegments(
       audioSegments,   // ✅ 直接使用音频片段（包含正确的文本）
       voiceConfig
   );
   ```

---

### 方案B：保持原始文本结构（不推荐）

**核心思路：** 不merge和split，保持原始文本结构

**问题：**
- TTS API有800字限制，必须拆分
- 不合并相同音色会导致大量API调用（慢+贵）

**结论：** 不推荐

---

### 方案C：双重映射（复杂，不推荐）

**核心思路：** 建立originalSegments到finalSegments的映射关系

**问题：**
- 实现复杂，容易出错
- 维护困难

**结论：** 不推荐

---

## 🎯 推荐方案

### 推荐：方案A（修复映射关系）

**优势：**
1. ✅ 简单直接
2. ✅ 100%准确（字幕=实际发音）
3. ✅ 易于维护
4. ✅ 不影响TTS性能

**实施难度：** 🟢 简单（约1小时）

**影响范围：**
- 修改：`DocumentTTSServiceImpl.java` - `buildDialogSegments()` 方法
- 修改：调用处（1处）
- 不影响：其他功能

---

## 📝 实施计划

### 第1步：修改buildDialogSegments方法（30分钟）

**修改内容：**
1. 移除`originalSegments`参数
2. 直接从`audioSegments`提取文本
3. 简化逻辑（不需要复杂的行合并）

---

### 第2步：测试验证（30分钟）

**测试场景：**
1. **简单文档：** 1-2段文本
2. **多音色文档：** 加粗+普通交替
3. **长文档：** 触发拆分（800字以上）
4. **专业版预览：** 实时预览字幕同步

**预期结果：**
- ✅ 字幕文本=音频内容
- ✅ 字幕时间=音频时间
- ✅ 字幕顺序=音频顺序

---

## ⚠️ 注意事项

### 注意1：字幕数量可能变化

**原来：** 字幕数量=原始文本行数

**修改后：** 字幕数量=TTS API调用次数（可能更少）

**影响：**
- 相同音色的连续行会合并为一条字幕
- 过长的行会拆分为多条字幕

**示例：**
```
原始文档（3行）：
第1行：你好（加粗）
第2行：我是云舟（普通）
第3行：很高兴认识你（加粗）

原来的字幕（3条）：
1. 你好
2. 我是云舟
3. 很高兴认识你

修改后的字幕（2条）：← 第1行和第3行合并
1. 你好很高兴认识你
2. 我是云舟
```

**这样合理吗？** ✅ 完全合理！
- 字幕和音频完全同步
- 用户听到什么，就看到什么

---

### 注意2：字幕显示时长可能更长

**原因：** 合并后的文本更长，显示时间也更长

**影响：** 无（这是正确的行为）

---

### 注意3：isBold属性可能丢失

**原因：** merge后只保留第一个片段的isBold

**影响：** 无（音频已经生成，isBold只是显示属性）

**解决：** 如果需要保留，可以在AudioSegment中保存isBold信息

---

## 🎬 剪映是怎么做的？

### 剪映的字幕生成逻辑

1. **语音识别（ASR）：** 从音频中识别出文本
   ```
   音频 → ASR识别 → 文本（带时间戳）
   ```

2. **字幕=识别结果：** 字幕直接使用识别的文本
   ```
   识别文本：[你好]（0-1秒）[我是云舟]（1-3秒）
   字幕：[你好]（0-1秒）[我是云舟]（1-3秒）
   ```

3. **100%同步：** 因为字幕来自音频，所以100%同步

### 我们的逻辑应该是什么？

**我们的流程：**
```
文本 → TTS合成 → 音频
       ↓
      字幕（应该基于TTS实际发音的文本）
```

**正确的逻辑：**
- 字幕应该显示TTS实际发音的文本
- 不是原始文本，而是merge+split后的文本
- 这样才能100%同步

---

## 📊 对比总结

| 方案 | 字幕来源 | 同步性 | 实施难度 | 推荐 |
|------|---------|--------|---------|------|
| 当前实现 | originalSegments | ❌ 不同步 | - | ❌ |
| **方案A** | **finalSegments（audioSegments）** | **✅ 100%同步** | **🟢 简单** | **✅ 推荐** |
| 方案B | 不merge/split | ✅ 同步 | 🟡 中等 | ❌ 性能差 |
| 方案C | 双重映射 | ✅ 同步 | 🔴 复杂 | ❌ 难维护 |

---

## 🎯 结论

**核心问题：** 字幕文本来源错误，应该基于`audioSegments`（实际发音的文本），而不是`originalSegments`

**推荐方案：** 方案A - 修复映射关系

**实施时间：** 约1小时

**风险评估：** 🟢 低风险（逻辑简化，更容易维护）

---

**等待用户确认后开始实施！** 🚀
