# 🔍 逐字高亮慢 - 诊断步骤

**问题：** 文字高亮完全没有跟上语音的速度  
**时间：** 2026-08-14 05:15

---

## 🚀 立即诊断

### 步骤1：强制刷新浏览器

```
按 Ctrl + Shift + Delete
清除缓存
或者直接 Ctrl + F5 强制刷新
```

### 步骤2：重新生成语音

1. 上传Word文档
2. 点击"生成对话语音"
3. 等待生成完成

### 步骤3：打开控制台查看日志

按F12打开控制台，应该看到：

```javascript
[音频事件] timeupdate事件已绑定
[音频播放] 开始播放，当前时间: 0.00
[音频时间更新] 当前时间: 0.50s
[逐字高亮] 时间0.50s，片段0，进度12.5%，高亮第2/16字，字符="好"
[音频时间更新] 当前时间: 1.00s
[逐字高亮] 时间1.00s，片段0，进度25.0%，高亮第4/16字，字符="我"
...
```

---

## 🔍 问题分析

### 可能的问题1：dialogSegments为空

**症状：** 控制台没有任何逐字高亮日志

**检查方法：**
```javascript
// 在控制台执行
console.log('dialogSegments:', dialogSegments);
console.log('片段数量:', dialogSegments.length);
```

**解决方案：** 重新生成语音

---

### 可能的问题2：片段时间不对

**症状：** 逐字高亮日志显示片段索引一直是-1

**检查方法：**
```javascript
// 在控制台执行
const audio = document.getElementById('audio');
console.log('当前时间:', audio.currentTime);

dialogSegments.forEach((seg, i) => {
    console.log(`片段${i}:`, 
                `开始=${seg.startTime.toFixed(2)}s`,
                `结束=${(seg.startTime + seg.duration).toFixed(2)}s`);
});
```

**解决方案：** 检查后端生成的时间戳是否正确

---

### 可能的问题3：字符元素没有渲染

**症状：** 控制台显示"片段X没有字符元素"

**检查方法：**
```javascript
// 在控制台执行
document.querySelectorAll('.char').forEach((char, i) => {
    console.log(`字${i}:`, char.textContent);
});
```

**解决方案：** 检查renderDocumentPreview函数是否正确执行

---

### 可能的问题4：CSS transition太慢

**症状：** 逐字高亮有延迟动画

**检查方法：** 查看CSS中的transition设置

**解决方案：** 减少transition时间或禁用

```css
/* 当前设置 */
.char {
    transition: all 0.15s ease;
}

/* 改为更快 */
.char {
    transition: all 0.05s ease;
}

/* 或者禁用 */
.char {
    transition: none;
}
```

---

## 🎯 快速测试方案

### 方案1：禁用逐字高亮，只保留整句

```javascript
// 在 bindAudioTimeUpdate 函数中注释掉逐字高亮
audio.addEventListener('timeupdate', () => {
    const currentTime = audio.currentTime;
    updateSegmentHighlight(currentTime);
    // updateCharHighlight(currentTime); // ← 注释掉
});
```

**测试：** 整句黄色背景是否准确？
- 如果准确：说明问题出在逐字高亮
- 如果不准确：说明问题出在音频加载或时间戳

---

### 方案2：简化逐字高亮逻辑

```javascript
// 在控制台执行，手动测试逐字高亮
const audio = document.getElementById('audio');
const currentTime = audio.currentTime;

console.log('当前时间:', currentTime);

const seg = dialogSegments[0];
const progress = (currentTime - seg.startTime) / seg.duration;
const charIndex = Math.floor(progress * 10); // 假设10个字

console.log('播放进度:', (progress * 100).toFixed(1) + '%');
console.log('应该高亮第几个字:', charIndex);
```

---

## 📊 期望的日志输出

### 正常情况下，控制台应该看到：

```
[音频事件] timeupdate事件已绑定
文档预览渲染完成，共 5 个片段（智能时长分配）
[音频播放] 开始播放，当前时间: 0.00

// 每0.5秒输出一次逐字高亮状态
[逐字高亮] 时间0.50s，片段0，进度10.0%，高亮第1/10字，字符="好"
[逐字高亮] 时间1.00s，片段0，进度20.0%，高亮第2/10字，字符="，"
[逐字高亮] 时间1.50s，片段0，进度30.0%，高亮第3/10字，字符="我"
...

// 每1秒输出一次音频时间
[音频时间更新] 当前时间: 1.00s
[音频时间更新] 当前时间: 2.00s
[音频时间更新] 当前时间: 3.00s
```

---

## 🔧 紧急修复方案

### 如果所有方法都不行，使用requestAnimationFrame

```javascript
// 替换 timeupdate 事件为 requestAnimationFrame
function bindAudioTimeUpdate(audio) {
    let animationId = null;
    
    function updateHighlight() {
        const currentTime = audio.currentTime;
        
        updateSegmentHighlight(currentTime);
        updateCharHighlight(currentTime);
        
        if (!audio.paused) {
            animationId = requestAnimationFrame(updateHighlight);
        }
    }
    
    audio.addEventListener('play', () => {
        if (animationId) cancelAnimationFrame(animationId);
        updateHighlight();
    });
    
    audio.addEventListener('pause', () => {
        if (animationId) cancelAnimationFrame(animationId);
    });
}
```

**优势：** 更高频率更新（60fps），更流畅

---

## 📝 请执行以下操作并反馈

1. ✅ **刷新浏览器**（Ctrl+F5）
2. ✅ **重新生成语音**
3. ✅ **打开F12控制台**
4. ✅ **复制所有日志发给我**

特别关注：
- 是否看到"[音频事件] timeupdate事件已绑定"？
- 是否看到"[逐字高亮]"的日志？
- 如果看到，日志中显示的时间、进度、字符是否正确？

---

**等待你的反馈！**
