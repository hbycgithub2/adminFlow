# 🎬 字幕编辑器专业版 - 使用指南

**版本：** v2.0 Pro  
**功能：** 实时预览字幕编辑，所见即所得  
**完成时间：** 2026-08-14

---

## 🎯 核心特性

### ✨ 专业版 vs 简单版对比

| 功能 | 简单版 | 专业版 ⭐ |
|------|--------|---------|
| 字幕列表 | ✅ 表格形式 | ✅ 卡片式，实时高亮 |
| 编辑字幕 | ✅ 对话框编辑 | ✅ 侧边栏编辑 |
| 样式调整 | ✅ 支持 | ✅ 支持 + 滑块优化 |
| **视频预览** | ❌ | ✅ **实时预览** |
| **Canvas渲染** | ❌ | ✅ **实时渲染字幕** |
| **播放控制** | ❌ | ✅ **播放/暂停/进度条** |
| **编辑同步** | ❌ | ✅ **修改样式立即预览** |
| **字幕高亮** | ❌ | ✅ **播放时自动高亮** |
| **位置九宫格** | ❌ | ✅ **可视化位置选择** |

---

## 🚀 快速开始

### 第1步：启动服务

```bash
cd d:\code\adminFlow\hm-service
mvn spring-boot:run
```

### 第2步：打开专业版编辑器

访问：`http://localhost:8080/subtitle-editor-pro.html`

**预期界面：**
- 🎨 **深色主题**（仿剪映风格）
- 📹 **左侧：视频预览区**（带播放器和进度条）
- ✏️ **右侧：编辑面板**（字幕列表 + 样式编辑器）

### 第3步：加载字幕并预览

1. **输入任务ID** → 粘贴到顶部输入框
2. **点击「加载字幕」** → 等待1-2秒
3. **视频自动加载** → 左侧显示视频画面
4. **点击播放按钮 ▶** → 视频开始播放
5. **观察字幕渲染** → Canvas实时渲染字幕，跟随视频播放

---

## 🎨 核心功能详解

### 功能1：实时视频预览 📹

**工作原理：**
- HTML5 Video播放器加载视频
- Canvas覆盖在视频上方
- 根据播放时间自动渲染对应字幕

**使用方法：**
1. 加载字幕后，视频自动加载
2. 点击「▶ 播放」按钮开始播放
3. 字幕会自动跟随视频时间显示
4. 可以拖动进度条跳转到任意时间

**控制按钮：**
- **▶ / ⏸** - 播放/暂停
- **⏪** - 后退5秒
- **⏩** - 前进5秒
- **进度条** - 拖动跳转

---

### 功能2：Canvas实时渲染 🎨

**渲染内容：**
- ✅ 字幕文本
- ✅ 字体样式（名称、大小、颜色）
- ✅ 边框样式（颜色、粗细）
- ✅ 字幕位置（九宫格）
- ✅ 动画效果（渐入、飞入、缩放等）

**渲染时机：**
- 视频播放时，每帧自动渲染
- 修改样式后，立即重新渲染
- 跳转时间后，立即渲染新字幕

**关键代码逻辑：**
```javascript
video.addEventListener('timeupdate', () => {
    const currentSubtitle = findCurrentSubtitle(video.currentTime);
    if (currentSubtitle) {
        drawSubtitle(currentSubtitle);
    }
});
```

---

### 功能3：所见即所得编辑 ✏️

**核心特性：**
- 修改字体大小 → 立即在视频中看到效果
- 修改字体颜色 → 立即在视频中看到效果
- 修改位置 → 立即在视频中看到效果
- 修改动画 → 立即在视频中看到效果

**使用流程：**
1. 点击右侧字幕列表中的某条字幕
2. 视频自动跳转到该字幕的开始时间
3. 右侧显示样式编辑器
4. 修改任何样式参数
5. **Canvas立即重新渲染，实时预览效果**

**示例：修改字体大小**
```
1. 拖动「字体大小」滑块：64 → 80
2. Canvas立即重新绘制字幕
3. 视频中字幕变大（无需重新生成视频）
```

---

### 功能4：字幕列表与视频同步 🔄

**双向同步：**

**方向1：点击字幕 → 视频跳转**
- 点击右侧字幕列表中的某条字幕
- 视频自动跳转到该字幕的开始时间
- 该字幕高亮显示

**方向2：播放视频 → 高亮字幕**
- 视频播放时
- 右侧字幕列表自动高亮当前显示的字幕
- 自动滚动到高亮字幕（保持可见）

---

### 功能5：位置九宫格 📐

**可视化位置选择：**
```
↖  ↑  ↗
←  ●  →
↙  ↓  ↘
```

**对应位置：**
- **↙ (1)** - 左下角
- **↓ (2)** - 底部居中（默认）
- **↘ (3)** - 右下角
- **← (4)** - 左侧中间
- **● (5)** - 正中央
- **→ (6)** - 右侧中间
- **↖ (7)** - 左上角
- **↑ (8)** - 顶部居中
- **↗ (9)** - 右上角

**使用方法：**
1. 选择一条字幕
2. 点击九宫格中的任意按钮
3. Canvas立即重新渲染字幕到新位置

---

### 功能6：8种动画效果 ✨

| 动画 | 效果 | 适用场景 |
|------|------|---------|
| **渐入渐出** | 淡入淡出 | 通用，自然流畅 |
| **从下飞入** | 从底部飞入 | 动感强，适合快节奏 |
| **从上飞入** | 从顶部飞入 | 快速出现 |
| **从左飞入** | 从左侧飞入 | 侧向进入 |
| **从右飞入** | 从右侧飞入 | 侧向进入 |
| **缩放进入** | 从小到大 | 有冲击力 |
| **弹跳效果** | 弹性动画 | 活泼可爱 |
| **无动画** | 直接显示 | 简洁，无干扰 |

**动画时长：** 0.3秒（前0.3秒有动画效果）

---

## 📋 完整使用流程

### 流程1：加载和预览

```
1. 打开专业版编辑器
   http://localhost:8080/subtitle-editor-pro.html

2. 输入任务ID
   例如：703f1a7a-0696-4a1c-9d3c-19646b585b21

3. 点击「加载字幕」
   → 视频加载
   → 字幕列表显示

4. 点击「▶ 播放」
   → 视频播放
   → 字幕自动渲染
   → 跟随视频时间显示
```

### 流程2：编辑字幕文本

```
1. 点击右侧字幕列表中的某条字幕
   → 视频跳转到该字幕时间
   → 右侧显示样式编辑器

2. 修改「文本内容」
   输入：你好，我是云舟 → 你好，我是云舟，很高兴认识你

3. Canvas立即重新渲染
   → 视频中字幕文本已更新

4. 点击「💾 保存修改」
   → 字幕保存到服务器
```

### 流程3：调整字幕样式

```
1. 选择某条字幕

2. 修改样式：
   - 字体大小：64 → 80（拖动滑块）
   - 字体颜色：白色 → 黄色（点击颜色块）
   - 边框粗细：3 → 4
   - 字幕位置：底部居中 → 顶部居中（点击九宫格）
   - 动画效果：渐入渐出 → 从下飞入

3. 每次修改后，Canvas立即重新渲染
   → 实时预览效果

4. 满意后，点击「💾 保存修改」
```

### 流程4：重新生成视频

```
1. 编辑完所有字幕

2. 点击「🎥 重新生成视频」
   → 确认对话框

3. 等待30-60秒
   → 服务器重新生成视频

4. 提示「视频重新生成成功」
   → 视频自动重新加载
   → 播放验证效果
```

---

## 🎬 使用场景示例

### 场景1：修正字幕文本

**需求：** 字幕有错别字，需要修正

**操作步骤：**
1. 加载字幕
2. 播放视频，找到有错别字的字幕
3. 点击该字幕，视频自动跳转
4. 修改「文本内容」
5. 实时预览：字幕文本已更新
6. 保存修改
7. 重新生成视频

**效果：** 新视频字幕文本已修正

---

### 场景2：调整字幕时间

**需求：** 字幕出现时间不准确，需要延后0.5秒

**操作步骤：**
1. 加载字幕
2. 播放视频，找到时间不准的字幕
3. 点击该字幕
4. 修改「开始时间」：4.5 → 5.0
5. 点击播放，验证时间
6. 保存修改
7. 重新生成视频

**效果：** 新视频字幕时间已调整

---

### 场景3：更换字幕样式

**需求：** 字幕不够醒目，需要改成黄色大字

**操作步骤：**
1. 加载字幕
2. 点击第一条字幕
3. 修改样式：
   - 字体颜色：白色 → 黄色
   - 字体大小：64 → 80
   - 边框粗细：3 → 4
4. **实时预览：视频中字幕已变成黄色大字**
5. 保存修改
6. 重新生成视频

**效果：** 新视频字幕样式已更新

---

### 场景4：调整字幕位置

**需求：** 字幕遮挡了视频中的重要内容，需要移动到顶部

**操作步骤：**
1. 加载字幕
2. 播放视频，找到被遮挡的地方
3. 点击该字幕
4. 点击九宫格中的「↑」（顶部居中）
5. **实时预览：字幕已移动到顶部**
6. 验证不再遮挡
7. 保存修改
8. 重新生成视频

**效果：** 新视频字幕位置已调整

---

### 场景5：测试不同动画效果

**需求：** 想尝试不同的动画效果，选择最合适的

**操作步骤：**
1. 加载字幕
2. 点击第一条字幕
3. 视频跳转到该字幕的开始时间
4. 选择「从下飞入」
5. **实时预览：字幕从底部飞入**
6. 后退5秒，重新播放
7. 选择「缩放进入」
8. **实时预览：字幕从小到大**
9. 重复测试其他动画
10. 选择最满意的效果
11. 保存修改

**优势：** 无需重新生成视频，实时预览所有动画效果

---

## ⚙️ 技术细节

### 技术架构

```
┌─────────────────────────────────────────┐
│          subtitle-editor-pro.html       │
│                                         │
│  ┌────────────────┐  ┌──────────────┐  │
│  │  Video Player  │  │ Subtitle List │  │
│  │  + Canvas      │  │              │  │
│  └────────────────┘  └──────────────┘  │
│          ↓                   ↓          │
│  ┌────────────────────────────────────┐ │
│  │   subtitle-editor-pro.js          │ │
│  │                                    │ │
│  │  - VideoPlayer (播放控制)         │ │
│  │  - SubtitleRenderer (Canvas渲染)  │ │
│  │  - StyleEditor (样式编辑)         │ │
│  │  - Sync (双向同步)                │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
                    ↓
            REST API (后端)
        /api/subtitle-editor/load
        /api/subtitle-editor/update
```

### 核心类结构

```javascript
class SubtitleEditorPro {
    // 数据
    taskId: string
    subtitles: SubtitleSegment[]
    currentSubtitleIndex: number
    videoUrl: string
    
    // DOM元素
    video: HTMLVideoElement
    canvas: HTMLCanvasElement
    ctx: CanvasRenderingContext2D
    
    // 核心方法
    loadSubtitles()           // 加载字幕数据
    renderSubtitle()          // 渲染字幕到Canvas
    findCurrentSubtitle()     // 查找当前时间的字幕
    drawSubtitle()            // 绘制字幕（应用样式）
    calculatePosition()       // 计算九宫格位置
    applyAnimation()          // 应用动画效果
    onStyleChange()           // 样式变化时触发
    highlightCurrentSubtitle()// 高亮当前字幕
}
```

### Canvas渲染流程

```javascript
// 1. 视频播放时触发
video.addEventListener('timeupdate', () => {
    // 2. 查找当前时间的字幕
    const subtitle = findCurrentSubtitle(video.currentTime);
    
    if (subtitle) {
        // 3. 清空Canvas
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        // 4. 计算位置（九宫格）
        const pos = calculatePosition(subtitle.style.position);
        
        // 5. 应用动画效果
        const animatedPos = applyAnimation(subtitle.style.animationType, pos);
        
        // 6. 绘制边框
        ctx.strokeStyle = subtitle.style.borderColor;
        ctx.lineWidth = subtitle.style.borderWidth;
        ctx.strokeText(subtitle.text, animatedPos.x, animatedPos.y);
        
        // 7. 绘制字体
        ctx.fillStyle = subtitle.style.fontColor;
        ctx.fillText(subtitle.text, animatedPos.x, animatedPos.y);
    }
});
```

### 实时同步机制

```javascript
// 样式变化 → 立即渲染
onStyleChange() {
    // 1. 更新数据模型
    subtitle.style.fontSize = input.value;
    
    // 2. 立即重新渲染
    renderSubtitle();  // ← 关键：立即刷新Canvas
    
    // 3. 更新列表显示
    renderSubtitleList();
}

// 视频播放 → 高亮字幕
onTimeUpdate() {
    // 1. 查找当前字幕
    const currentIndex = findCurrentSubtitleIndex(video.currentTime);
    
    // 2. 高亮列表中的字幕
    highlightSubtitle(currentIndex);
    
    // 3. 自动滚动到当前字幕
    scrollToSubtitle(currentIndex);
}
```

---

## 🔧 故障排查

### 问题1：视频加载失败

**症状：** 点击加载后，视频区域一片黑

**可能原因：**
- 视频文件不存在
- 视频URL错误
- 浏览器不支持视频格式

**解决方案：**
1. 检查视频文件是否存在：`./tts/videos/{taskId}.mp4`
2. 检查浏览器控制台是否有错误
3. 尝试直接访问视频URL：`http://localhost:8080/tts/videos/{taskId}.mp4`

---

### 问题2：字幕不显示

**症状：** 视频播放，但没有字幕显示

**可能原因：**
- Canvas未正确覆盖在视频上
- 字幕时间不匹配
- Canvas尺寸问题

**解决方案：**
1. 打开浏览器开发者工具（F12）
2. 检查Canvas元素是否存在
3. 检查Canvas的width和height是否正确
4. 检查console是否有错误信息

---

### 问题3：修改样式后不生效

**症状：** 修改了字体大小，但视频中没有变化

**可能原因：**
- onStyleChange()没有触发
- renderSubtitle()没有执行
- 当前时间不在该字幕的时间范围内

**解决方案：**
1. 确保选中了某条字幕
2. 确保视频时间在该字幕的时间范围内
3. 检查浏览器控制台是否有JavaScript错误

---

### 问题4：视频和字幕不同步

**症状：** 字幕和视频播放对不上

**可能原因：**
- 字幕时间配置错误
- 视频缓冲延迟
- Canvas渲染延迟

**解决方案：**
1. 检查字幕的开始时间和持续时间是否正确
2. 暂停视频，检查当前时间是否在字幕时间范围内
3. 刷新页面重新加载

---

## 💡 使用技巧

### 技巧1：快速预览所有字幕

**方法：**
1. 点击播放按钮
2. 视频从头播放
3. 字幕自动跟随，实时渲染
4. 观察所有字幕的显示效果

**优势：** 一次播放，查看所有字幕

---

### 技巧2：精确编辑某条字幕

**方法：**
1. 点击右侧字幕列表中的某条字幕
2. 视频自动跳转到该字幕的开始时间
3. 右侧显示样式编辑器
4. 修改任何样式参数
5. 实时预览效果

**优势：** 快速定位，精确编辑

---

### 技巧3：对比不同样式效果

**方法：**
1. 选择某条字幕
2. 修改字体颜色为黄色
3. 观察效果
4. 后退5秒，重新播放
5. 修改字体颜色为红色
6. 观察效果
7. 选择最满意的颜色

**优势：** 无需重新生成视频，快速对比

---

### 技巧4：批量调整样式（未来功能）

**当前限制：** 只能逐条编辑

**解决方法：**
1. 编辑第一条字幕的样式
2. 记住样式参数
3. 逐条应用到其他字幕

**未来版本：** 将支持批量应用样式

---

## 📊 性能参考

| 操作 | 响应时间 | 说明 |
|------|---------|------|
| 加载字幕 | 1-2秒 | 加载视频+字幕数据 |
| 视频播放 | 实时 | 60fps流畅播放 |
| Canvas渲染 | <16ms | 每帧渲染时间 |
| 样式修改 | 立即 | <50ms重新渲染 |
| 保存字幕 | 0.5-1秒 | 写入ASS文件 |
| 重新生成视频 | 30-60秒 | 取决于视频长度 |

---

## 🎉 总结

### 专业版的核心优势

1. **✅ 实时预览** - 修改样式立即看到效果
2. **✅ 所见即所得** - 编辑体验接近剪映
3. **✅ 双向同步** - 字幕列表与视频自动同步
4. **✅ 可视化编辑** - 九宫格位置选择
5. **✅ 动画预览** - 实时查看8种动画效果

### 适用场景

- ✅ 需要精细调整字幕样式
- ✅ 需要实时预览效果
- ✅ 需要快速对比不同样式
- ✅ 需要专业的编辑体验

### 下一步

- 🚀 开始测试专业版功能
- 🎨 尝试不同的字幕样式
- 🎬 制作你的第一个专业字幕视频

---

**访问地址：** `http://localhost:8080/subtitle-editor-pro.html`

**文档位置：** `d:\code\adminFlow\字幕编辑器专业版-使用指南.md`

**状态：** ✅ 专业版实现完成

---

**享受所见即所得的编辑体验！** 🎬✨
