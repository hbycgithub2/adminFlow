# 字幕显示问题 - 已修复

## 🐛 发现的问题

### 问题1：乱码显示

**现象**：字幕显示 `\fad(300,300)你好，我是云舟，很高兴认识你小薇`

**原因**：ASS特效代码`\fad(300,300)`没有用`{}`包裹，被当作普通文本显示

**修复**：
```java
// 修复前
String animationTag = config.getAnimationType().getAssTag();

// 修复后
String animationTag = config.getAnimationType().getAssTag();
if (animationTag != null && !animationTag.isEmpty()) {
    animationTag = "{" + animationTag + "}";  // ✅ 用{}包裹
} else {
    animationTag = "";
}
```

### 问题2：字幕难看

**原因**：
1. 使用Arial字体，不支持中文显示
2. 字体太小（48px）
3. 边框太细（2px）

**修复**：
```java
// 1. 字体改为微软雅黑
private String fontName = "Microsoft YaHei";  // ✅ 支持中文

// 2. 字体加大
private Integer fontSize = 64;  // ✅ 从48改为64

// 3. 边框加粗
private Integer borderWidth = 3;  // ✅ 从2改为3
```

---

## ✅ 修复的文件

### 文件1：ASSSubtitleGenerator.java

**位置**：`com.hmall.tts.video.subtitle.ASSSubtitleGenerator`

**修改**：`generateDialogueLine`方法

```java
// 获取动画标签（必须用{}包裹）
String animationTag = config.getAnimationType().getAssTag();
if (animationTag != null && !animationTag.isEmpty()) {
    animationTag = "{" + animationTag + "}";  // ✅ 关键修复
} else {
    animationTag = "";
}
```

### 文件2：SubtitleConfig.java

**位置**：`com.hmall.tts.video.dto.SubtitleConfig`

**修改**：默认配置

```java
// 字体名称
@Builder.Default
private String fontName = "Microsoft YaHei";  // ✅ 从Arial改为微软雅黑

// 字体大小  
@Builder.Default
private Integer fontSize = 64;  // ✅ 从48改为64

// 边框粗细
@Builder.Default
private Integer borderWidth = 3;  // ✅ 从2改为3
```

---

## 🎨 优化后的字幕效果

### 新的默认配置

| 配置项 | 修改前 | 修改后 | 效果 |
|--------|--------|--------|------|
| 字体 | Arial | **Microsoft YaHei** | ✅ 中文显示正常 |
| 字号 | 48px | **64px** | ✅ 更清晰 |
| 边框粗细 | 2px | **3px** | ✅ 更醒目 |
| 特效代码 | 裸露显示 | **{}包裹** | ✅ 正确渲染 |

### 预期显示效果

**修复前**：
```
\fad(300,300)你好，我是云舟，很高兴认识你小薇
（特效代码被当作文本显示，字体难看）
```

**修复后**：
```
你好，我是云舟，很高兴认识你小薇
（特效正常工作，字幕渐入渐出，字体清晰美观）
```

---

## 🚀 重启服务测试

### 步骤1：重启服务

```bash
# Ctrl+C 停止服务
cd d:\code\adminFlow\hm-service
mvn spring-boot:run
```

### 步骤2：重新生成视频

访问：http://localhost:8080/video-generator-test.html

1. 上传Word文档
2. 配置参数（使用默认即可）
3. 点击"生成视频"

### 步骤3：查看效果

下载视频后查看：
- ✅ 字幕中文显示正常
- ✅ 字幕清晰美观
- ✅ 渐入渐出动画正常
- ✅ 没有乱码

---

## 🎯 ASS字幕特效标签说明

### 常用特效标签（必须用{}包裹）

| 标签 | 说明 | 示例 |
|------|------|------|
| `{\fad(t1,t2)}` | 渐入渐出 | `{\fad(300,300)}` |
| `{\move(x1,y1,x2,y2)}` | 移动 | `{\move(0,920,960,920)}` |
| `{\t(t1,t2,\fscx120)}` | 缩放 | `{\t(0,500,\fscx120)}` |
| `{\c&HBBGGRR&}` | 字体颜色 | `{\c&H00FFFF&}` |
| `{\bord3}` | 边框粗细 | `{\bord3}` |

### 错误示例（会显示为文本）

```
\fad(300,300)文本内容  ← ❌ 错误：没有{}包裹
```

### 正确示例（特效正常）

```
{\fad(300,300)}文本内容  ← ✅ 正确：用{}包裹
```

---

## 📋 其他字幕优化建议

### 可选配置调整

如果字幕还需要优化，可以在测试页面调整：

```javascript
// 字体大小（建议56-72）
fontSize: 64

// 字体颜色
fontColor: "#FFFFFF"  // 白色
fontColor: "#FFFF00"  // 黄色（更醒目）

// 边框颜色
borderColor: "#000000"  // 黑色（经典）
borderColor: "#0000FF"  // 蓝色（炫酷）

// 边框粗细
borderWidth: 3  // 清晰
borderWidth: 4  // 更醒目

// 位置
position: 2  // 底部居中（推荐）
position: 5  // 正中央
position: 8  // 顶部居中
```

---

## 🔍 验证清单

重启服务后验证：

- [ ] 字幕没有乱码（特效代码不可见）
- [ ] 字幕中文显示正常（微软雅黑字体）
- [ ] 字幕大小合适（64px）
- [ ] 字幕清晰可见（3px边框）
- [ ] 渐入渐出动画正常
- [ ] 字幕与语音同步

---

**修复完成！重启服务后，字幕显示效果将大幅提升！** 🎉

