# 6音色系统未生效问题分析

## 📋 问题描述

**现象：** Word文档中的蓝色、红色、绿色、紫色文字都使用同一种音色，没有按照前端选择的不同音色来播放。

**用户诉求：** 每种颜色的文字应该使用其对应下拉框中选择的音色。

---

## 🔍 根本原因

### ✅ 前端实现（已完成）
前端正确发送了6个音色参数：
- `boldVoice`：黑色加粗文字音色
- `normalVoice`：黑色非加粗文字音色
- `blueVoice`：蓝色文字音色（0070C0）
- `redVoice`：红色文字音色（C00000）
- `greenVoice`：绿色文字音色（00B050）
- `purpleVoice`：紫色文字音色（7030A0）

### ✅ Controller层（已完成）
Controller正确接收并传递了所有参数到Service层。

### ✅ VoiceConfig类（已完成）
`VoiceConfig.getVoiceId(colorRGB, isBold)` 方法正确实现了颜色匹配逻辑。

### ❌ **核心问题：WordDocumentParser未读取文字颜色**

**问题代码位置：** `WordDocumentParser.java` 第62-95行

```java
// 遍历段落中的Run（格式化文本片段）
for (XWPFRun run : paragraph.getRuns()) {
    String text = run.getText(0);
    
    // 跳过空文本
    if (text == null || text.trim().isEmpty()) {
        continue;
    }
    
    // 判断是否加粗
    Boolean isBold = run.isBold();
    
    // ❌ 问题：只根据isBold选择音色，完全没有读取颜色！
    String speaker;
    if (isBold != null && isBold) {
        speaker = voiceConfig.getBoldVoice();
    } else {
        speaker = voiceConfig.getNormalVoice();
    }
    
    // ❌ 问题：TextSegment没有存储颜色信息
    TextSegment segment = TextSegment.builder()
            .text(text)
            .speaker(speaker)  // ← 这里已经固定死了，不会再改变
            .isBold(isBold)
            .order(order++)
            .paragraphId(paragraphId)
            .build();
    
    segments.add(segment);
}
```

**核心问题分析：**

1. **未提取颜色**：代码中只调用了 `run.isBold()`，完全没有调用 `run.getColor()` 来提取文字颜色
2. **音色选择错误**：直接使用 `boldVoice` 或 `normalVoice`，没有调用 `voiceConfig.getVoiceId(colorRGB, isBold)`
3. **信息丢失**：`TextSegment` 对象创建时就已经确定了 `speaker`，后续流程无法更改

---

## 🎯 解决方案

### 修改点1：提取文字颜色

在 `WordDocumentParser.java` 第71行后添加：

```java
// 判断是否加粗
Boolean isBold = run.isBold();

// ✅ 新增：提取文字颜色（RGB格式，例如："0070C0"）
String colorRGB = run.getColor();
```

### 修改点2：使用VoiceConfig.getVoiceId选择音色

将第73-79行的音色选择逻辑替换为：

```java
// ✅ 修改：根据颜色和加粗状态选择音色
String speaker = voiceConfig.getVoiceId(
    colorRGB, 
    isBold != null && isBold
);
```

### 修改点3：添加调试日志

在第99行的日志后添加详细日志：

```java
log.debug("解析文本片段: text={}, isBold={}, colorRGB={}, speaker={}, paragraphId={}", 
        text, isBold, colorRGB, speaker, paragraphId);
```

### 修改点4：（可选）在TextSegment中存储颜色

如果需要在后续流程中使用颜色信息，可以在 `TextSegment` 类中添加 `colorRGB` 字段。
但根据当前设计，音色选择已经在解析阶段完成，所以这一步**不是必须的**。

---

## 📝 完整修改代码

### 修改文件：`WordDocumentParser.java`

**修改前（第71-95行）：**

```java
// 判断是否加粗
Boolean isBold = run.isBold();

// 根据是否加粗选择音色
String speaker;
if (isBold != null && isBold) {
    speaker = voiceConfig.getBoldVoice();
} else {
    speaker = voiceConfig.getNormalVoice();
}

// 创建文本片段
TextSegment segment = TextSegment.builder()
        .text(text)
        .speaker(speaker)
        .isBold(isBold)
        .order(order++)
        .paragraphId(paragraphId)
        .build();

segments.add(segment);

log.debug("解析文本片段: text={}, isBold={}, speaker={}, paragraphId={}", 
        text, isBold, speaker, paragraphId);
```

**修改后（第71-98行）：**

```java
// 判断是否加粗
Boolean isBold = run.isBold();

// ✅ 新增：提取文字颜色（RGB格式，例如："0070C0"）
String colorRGB = run.getColor();

// ✅ 修改：根据颜色和加粗状态选择音色
String speaker = voiceConfig.getVoiceId(
    colorRGB, 
    isBold != null && isBold
);

// 创建文本片段
TextSegment segment = TextSegment.builder()
        .text(text)
        .speaker(speaker)  // ← 现在根据颜色选择的音色
        .isBold(isBold)
        .order(order++)
        .paragraphId(paragraphId)
        .build();

segments.add(segment);

// ✅ 新增：详细调试日志
log.debug("解析文本片段: text='{}', isBold={}, colorRGB={}, speaker={}, paragraphId={}", 
        text, isBold, colorRGB, speaker, paragraphId);
```

---

## 🧪 测试步骤

### 步骤1：准备测试Word文档

创建一个Word文档，包含以下内容：

```
黑色加粗文字（应该用音色1）
黑色普通文字（应该用音色2）
蓝色文字（0070C0，应该用音色3）
红色文字（C00000，应该用音色4）
绿色文字（00B050，应该用音色5）
紫色文字（7030A0，应该用音色6）
```

### 步骤2：前端选择6种不同音色

在 `document-tts-test.html` 中：
- 加粗音色：选择"云舟"
- 非加粗音色：选择"薇薇"
- 蓝色音色：选择"佳佳"
- 红色音色：选择"欢小鹿"
- 绿色音色：选择"儒雅才俊 2.0"
- 紫色音色：选择"温柔文雅 2.0"

### 步骤3：上传Word文档并生成音频

点击"生成音频"按钮。

### 步骤4：检查日志

查看后端日志，应该看到类似以下输出：

```
解析文本片段: text='黑色加粗文字', isBold=true, colorRGB=null, speaker=zh_male_m191_uranus_bigtts, paragraphId=1
解析文本片段: text='黑色普通文字', isBold=false, colorRGB=null, speaker=zh_female_vv_uranus_bigtts, paragraphId=2
解析文本片段: text='蓝色文字', isBold=false, colorRGB=0070C0, speaker=zh_female_m191_uranus_bigtts, paragraphId=3
解析文本片段: text='红色文字', isBold=false, colorRGB=C00000, speaker=zh_female_xiaolu_uranus_bigtts, paragraphId=4
解析文本片段: text='绿色文字', isBold=false, colorRGB=00B050, speaker=ICL_uranus_zh_male_ruyacaijun_tob, paragraphId=5
解析文本片段: text='紫色文字', isBold=false, colorRGB=7030A0, speaker=ICL_uranus_zh_female_wenrouwenya_tob, paragraphId=6
```

### 步骤5：播放音频验证

播放生成的音频，验证每种颜色文字使用了对应的音色。

---

## 🔧 预期效果

修改完成后：

1. ✅ 黑色加粗文字使用"加粗音色"（例如：云舟）
2. ✅ 黑色非加粗文字使用"非加粗音色"（例如：薇薇）
3. ✅ 蓝色文字使用"蓝色音色"（例如：佳佳）
4. ✅ 红色文字使用"红色音色"（例如：欢小鹿）
5. ✅ 绿色文字使用"绿色音色"（例如：儒雅才俊 2.0）
6. ✅ 紫色文字使用"紫色音色"（例如：温柔文雅 2.0）

---

## 📌 关键点总结

### 为什么之前不生效？

1. **Word解析时未读取颜色**：只读取了 `isBold`，没有读取 `colorRGB`
2. **音色选择逻辑错误**：直接使用 `boldVoice/normalVoice`，没有调用 `getVoiceId` 方法
3. **前端参数被忽略**：虽然前端发送了6个音色参数，但后端解析Word时根本没用上

### 修改的核心原理

1. **提取颜色**：`String colorRGB = run.getColor()`
2. **智能选择音色**：`voiceConfig.getVoiceId(colorRGB, isBold)`
3. **降级策略**：如果颜色不匹配或未启用多音色模式，自动降级到 `boldVoice/normalVoice`

### 改动范围

**只需要修改1个文件的3行代码：**
- 文件：`WordDocumentParser.java`
- 行数：第73、75-79、99行
- 工作量：5分钟

---

## ⚠️ 注意事项

### 1. Apache POI版本

确保Apache POI版本支持 `XWPFRun.getColor()` 方法。
如果编译报错，可能需要升级POI版本到4.1.2或更高版本。

### 2. 颜色格式

Word颜色格式为6位RGB十六进制字符串，例如：
- 蓝色：`0070C0`
- 红色：`C00000`
- 绿色：`00B050`
- 紫色：`7030A0`
- 黑色（自动）：`null` 或 `"000000"`

### 3. 降级策略

如果颜色不匹配（例如用户使用了其他颜色），`getVoiceId` 会自动降级到基础模式（加粗/非加粗）。

---

## 🚀 下一步计划

1. ✅ 修改 `WordDocumentParser.java`（3行代码）
2. ✅ 编译测试
3. ✅ 使用测试Word文档验证
4. ✅ 查看日志确认颜色提取正确
5. ✅ 播放音频确认音色切换正确

---

**创建时间：** 2026-08-21  
**作者：** Kiro  
**状态：** 待执行
