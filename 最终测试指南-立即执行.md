# 最终测试指南 - 立即执行

**创建时间：** 2026-08-14  
**状态：** 准备就绪  
**预计时间：** 3分钟

---

## 🚀 立即执行（3步骤）

### 步骤1：强制重启服务（2分钟）

**在命令行执行：**

```bash
# 停止当前服务（如果正在运行）
# 按 Ctrl+C

# 进入项目目录
cd d:\code\adminFlow\hm-service

# 强制清理并重新编译
mvn clean compile -DskipTests

# 启动服务
mvn spring-boot:run
```

**等待启动完成：**
```
看到：Started HmServiceApplication in X.XXX seconds
```

---

### 步骤2：创建全新的测试文档（30秒）

**重要：** 不要使用之前的文档，重新创建一个！

**操作：**

1. 打开Microsoft Word（或WPS）
2. 新建空白文档
3. **手动输入**以下内容（不要复制粘贴）：

```
你好

我是云舟

很高兴认识你

我还喜欢游泳，滑冰
```

4. 选中"你好"，按 `Ctrl+B` 加粗
5. 选中"很高兴认识你"，按 `Ctrl+B` 加粗  
6. 选中"我还喜欢游泳，滑冰"，按 `Ctrl+B` 加粗
7. 保存为 `test-final.docx`

**验证文档：**
- 打开文档，按 `Ctrl+End` 跳到末尾
- 确认最后一行是"我还喜欢游泳，滑冰"
- 确认没有多余的空行

---

### 步骤3：上传测试（30秒）

**使用Postman：**

1. URL: `POST http://localhost:8080/tts/video/generate`
2. Body → form-data
3. 添加参数：
   - Key: `file`, Type: File, Value: 选择 `test-final.docx`
   - Key: `boldVoice`, Type: Text, Value: `zh_male_chunhoushumoyinnan_moon_bigtts`
   - Key: `normalVoice`, Type: Text, Value: `zh_female_qingxin`
4. 点击 Send
5. 等待响应（约10-15秒）

---

## ✅ 成功标志

### 1. 查看Postman返回

**成功的返回：**
```json
{
  "success": true,
  "videoUrl": "/tts/videos/xxx.mp4",
  "duration": 8.5,
  "dialogSegments": [
    {"text": "你好", "startTime": 0.0, ...},
    {"text": "我是云舟", "startTime": 1.5, ...},
    {"text": "很高兴认识你", "startTime": 3.5, ...},
    {"text": "我还喜欢游泳，滑冰", "startTime": 6.0, ...}
  ]
}
```

**关键验证：**
- ✅ HTTP 200 OK
- ✅ `success: true`
- ✅ 有 `videoUrl`
- ✅ `dialogSegments` 有4条（对应4行文本）

---

### 2. 查看服务日志

**成功的日志应该包含：**

```
✅ 文本检查通过，准备生成音频：音色=xxx, 文本=[你好], 长度=2
✅ 文本检查通过，准备生成音频：音色=xxx, 文本=[我是云舟], 长度=4
✅ 文本检查通过，准备生成音频：音色=xxx, 文本=[很高兴认识你], 长度=7
✅ 文本检查通过，准备生成音频：音色=xxx, 文本=[我还喜欢游泳，滑冰], 长度=11
并发TTS合成完成，共生成4个音频片段
```

**如果看到警告（但不影响结果）：**
```
⚠️ 检测到只包含空白字符的片段（最后防线），原始长度: X
⚠️ 检测到只包含特殊空白字符的片段（最后防线）...
```

这说明防护机制生效了，跳过了空文本片段。

---

### 3. 播放视频验证

**打开视频URL：**
```
http://localhost:8080/tts/videos/xxx.mp4
```

**验证字幕和语音对齐：**
- 0-1.5秒：说"你好"，字幕显示"你好" ✅
- 1.5-3.5秒：说"我是云舟"，字幕显示"我是云舟" ✅
- 3.5-6.0秒：说"很高兴认识你"，字幕显示"很高兴认识你" ✅
- 6.0-9.0秒：说"我还喜欢游泳，滑冰"，字幕显示"我还喜欢游泳，滑冰" ✅

---

## ❌ 如果还是失败

### 如果还报 "No readable text!" 错误

**查看日志中的关键信息：**

日志应该包含：
```
⚠️ 检测到只包含特殊空白字符的片段（最后防线），原始文本长度: X, 原始文本hex: XX XX XX
```

**这行日志会告诉我们：**
- 原始文本长度是多少
- 原始文本的十六进制表示（可以看到隐藏的字符）

**请把这行日志完整复制给我**，我可以分析出到底是什么字符导致的问题。

---

### 如果没有日志输出

**说明代码没有生效，需要：**

1. **确认服务真的重启了**
   ```bash
   # 查看java进程
   tasklist | findstr java
   
   # 如果有多个进程，全部结束
   taskkill /F /IM java.exe
   ```

2. **手动删除target目录**
   ```bash
   cd d:\code\adminFlow\hm-service
   rd /s /q target
   ```

3. **重新编译并启动**
   ```bash
   mvn clean compile -DskipTests
   mvn spring-boot:run
   ```

---

## 🎯 核心改进

### 新增的4层检查

现在在TTS调用前有**4层检查**：

```java
// 检查1：null或空字符串
if (text == null || text.isEmpty()) {
    continue;
}

// 检查2：trim后为空（空格、换行符、制表符）
if (text.trim().isEmpty()) {
    continue;
}

// 检查3：只包含特殊字符（所有Unicode空白字符）
String cleaned = text.replaceAll("[\\s\\p{Zs}\\p{Cc}]+", "");
if (cleaned.isEmpty()) {
    continue;
}

// 检查4：长度检查
if (cleaned.length() < 1) {
    continue;
}
```

**这4层检查可以捕获：**
- ✅ null
- ✅ 空字符串 ""
- ✅ 只包含空格 "   "
- ✅ 只包含换行符 "\n\n"
- ✅ 只包含制表符 "\t\t"
- ✅ 只包含回车符 "\r\r"
- ✅ 只包含Unicode空白字符（如零宽空格）
- ✅ 以上字符的任意组合

**理论上100%可以防止所有空文本！** ✅

---

## 📝 测试检查清单

- [ ] 服务已重启（mvn clean compile）
- [ ] 创建了全新的测试文档（test-final.docx）
- [ ] 文档只有4行文本，没有多余空行
- [ ] 使用Postman上传文档
- [ ] 返回HTTP 200（不是500）
- [ ] 返回 `success: true`
- [ ] 返回 `videoUrl`
- [ ] `dialogSegments` 有4条
- [ ] 视频可以播放
- [ ] 字幕和语音对齐

---

## 💬 如果全部成功

**恭喜！** 🎉

两个功能都已完成：
1. ✅ 字幕语音同步 - 每句话独立显示
2. ✅ 空文本问题修复 - 4层防护机制

你可以：
- 使用字幕编辑器编辑字幕
- 生成任何Word文档的视频
- 不用担心空行问题

---

## 💬 如果还是失败

**请提供以下信息：**

1. **Postman返回的完整JSON**
2. **服务日志（从启动到错误的所有日志）**
3. **Word文档的截图**（显示所有内容）
4. **如果日志中有hex信息，完整复制那一行**

我会根据这些信息进一步分析问题。

---

**现在立即执行上面的3个步骤！** 🚀

预计3分钟完成测试。

