# 🚀 快速验证清单（5分钟版）

## 1️⃣ 启动服务（1分钟）
```bash
cd D:\code\adminFlow\hm-service
mvn spring-boot:run
```
✅ 等待看到：`Started Application in XX seconds`

---

## 2️⃣ 打开页面，一键生成视频（1分钟）

### 重要说明：
> **整个流程只需点击一次"生成视频"！**  
> 后端会自动完成：解析Word → 生成MP3 → Whisper识别 → 生成ASS字幕 → 合成视频

### 在页面上操作：
1. **点击"选择文件"**，选择你的Word文档：`语音对话.docx`

2. **音色配置**（根据页面下拉框）：
   - 加粗文本音色：选一个女声
   - 普通文本音色：选一个男声

3. **视频配置**（使用默认值）：
   - 分辨率：1920x1080
   - 帧率：30fps
   - 字幕大小：60
   - 字幕颜色：白色

4. **点击"生成视频"**（只需点一次，等待2-3分钟）

---

## 3️⃣ 观察后端日志（2分钟）

### 后端会自动执行5个步骤，观察日志：

#### 步骤1：解析Word文档
```
[taskId] 开始生成视频：文件名=语音对话.docx
[taskId] 步骤1：调用TTS生成音频和字幕（一次性获取，确保100%同步）
```

#### 步骤2：生成MP3 + Whisper识别（核心步骤⭐）
```
开始生成文档TTS音频，文件名: 语音对话.docx
步骤1: 解析Word文档...
步骤2: 合并文本片段...
步骤3: 并发生成语音（XX个API调用）...
步骤4: 计算智能停顿...
步骤5: 构建对话片段时间信息...（这里调用Whisper）

✅ 看到以下日志 = Whisper正常工作：
  [Whisper] 服务可用
  [Whisper] 开始识别逐字时间戳，文本长度：XX
  [Whisper日志] 加载base模型...
  [Whisper日志] 识别音频：xxx.mp3
  [Whisper日志] 识别完成，字数：XX
  [Whisper] 识别成功 ✅ 字数：XX，准确率：88-92%（免费）
  
⚠️ 看到以下日志 = Whisper失败，自动降级：
  [Whisper] 服务不可用，降级到智能分配算法
  或
  [Whisper] 识别失败，降级到智能分配算法
  
步骤6: 合并音频片段...
文档TTS音频生成完成
```

#### 步骤3：复制音频
```
[taskId] TTS生成成功，音频URL：/tts/documents/xxx.mp3
[taskId] 步骤2：复制音频文件到temp目录
```

#### 步骤4：生成ASS字幕
```
[taskId] 步骤3：使用TTS返回的DialogSegments（100%同步）
[taskId] 对话片段数量：XX（来自实际TTS音频）
[taskId] 步骤4：生成ASS字幕文件
[taskId] ASS字幕文件已保存
```

#### 步骤5：合成视频
```
[taskId] 步骤5：调用FFmpeg生成视频
[taskId] 视频生成成功
[taskId] 视频生成完成，总时长：XX秒，字幕片段数：XX
```

---

## 4️⃣ 下载视频验证（1分钟）

1. **等待视频生成完成**（页面显示"生成成功"）

2. **点击"下载视频"**，保存到本地

3. **用播放器打开**（推荐PotPlayer或VLC）

4. **观察字幕同步**：
   - ✅ 字幕与说话同步
   - ✅ 每个字的显示时间合理
   - ✅ 字幕在音频结束后才消失
   - ❌ 字幕提前消失 = 有问题

---

## 🎯 快速判断结果

### ✅ 验证通过（满足以下条件）
- [x] 视频生成成功
- [x] 后端日志显示"Whisper识别成功"
- [x] 字幕与音频同步（误差<200ms）

### ⚠️ 部分通过（满足以下条件）
- [x] 视频生成成功
- [x] 后端日志显示"降级到智能分配算法"
- [x] 字幕与音频同步（误差<500ms）

### ❌ 验证失败（出现以下情况）
- [ ] 视频生成失败
- [ ] 后端报错
- [ ] 字幕与音频严重不同步（误差>1秒）

---

## 📞 遇到问题？

### 问题1：Python不可用
```bash
# 检查Python
py --version

# 如果不存在，安装Python 3.8+
# https://www.python.org/downloads/
```

### 问题2：Whisper模块未安装
```bash
# 安装openai-whisper
py -m pip install -U openai-whisper
```

### 问题3：视频生成失败
```bash
# 查看完整错误日志
tail -f D:\code\adminFlow\hm-service\logs\application.log
```

---

**下一步**：如果验证通过，查看《Whisper功能验证操作手册.md》进行深入测试！
