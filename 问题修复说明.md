# 问题修复说明 - 音频文件无法访问

> **问题时间：** 2026-08-14  
> **问题现象：** 点击生成对话语音后，无法播放音频

---

## 🐛 问题分析

### 错误信息
```
GET http://localhost:8080/tts/documents/xxx.mp3 500 (Internal Server Error)
- Blob 大小: 155 bytes
- Blob 类型: application/json  ← ❌ 应该是 audio/mpeg
```

### 根本原因
**静态资源访问配置缺失**

1. ✅ TTS生成成功（音频文件已保存到磁盘）
2. ✅ 返回了正确的audioUrl：`/tts/documents/xxx.mp3`
3. ❌ **但是访问 `/tts/documents/` 路径时返回500错误**
4. ❌ Spring Boot没有配置该路径的资源访问

**问题路径：**
```
浏览器请求: GET /tts/documents/xxx.mp3
    ↓
Spring Boot找不到处理该路径的Controller
    ↓
返回500错误（或404）
    ↓
浏览器收到错误响应（155 bytes JSON）
    ↓
audio标签无法播放（NotSupportedError）
```

---

## ✅ 解决方案

### 方案：创建专门的资源访问Controller

**新增文件：** `TtsResourceController.java`

**功能：**
- 处理 `/tts/documents/{fileName}` 请求
- 从磁盘读取音频文件
- 返回正确的Content-Type（audio/mpeg, audio/wav, audio/ogg）
- 设置正确的HTTP响应头

**代码位置：**
```
d:\code\adminFlow\hm-service\src\main\java\com\hmall\tts\volcengine\controller\TtsResourceController.java
```

**核心逻辑：**
```java
@GetMapping("/documents/{fileName}")
public ResponseEntity<Resource> downloadDocumentAudio(@PathVariable String fileName) {
    // 1. 读取文件路径
    Path filePath = Paths.get(outputDir, "documents", fileName);
    
    // 2. 检查文件是否存在
    if (!Files.exists(filePath)) {
        return ResponseEntity.notFound().build();
    }
    
    // 3. 创建Resource对象
    Resource resource = new FileSystemResource(filePath.toFile());
    
    // 4. 设置正确的Content-Type
    String contentType = "audio/mpeg"; // MP3
    
    // 5. 返回文件
    return ResponseEntity.ok()
            .contentType(MediaType.parseMediaType(contentType))
            .body(resource);
}
```

---

## 🔧 修复步骤

### 步骤1：确认文件已创建
✅ 文件已创建：`TtsResourceController.java`

### 步骤2：重启服务（必须！）
```bash
# 停止当前服务（Ctrl+C）

# 重新启动
cd D:\code\adminFlow\hm-service
mvn spring-boot:run
```

**为什么必须重启？**
- 新增了Controller类
- 需要Spring Boot重新扫描并注册路由

### 步骤3：验证修复
1. 刷新页面（Ctrl+F5）
2. 上传文档并生成语音
3. 查看Console输出

**预期结果：**
```
✅ GET /tts/documents/xxx.mp3 200 (OK)
✅ Blob 大小: 138939 bytes
✅ Blob 类型: audio/mpeg
✅ 音频成功播放
```

---

## 📊 修复前后对比

### 修复前 ❌
```
请求: GET /tts/documents/xxx.mp3
    ↓
Spring Boot: 找不到处理器
    ↓
返回: 500 Internal Server Error
    ↓
响应: application/json (155 bytes 错误信息)
    ↓
结果: 无法播放音频
```

### 修复后 ✅
```
请求: GET /tts/documents/xxx.mp3
    ↓
Spring Boot: TtsResourceController.downloadDocumentAudio()
    ↓
读取文件: ~/tts-output/documents/xxx.mp3
    ↓
返回: 200 OK
    ↓
响应: audio/mpeg (138939 bytes 音频数据)
    ↓
结果: 成功播放音频 🎵
```

---

## 🎯 完整测试流程

### 1. 重启服务
```bash
cd D:\code\adminFlow\hm-service
mvn spring-boot:run
```

**等待启动完成，看到：**
```
Started HmServiceApplication in X seconds
```

### 2. 刷新页面
```
http://localhost:8080/document-tts-test.html
按 Ctrl+F5 强制刷新
```

### 3. 上传测试
1. 选择 test-simple.docx
2. 点击"生成对话语音"
3. 等待20-30秒

### 4. 查看结果

**Console输出（正常）：**
```javascript
生成语音配置:
- 文件名: 语音对话
- 格式: mp3
- 采样率: 24000

TTS生成结果: {success: true, ...}
对话片段数量: 7
音频总时长: 8.68 秒

✅ GET /tts/documents/xxx.mp3 200 (OK)  ← 关键！

音频加载成功:
- Blob 大小: 138939 bytes  ← 应该是几十KB到几百KB
- Blob 类型: audio/mpeg    ← 正确的类型

文档预览渲染完成，共 7 个片段
```

**Network标签（正常）：**
```
/api/document-tts/generate
Status: 200 OK
Response: {success: true, audioUrl: "/tts/documents/xxx.mp3", ...}

/tts/documents/xxx.mp3
Status: 200 OK
Type: audio/mpeg
Size: 138 KB
```

**页面效果（正常）：**
- ✅ 显示音频播放器
- ✅ 显示文档预览区域（7行文本）
- ✅ 蓝色和绿色交替显示
- ✅ 音频自动播放
- ✅ 实时高亮同步

---

## 🔍 如果还有问题

### 问题1：还是返回500错误

**检查：**
1. 确认服务已重启
2. 查看后端日志：
   ```
   请求下载音频文件: xxx.mp3
   音频文件路径: C:\Users\xxx\tts-output\documents\xxx.mp3
   音频文件不存在: xxx  ← 如果显示这个，说明文件路径有问题
   ```

**解决：**
- 检查 `VolcengineConfig` 中的 `outputDir` 配置
- 确认文件确实保存到了该路径

---

### 问题2：返回404 Not Found

**原因：** Controller没有注册成功

**检查：**
1. 确认 `TtsResourceController.java` 文件存在
2. 确认类上有 `@RestController` 注解
3. 确认方法上有 `@GetMapping("/documents/{fileName}")` 注解
4. 重启服务

---

### 问题3：文件大小为0或很小

**原因：** 音频文件没有正确生成

**检查：**
1. 查看后端日志，是否有TTS生成错误
2. 检查 `~/tts-output/documents/` 目录下的文件
3. 确认文件大小是否正常（应该是几十KB到几百KB）

---

## ✅ 验收标准

**修复成功的标志：**
- [x] 服务重启无错误
- [x] GET `/tts/documents/xxx.mp3` 返回200
- [x] Blob类型为 `audio/mpeg`（不是`application/json`）
- [x] Blob大小为几十KB到几百KB（不是155 bytes）
- [x] 音频成功播放
- [x] 文档预览正常显示
- [x] 实时高亮功能正常

---

**修复完成时间：** 2026-08-14  
**修复方式：** 新增TtsResourceController  
**测试状态：** ⏳ 待测试

---

## 🚀 立即执行

**现在请执行：**

1. **重启服务**（必须！）
   ```bash
   # 在IDEA中停止服务
   # 或在终端按 Ctrl+C
   
   # 重新启动
   cd D:\code\adminFlow\hm-service
   mvn spring-boot:run
   ```

2. **等待启动完成**
   ```
   看到 "Started HmServiceApplication" 后继续
   ```

3. **刷新页面测试**
   ```
   http://localhost:8080/document-tts-test.html
   Ctrl+F5 强制刷新
   ```

4. **上传测试并观察Console**
   ```
   查看 GET /tts/documents/xxx.mp3 的状态码
   应该是 200 OK
   ```

---

**问题已修复，请重启服务并测试！** 🎉
