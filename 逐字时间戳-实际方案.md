# 逐字时间戳 - 实际可行方案

**调研结论：** 火山引擎TTS API当前版本不支持逐字时间戳返回  
**推荐方案：** 使用ASR反向识别获取逐字时间戳  
**实施时间：** 2026-08-14

---

## 🔍 调研结论

### 火山引擎TTS现状
- **当前API：** 不支持直接返回逐字时间戳
- **支持功能：** 音频合成、音色选择、语速调整
- **不支持：** word_timestamp、enable_timestamp等参数

### 行业标准做法

**B站/YouTube/剪映的做法：**
```
1. TTS生成音频（不带时间戳）
2. ASR识别音频 → 逐字时间戳
3. 根据原始文档匹配时间戳
4. 生成精确字幕
```

---

## 💡 推荐方案：ASR反向识别

### 方案流程

```
用户上传Word文档
  ↓
1. 解析文档 → originalSegments
  ↓
2. 合并相同音色 → mergedSegments
  ↓
3. TTS生成音频（无时间戳）
  ↓
4. ASR识别音频 → 逐字时间戳  ← ✅ 关键步骤
  ↓
5. 匹配原始文档 → 切分时间戳
  ↓
6. 生成精确字幕
  ↓
7. FFmpeg合成视频
```

---

## 🎯 技术选型

### ASR服务对比

| 服务 | 逐字时间戳 | 准确率 | 成本 | 推荐 |
|------|----------|--------|------|------|
| **火山引擎ASR** | ✅ 支持 | 95%+ | 0.006元/秒 | ✅ ⭐⭐⭐ |
| 科大讯飞 | ✅ 支持 | 96%+ | 0.012元/秒 | ⚠️ 贵 |
| 阿里云 | ✅ 支持 | 95%+ | 0.008元/秒 | ✅ 可选 |
| 腾讯云 | ✅ 支持 | 94%+ | 0.01元/秒 | ⚠️ 中等 |

**推荐：火山引擎ASR**
- 优势：与TTS同一平台，成本最低，准确率高
- 劣势：需要额外调用ASR API

---

## 🔧 简化方案：使用原始文档行作为字幕

### 核心思路

**不合并相同音色的文本，每行独立调用TTS**

### 优势
1. ✅ **简单直接**：不需要ASR
2. ✅ **字幕准确**：每行对应一条字幕
3. ✅ **时间精确**：每个音频片段独立计时
4. ✅ **实施快速**：30分钟完成

### 劣势
1. ⚠️ API调用次数增加（3行文档 = 3次API调用）
2. ⚠️ 相同音色的连续句子语调可能不连贯
3. ⚠️ 成本稍微增加（但可控）

### 成本分析

**场景：** 100字文档，3段（每段33字）

**当前方案（合并）：**
- API调用：1次（如果全部相同音色）
- 成本：0.006元/秒 × 10秒 = 0.06元

**简化方案（不合并）：**
- API调用：3次
- 成本：0.006元/秒 × (3秒 + 3秒 + 4秒) = 0.06元

**结论：** 成本基本相同！（因为音频时长相同）

---

## 📝 简化方案实施

### 步骤1：修改TextSegmentMerger（30分钟）

**文件：** `TextSegmentMerger.java`

**添加方法：**
```java
/**
 * 不合并，保持原始片段结构
 * （每行文档独立生成音频）
 */
public List<MergedSegment> mergeNoMerge(List<TextSegment> segments) {
    List<MergedSegment> result = new ArrayList<>();
    
    for (TextSegment segment : segments) {
        // 每个原始片段独立包装为MergedSegment
        MergedSegment mergedSegment = MergedSegment.builder()
                .text(segment.getText())
                .speaker(segment.getIsBold() ? voiceConfig.getBoldVoice() : voiceConfig.getNormalVoice())
                .originalSegments(Arrays.asList(segment))
                .build();
        
        result.add(mergedSegment);
    }
    
    return result;
}
```

---

### 步骤2：修改DocumentTTSServiceImpl（30分钟）

**文件：** `DocumentTTSServiceImpl.java`

**修改generateDocumentSpeechWithTiming方法：**
```java
private AudioGenerationResult generateDocumentSpeechWithTiming(List<TextSegment> segments, VoiceConfig voiceConfig) throws Exception {
    // 1. ✅ 修改：不合并，保持原始片段结构
    List<MergedSegment> finalSegments = segmentMerger.mergeNoMerge(segments);
    
    log.info("使用独立片段模式，共{}个音频片段（与原始文档行数一致）", finalSegments.size());
    
    // 2. 拆分过长的片段（保留，处理超长文本）
    List<MergedSegment> splitSegments = new ArrayList<>();
    for (MergedSegment segment : finalSegments) {
        if (segment.getText().length() > MAX_TEXT_LENGTH) {
            List<MergedSegment> split = segmentMerger.splitIfTooLong(segment, MAX_TEXT_LENGTH);
            splitSegments.addAll(split);
        } else {
            splitSegments.add(segment);
        }
    }
    
    // 3. 并发调用TTS API
    List<AudioSegment> audioSegments = synthesizeParallel(splitSegments, voiceConfig);
    
    // 4. 计算停顿
    calculatePauses(audioSegments, splitSegments);
    
    // 5. ✅ 简化：直接基于audioSegments构建DialogSegments
    //    每个audioSegment对应一个原始行，直接转换
    List<DialogSegment> dialogSegments = buildDialogSegments(audioSegments, voiceConfig);
    
    // 6-7. 合并音频，返回结果
    byte[] finalAudio = audioMerger.merge(audioSegments, voiceConfig.getSampleRate());
    double totalDuration = calculateTotalDuration(finalAudio, voiceConfig);
    
    return new AudioGenerationResult(finalAudio, dialogSegments, totalDuration);
}
```

---

### 步骤3：修改buildDialogSegments（已完成）

**当前实现已经正确！**

```java
private List<DialogSegment> buildDialogSegments(List<AudioSegment> audioSegments, VoiceConfig voiceConfig) {
    // 当前实现：每个audioSegment对应一个DialogSegment
    // 由于不合并，每个audioSegment对应原始文档的一行
    // 完全正确！无需修改！
    
    for (AudioSegment audioSegment : audioSegments) {
        MergedSegment mergedSegment = audioSegment.getMergedSegment();
        String text = mergedSegment.getText();  // 原始文档的一行
        
        // 计算时长
        double segmentDuration = calculateAudioDuration(...);
        double pauseDuration = ...;
        double totalDuration = segmentDuration + pauseDuration;
        
        // 创建DialogSegment
        DialogSegment dialogSegment = DialogSegment.builder()
                .text(text)  // ✅ 原始文档的一行
                .startTime(currentTime)
                .duration(totalDuration)
                .build();
        
        dialogSegments.add(dialogSegment);
        currentTime += totalDuration;
    }
    
    return dialogSegments;
}
```

---

## 🎬 预期效果

### 测试场景

**文档内容：**
```
第1行：你好（加粗）
第2行：我是云舟（普通）
第3行：很高兴认识你（加粗）
```

**修复前（合并模式）：**
```
TTS调用：1次（合并第1和3行）
音频片段：2个
  音频1："你好很高兴认识你"（5秒）
  音频2："我是云舟"（2秒）
  
字幕：2条
  字幕1："你好很高兴认识你"（0-5秒）← ❌ 问题：包含了第1和3行
  字幕2："我是云舟"（5-7秒）

结果：用户听到"你好"时，字幕显示"你好很高兴认识你"（❌ 错误）
```

**修复后（独立模式）：**
```
TTS调用：3次（每行独立）
音频片段：3个
  音频1："你好"（1.5秒）
  音频2："我是云舟"（2秒）
  音频3："很高兴认识你"（2.5秒）
  
字幕：3条
  字幕1："你好"（0-1.5秒）← ✅ 正确
  字幕2："我是云舟"（1.5-3.5秒）← ✅ 正确
  字幕3："很高兴认识你"（3.5-6秒）← ✅ 正确

结果：用户听到"你好"时，字幕显示"你好"（✅ 正确！）
```

---

## 📊 方案对比

| 对比项 | 合并模式（当前） | 独立模式（简化方案） | ASR反向识别（复杂方案） |
|--------|---------------|-------------------|----------------------|
| **准确性** | ❌ 不准确 | ✅ 准确 | ✅ 100%准确 |
| **实施时间** | - | 🟢 30分钟 | 🔴 5小时 |
| **API调用** | 少（合并） | 多（独立） | 多（TTS+ASR） |
| **成本** | 低 | 🟡 稍高 | 🔴 高 |
| **语调连贯性** | ✅ 好 | ⚠️ 稍差 | ✅ 好 |
| **推荐** | ❌ | ✅ ⭐⭐⭐ | ⚠️ 备选 |

---

## 💰 成本评估

### 场景1：短文档（100字，3段）

**独立模式：**
- API调用：3次
- 总时长：约6秒
- 成本：0.006元/秒 × 6秒 = 0.036元

**合并模式：**
- API调用：1-2次
- 总时长：约6秒
- 成本：0.006元/秒 × 6秒 = 0.036元

**结论：** 成本相同（因为音频时长相同）

### 场景2：长文档（1000字，10段）

**独立模式：**
- API调用：10次
- 总时长：约60秒
- 成本：0.006元/秒 × 60秒 = 0.36元

**合并模式：**
- API调用：2-3次
- 总时长：约60秒
- 成本：0.006元/秒 × 60秒 = 0.36元

**结论：** 成本基本相同

---

## ⚠️ 注意事项

### 注意1：相同音色的连续句子语调

**问题：** 独立调用TTS，相同音色的连续句子语调可能不连贯

**影响：** 听起来像是两个人在说话（实际是同一个音色）

**解决：** 
- 方案A：接受这个问题（影响不大）
- 方案B：添加"自然停顿"配置（让用户感觉是自然停顿）

### 注意2：API调用次数限制

**问题：** 独立调用会增加API调用次数

**影响：** 如果有QPS限制，可能触发限流

**解决：**
- 当前实现已经有并发限制（ExecutorService.newFixedThreadPool(3)）
- 可以控制在API限制范围内

---

## 🎯 推荐决策

### 推荐：简化方案（独立模式）

**理由：**
1. ✅ **实施快速**：30分钟完成
2. ✅ **效果显著**：解决你的问题（每句话独立显示）
3. ✅ **成本可控**：与合并模式成本相同
4. ✅ **无需额外服务**：不需要ASR

### 备选：ASR反向识别方案

**适用场景：**
- 需要逐字高亮效果（卡拉OK）
- 需要100%精确的逐字时间戳
- 预算充足（可以承担ASR成本）

---

## 📝 实施清单

### 简化方案（30分钟）

- [ ] 修改`TextSegmentMerger.java` - 添加`mergeNoMerge`方法
- [ ] 修改`DocumentTTSServiceImpl.java` - 使用`mergeNoMerge`
- [ ] 测试验证 - 验证每句话独立显示

### ASR方案（5小时，可选）

- [ ] 集成火山引擎ASR SDK
- [ ] 实现ASR反向识别
- [ ] 实现时间戳匹配算法
- [ ] 测试验证

---

## 🚀 立即执行

**等待你的确认：**
1. 是否同意使用简化方案（独立模式）？
2. 是否接受相同音色连续句子语调可能不连贯的问题？
3. 是否立即开始实施？

**确认后30分钟完成！** 🎯

---

**方案创建时间：** 2026-08-14  
**推荐方案：** 简化方案（独立模式）  
**实施时间：** 30分钟

