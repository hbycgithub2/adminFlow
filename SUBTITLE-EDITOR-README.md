# 🎬 字幕编辑器 - 使用指南

**功能：** 编辑已生成视频的字幕内容、时间和样式  
**版本：** v1.0 (阶段1 - 基础编辑)  
**完成时间：** 2026-08-14

---

## 🎯 功能特点

### ✅ 已实现功能（阶段1）

1. **字幕列表展示**
   - 表格形式展示所有字幕
   - 显示序号、文本、开始时间、持续时间
   - 支持文本截断显示（超过30字符显示省略号）

2. **字幕内容编辑**
   - 修改字幕文本（最多100字符）
   - 调整开始时间（0.1秒精度）
   - 调整持续时间（0.1秒精度）

3. **字幕样式编辑**
   - 字体名称选择（微软雅黑、黑体、宋体、Arial）
   - 字体大小调整（24-96px）
   - 字体颜色选择（颜色选择器）
   - 边框颜色选择（颜色选择器）
   - 边框粗细调整（1-5px）
   - 动画效果选择（8种）

4. **保存和重新生成**
   - 保存字幕修改到服务器
   - 重新生成视频（使用原音频+新字幕）
   - 友好的中文提示信息

---

## 📋 使用步骤

### 步骤1：准备工作

确保你已经通过视频生成器生成了视频，并记下任务ID（Task ID）。

**任务ID位置：**
- 视频生成成功后，响应中的 `taskId` 字段
- 视频文件名，例如：`703f1a7a-0696-4a1c-9d3c-19646b585b21.mp4`

### 步骤2：打开字幕编辑器

访问：`http://localhost:8080/subtitle-editor.html`

### 步骤3：加载字幕

1. 在页面左上角输入框输入任务ID
2. 点击「加载字幕」按钮
3. 等待加载完成（通常1-2秒）

**成功后显示：**
- 视频ID、总时长、字幕数量
- 字幕列表表格
- 「保存修改」和「重新生成视频」按钮变为可用

### 步骤4：编辑字幕

**编辑单条字幕：**
1. 点击字幕行右侧的「编辑」按钮
2. 在弹出的对话框中修改内容：
   - **文本内容** - 修改字幕文字
   - **开始时间** - 调整字幕出现时间（秒）
   - **持续时间** - 调整字幕显示时长（秒）
   - **字体名称** - 选择字体
   - **字体大小** - 调整字号（推荐64px）
   - **字体颜色** - 点击颜色块选择
   - **边框颜色** - 点击颜色块选择
   - **边框粗细** - 调整边框粗细（推荐3px）
   - **动画效果** - 选择字幕动画
3. 点击「保存修改」按钮
4. 修改会保存到本地，但尚未提交到服务器

### 步骤5：保存到服务器

**方式1：只保存字幕**
- 点击右上角「💾 保存修改」按钮
- 字幕修改会保存到ASS文件
- 视频不会重新生成

**方式2：保存并重新生成视频**
- 点击右上角「🎥 重新生成视频」按钮
- 字幕修改会保存并重新生成视频
- 等待30-60秒（根据视频长度）

### 步骤6：验证结果

**方式1：下载新视频**
- 视频生成成功后，可以在 `/tts/videos/{taskId}.mp4` 下载

**方式2：重新加载字幕**
- 再次点击「加载字幕」按钮
- 验证修改是否保存成功

---

## 🎨 使用示例

### 示例1：修正错别字

**场景：** 字幕文本有错别字

**操作：**
1. 加载字幕：`taskId = 703f1a7a-0696-4a1c-9d3c-19646b585b21`
2. 找到错误的字幕，点击「编辑」
3. 修改文本内容：`你好，我是云舟` → `你好，我是云舟，很高兴认识你`
4. 点击「保存修改」
5. 点击「💾 保存修改」按钮

**结果：** 字幕文本已更新，视频未重新生成

---

### 示例2：调整字幕时间

**场景：** 字幕出现时间不准确

**操作：**
1. 加载字幕
2. 找到需要调整的字幕，点击「编辑」
3. 修改开始时间：`4.5` → `5.0`（延后0.5秒）
4. 修改持续时间：`3.8` → `4.0`（延长0.2秒）
5. 点击「保存修改」
6. 点击「🎥 重新生成视频」按钮

**结果：** 字幕时间已调整，视频已重新生成

---

### 示例3：更换字幕样式

**场景：** 字幕颜色不够醒目

**操作：**
1. 加载字幕
2. 点击第一条字幕的「编辑」按钮
3. 修改样式：
   - 字体颜色：`#FFFFFF`（白色） → `#FFFF00`（黄色）
   - 字体大小：`64` → `72`
   - 边框粗细：`3` → `4`
   - 动画效果：`渐入渐出` → `从下飞入`
4. 点击「保存修改」
5. 点击「🎥 重新生成视频」按钮

**结果：** 字幕样式已更新，视频已重新生成

---

## 🔧 API文档

### 1. 加载字幕数据

```
GET /api/subtitle-editor/load?taskId={taskId}

Response:
{
  "taskId": "703f1a7a-0696-4a1c-9d3c-19646b585b21",
  "subtitles": [
    {
      "id": 1,
      "text": "你好，我是云舟，很高兴认识你",
      "startTime": 0.0,
      "duration": 4.5,
      "style": {
        "fontName": "Microsoft YaHei",
        "fontSize": 64,
        "fontColor": "#FFFFFF",
        "borderColor": "#000000",
        "borderWidth": 3,
        "position": 2,
        "animationType": "fade"
      }
    },
    ...
  ],
  "totalDuration": 28.6,
  "videoUrl": "/tts/videos/703f1a7a-0696-4a1c-9d3c-19646b585b21.mp4"
}
```

### 2. 更新字幕数据

```
POST /api/subtitle-editor/update

Request:
{
  "taskId": "703f1a7a-0696-4a1c-9d3c-19646b585b21",
  "subtitles": [...],  // 修改后的字幕列表
  "regenerateVideo": false  // 是否立即重新生成视频
}

Response:
{
  "success": true,
  "message": "字幕更新成功",
  "videoUrl": null
}
```

### 3. 重新生成视频

```
POST /api/subtitle-editor/regenerate?taskId={taskId}

Response:
{
  "success": true,
  "message": "视频重新生成成功",
  "videoUrl": "/tts/videos/703f1a7a-0696-4a1c-9d3c-19646b585b21.mp4",
  "duration": 15.5  // 生成耗时（秒）
}
```

---

## ⚙️ 配置说明

### 字幕样式选项

| 配置项 | 类型 | 范围/选项 | 推荐值 | 说明 |
|--------|------|----------|--------|------|
| 字体名称 | String | Microsoft YaHei, SimHei, SimSun, Arial | Microsoft YaHei | 微软雅黑支持中文显示 |
| 字体大小 | Integer | 24-96px | 64px | 太小看不清，太大超出屏幕 |
| 字体颜色 | String | #RRGGBB | #FFFFFF | 白色在深色背景下最清晰 |
| 边框颜色 | String | #RRGGBB | #000000 | 黑色边框增加对比度 |
| 边框粗细 | Integer | 1-5px | 3px | 边框太细看不清，太粗显臃肿 |
| 字幕位置 | Integer | 1-9 | 2 | 2=底部居中（最常用） |
| 动画效果 | String | 见下表 | fade | 渐入渐出最自然 |

### 动画效果选项

| 代码 | 名称 | 描述 | 视觉效果 |
|------|------|------|---------|
| fade | 渐入渐出 | 淡入淡出 | 平滑自然 |
| slide_up | 从下飞入 | 从屏幕下方飞入 | 动感强 |
| slide_down | 从上飞入 | 从屏幕上方飞入 | 快速出现 |
| slide_left | 从左飞入 | 从屏幕左侧飞入 | 侧向进入 |
| slide_right | 从右飞入 | 从屏幕右侧飞入 | 侧向进入 |
| zoom_in | 缩放进入 | 从小到大 | 有冲击力 |
| bounce | 弹跳效果 | 弹性动画 | 活泼可爱 |
| none | 无动画 | 直接显示 | 简洁 |

---

## 🐛 故障排查

### 问题1：加载字幕失败

**症状：** 点击「加载字幕」后提示"字幕文件不存在"

**可能原因：**
- 任务ID错误
- ASS文件已被删除
- 文件路径配置错误

**解决方案：**
1. 检查任务ID是否正确（复制粘贴避免输入错误）
2. 检查 `./tts/temp/{taskId}.ass` 文件是否存在
3. 检查 `application.yml` 中的 `tts.temp.dir` 配置

---

### 问题2：保存失败

**症状：** 点击「保存修改」后提示"更新字幕失败"

**可能原因：**
- 字幕时间重叠
- 文件权限不足
- 服务器异常

**解决方案：**
1. 检查字幕时间是否重叠（当前字幕结束时间 > 下一条字幕开始时间）
2. 检查文件权限（确保服务器有写入权限）
3. 查看服务器日志排查错误

---

### 问题3：视频重新生成失败

**症状：** 点击「重新生成视频」后提示失败

**可能原因：**
- 原音频文件不存在
- FFmpeg不可用
- 磁盘空间不足

**解决方案：**
1. 检查 `./tts/temp/{taskId}.mp3` 是否存在
2. 检查FFmpeg是否正确安装（`ffmpeg -version`）
3. 检查磁盘空间是否充足

---

### 问题4：字幕时间重叠

**症状：** 保存时提示"字幕时间重叠"

**原因：** 当前字幕的结束时间大于下一条字幕的开始时间

**解决方案：**
1. 调整当前字幕的持续时间，使其不超过下一条字幕的开始时间
2. 或者调整下一条字幕的开始时间，使其晚于当前字幕的结束时间

**示例：**
```
字幕1: 开始时间=0.0, 持续时间=4.5 → 结束时间=4.5
字幕2: 开始时间=4.0 ← ❌ 重叠！(4.0 < 4.5)

修正：
字幕1: 开始时间=0.0, 持续时间=4.0 → 结束时间=4.0
字幕2: 开始时间=4.0 ← ✅ 正确！
```

---

## 💡 使用技巧

### 技巧1：批量调整样式

**场景：** 想统一修改所有字幕的样式

**方法：**
1. 修改第一条字幕的样式
2. 记住样式参数
3. 逐条编辑其他字幕，手动应用相同样式

**提示：** 阶段2将支持批量样式应用功能

---

### 技巧2：精确时间调整

**场景：** 需要精确调整字幕时间

**方法：**
1. 播放视频，记录精确的时间点
2. 在编辑对话框输入精确到0.1秒的时间
3. 例如：`5.3` 表示 5.3秒

---

### 技巧3：颜色搭配

**推荐搭配：**
- **白色字幕 + 黑色边框** - 通用，适合大多数场景
- **黄色字幕 + 黑色边框** - 醒目，适合重要内容
- **黑色字幕 + 白色背景** - 简洁，适合演示文档

---

## 📈 性能参考

### 操作响应时间

| 操作 | 响应时间 | 说明 |
|------|---------|------|
| 加载字幕 | 1-2秒 | 解析ASS文件 |
| 保存字幕 | 0.5-1秒 | 写入ASS文件 |
| 重新生成视频 | 30-60秒 | 取决于视频时长 |

### 视频生成时间

| 视频时长 | 生成时间 | 文件大小 |
|---------|---------|---------|
| 30秒 | 15-20秒 | 2-3MB |
| 1分钟 | 30-40秒 | 4-6MB |
| 3分钟 | 60-90秒 | 12-18MB |

---

## 🚧 下一步计划（阶段2）

### 即将实现的功能

1. **批量操作**
   - 全选字幕
   - 批量应用样式
   - 批量删除字幕

2. **视频预览同步**
   - HTML5 Video播放器
   - Canvas字幕实时渲染
   - 播放时高亮当前字幕

3. **编辑历史**
   - 撤销/重做（Ctrl+Z / Ctrl+Y）
   - 自动保存到本地存储
   - 恢复上次编辑

4. **导出导入**
   - 导出字幕为JSON
   - 导入JSON恢复字幕
   - 支持备份和迁移

---

## 📞 技术支持

**文档位置：** `d:\code\adminFlow\SUBTITLE-EDITOR-README.md`

**相关文件：**
- 需求文档：`.kiro/specs/subtitle-editor/requirements.md`
- 设计文档：`.kiro/specs/subtitle-editor/design.md`
- 任务清单：`.kiro/specs/subtitle-editor/tasks.md`
- 测试页面：`http://localhost:8080/subtitle-editor.html`

**常见问题：**
- 视频生成器使用指南：`VIDEO-GENERATOR-README.md`
- FFmpeg安装指南：`FFmpeg快速安装指南.md`
- Word文档TTS测试：`WORD-DOCUMENT-TTS-测试指南.md`

---

**状态：** ✅ 阶段1完成（基础编辑功能）  
**版本：** v1.0  
**最后更新：** 2026-08-14

---

## 🎉 快速开始

1. **启动服务**：`mvn spring-boot:run`
2. **打开编辑器**：`http://localhost:8080/subtitle-editor.html`
3. **输入任务ID**：例如 `703f1a7a-0696-4a1c-9d3c-19646b585b21`
4. **点击加载字幕**
5. **开始编辑** 🚀
