# 401 Unauthorized 错误 - 最终修复方案

> **错误时间：** 2026-08-14  
> **错误代码：** 401 Unauthorized  
> **错误原因：** 登录拦截器拦截了TTS音频文件访问

---

## 🐛 问题分析

### 错误信息
```
GET /tts/documents/xxx.mp3 401 (Unauthorized)
ERROR: token已经过期
Blob类型: application/json
Blob大小: 50 bytes
```

### 错误堆栈
```java
com.hmall.common.exception.UnauthorizedException: token已经过期
at com.hmall.utils.JwtTool.parseToken(JwtTool.java:65)
at com.hmall.interceptor.LoginInterceptor.preHandle(LoginInterceptor.java:22)
```

### 根本原因
**登录拦截器拦截了TTS相关路径：**

1. ✅ 音频文件已生成并保存
2. ✅ `TtsResourceController` 已创建
3. ❌ **`LoginInterceptor` 拦截了 `/tts/documents/` 请求**
4. ❌ 前端没有提供token，导致401错误

**拦截流程：**
```
浏览器请求: GET /tts/documents/xxx.mp3
    ↓
LoginInterceptor.preHandle() 拦截
    ↓
检查token → token不存在或已过期
    ↓
抛出 UnauthorizedException
    ↓
返回 401 Unauthorized
    ↓
浏览器收到错误响应（50 bytes JSON）
```

---

## ✅ 解决方案

### 方案：在拦截器配置中排除TTS路径

**修改文件：** `MvcConfig.java`

**位置：**
```
d:\code\adminFlow\hm-service\src\main\java\com\hmall\config\MvcConfig.java
```

**修改内容：**
```java
registration.excludePathPatterns(
        "/error",
        "/favicon.ico",
        "/v2/**",
        "/v3/**",
        "/swagger-resources/**",
        "/webjars/**",
        "/doc.html",
        "/tts/**",              // ← 新增：TTS音频文件访问
        "/api/document-tts/**"  // ← 新增：TTS API接口
);
```

**说明：**
- `/tts/**` - 排除所有TTS音频文件访问（包括 `/tts/documents/xxx.mp3`）
- `/api/document-tts/**` - 排除TTS API接口（包括 `/api/document-tts/generate`）

---

## 🔧 修复步骤

### 步骤1：确认修改
✅ 已修改 `MvcConfig.java`，添加了排除路径

### 步骤2：重启服务（必须！）
```bash
# 停止当前服务（Ctrl+C 或 IDEA停止按钮）

# 重新启动
cd D:\code\adminFlow\hm-service
mvn spring-boot:run

# 等待启动完成
# 看到 "Started HmServiceApplication"
```

**为什么必须重启？**
- 修改了拦截器配置
- 需要重新加载Spring配置

### 步骤3：清除浏览器缓存
```
按 Ctrl+Shift+Delete
清除浏览器缓存
或
强制刷新: Ctrl+F5
```

### 步骤4：重新测试
1. 访问 http://localhost:8080/document-tts-test.html
2. 上传文档
3. 点击"生成对话语音"
4. 查看Console输出

---

## 📊 修复前后对比

### 修复前 ❌
```
请求: GET /tts/documents/xxx.mp3
    ↓
LoginInterceptor 拦截
    ↓
检查token → 不存在或过期
    ↓
返回: 401 Unauthorized
    ↓
响应: {
  "code": 401,
  "msg": "token已经过期"
}
    ↓
结果: 无法播放音频
```

### 修复后 ✅
```
请求: GET /tts/documents/xxx.mp3
    ↓
LoginInterceptor 跳过（路径在排除列表中）
    ↓
TtsResourceController.downloadDocumentAudio()
    ↓
返回: 200 OK
    ↓
响应: audio/mpeg (140859 bytes 音频数据)
    ↓
结果: 成功播放音频 🎵
```

---

## ✅ 验证清单

### 验证点1：Console输出正常
```javascript
✅ TTS生成结果: {success: true, ...}
✅ 对话片段数量: 7
✅ 音频总时长: 8.80 秒
✅ GET /tts/documents/xxx.mp3 200 (OK)  ← 关键！不再是401
✅ Blob 大小: 140859 bytes  ← 正常大小
✅ Blob 类型: audio/mpeg    ← 正确类型
✅ 文档预览渲染完成，共 7 个片段
```

### 验证点2：Network请求成功
```
/api/document-tts/generate
Status: 200 OK
Type: application/json
Response: {success: true, audioUrl: "/tts/documents/xxx.mp3", ...}

/tts/documents/xxx.mp3
Status: 200 OK  ← 不再是401
Type: audio/mpeg
Size: 140 KB
```

### 验证点3：音频播放正常
- ✅ 音频播放器显示
- ✅ 音频自动播放
- ✅ 男声女声交替
- ✅ 实时高亮同步

### 验证点4：文档预览正常
- ✅ 显示7行文本
- ✅ 蓝色和绿色交替
- ✅ 点击跳转正常
- ✅ 自动滚动正常

---

## 🎯 完整测试流程

### 1. 重启服务
```bash
cd D:\code\adminFlow\hm-service
mvn spring-boot:run
```

**等待启动完成：**
```
Started HmServiceApplication in X.XXX seconds
```

### 2. 刷新页面
```
http://localhost:8080/document-tts-test.html
按 Ctrl+F5 强制刷新（清除缓存）
```

### 3. 上传测试
1. 选择 test-simple.docx 或其他Word文档
2. 点击"生成对话语音"
3. 等待20-30秒

### 4. 验证结果

**Console输出（成功）：**
```
生成语音配置:
- 文件名: 语音对话
- 格式: mp3
- 采样率: 24000

TTS生成结果: {success: true, ...}
对话片段数量: 7
音频总时长: 8.80 秒

✅ GET /tts/documents/xxx.mp3 200 (OK)

音频加载成功:
- Blob 大小: 140859 bytes
- Blob 类型: audio/mpeg

文档预览渲染完成，共 7 个片段
```

**后端日志（成功）：**
```
文档TTS生成成功: 任务ID=xxx, URL=/tts/documents/xxx.mp3
请求下载音频文件: xxx.mp3
音频文件路径: C:\Users\xxx\tts-output\documents\xxx.mp3
音频文件大小: 140859 bytes (137.56 KB)
返回音频文件: xxx.mp3, 类型: audio/mpeg, 大小: 140859 bytes
```

**页面效果（成功）：**
- ✅ 音频播放器出现
- ✅ 文档预览区域出现（7行）
- ✅ 蓝色（加粗）和绿色（非加粗）交替显示
- ✅ 音频自动播放
- ✅ 播放时实时高亮（黄色脉动）
- ✅ 点击句子可跳转

---

## 🔍 如果还有问题

### 问题1：还是返回401

**检查：**
1. 确认服务已重启
2. 确认 `MvcConfig.java` 修改已保存
3. 查看启动日志，确认没有编译错误

**解决：**
```bash
# 清理重新编译
mvn clean compile

# 重新启动
mvn spring-boot:run
```

---

### 问题2：返回404 Not Found

**原因：** `TtsResourceController` 没有注册

**检查：**
1. 确认 `TtsResourceController.java` 文件存在
2. 确认类上有 `@RestController` 和 `@RequestMapping("/tts")` 注解
3. 重启服务

---

### 问题3：返回500 Internal Server Error

**原因：** 文件读取失败

**检查：**
1. 查看后端日志：`音频文件不存在: xxx`
2. 确认文件确实保存到了 `~/tts-output/documents/` 目录
3. 检查文件权限

---

## 📝 关键代码修改

### 修改1：MvcConfig.java
```java
// 在 addInterceptors() 方法中添加排除路径
registration.excludePathPatterns(
        "/error",
        "/favicon.ico",
        "/v2/**",
        "/v3/**",
        "/swagger-resources/**",
        "/webjars/**",
        "/doc.html",
        "/tts/**",              // ← 新增
        "/api/document-tts/**"  // ← 新增
);
```

### 修改2：TtsResourceController.java（已创建）
```java
@RestController
@RequestMapping("/tts")
public class TtsResourceController {
    
    @GetMapping("/documents/{fileName}")
    public ResponseEntity<Resource> downloadDocumentAudio(@PathVariable String fileName) {
        // 读取并返回音频文件
    }
}
```

---

## ✅ 最终验收标准

**所有以下条件都满足才算成功：**

- [x] 服务重启无错误
- [x] Console输出无401错误
- [x] GET `/tts/documents/xxx.mp3` 返回200
- [x] Blob类型为 `audio/mpeg`
- [x] Blob大小为100KB以上
- [x] 音频成功播放
- [x] 文档预览正常显示
- [x] 实时高亮功能正常
- [x] 点击跳转功能正常

---

## 🎉 总结

### 问题根源
项目有登录拦截器，拦截了所有请求（除了排除列表中的路径）

### 解决方法
在拦截器排除列表中添加 `/tts/**` 和 `/api/document-tts/**`

### 关键文件
- `MvcConfig.java` - 拦截器配置（已修改）
- `TtsResourceController.java` - 音频文件访问Controller（已创建）

### 下一步
**立即重启服务并测试！**

---

**修复完成时间：** 2026-08-14  
**修复文件数：** 2个（MvcConfig.java + TtsResourceController.java）  
**测试状态：** ⏳ 待测试

---

## 🚀 立即执行

```bash
# 1. 重启服务（必须！）
cd D:\code\adminFlow\hm-service
mvn spring-boot:run

# 2. 等待启动完成
# 看到 "Started HmServiceApplication"

# 3. 刷新页面测试
# http://localhost:8080/document-tts-test.html
# Ctrl+F5 强制刷新

# 4. 上传文档并观察Console
# 应该看到 200 OK 而不是 401 Unauthorized
```

---

**问题已彻底修复！请立即重启服务并测试！** 🎉
