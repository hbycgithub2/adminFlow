# 完整修复指南 - 空文本问题（三层防护）

**修复时间：** 2026-08-14  
**问题：** "No readable text!" - TTS API拒绝处理空文本  
**解决方案：** 三层防护机制  
**状态：** ✅ 已完成

---

## 🛡️ 三层防护机制

### 第1层：Word解析器（WordDocumentParser）

**位置：** `WordDocumentParser.java`

**防护内容：**
```java
// 跳过空段落
if (paragraph.getText().trim().isEmpty()) {
    continue;
}

// 跳过空Run
if (text == null || text.trim().isEmpty()) {
    continue;
}
```

**作用：** 在源头过滤掉Word文档中的空行和空格

---

### 第2层：文本合并器（TextSegmentMerger）

**位置：** `TextSegmentMerger.java`

**防护内容：**

#### 2.1 独立模式（mergeNoMerge）
```java
for (TextSegment segment : segments) {
    // 跳过空文本或只有空格的文本
    String text = segment.getText();
    if (text == null || text.trim().isEmpty()) {
        log.debug("跳过空文本片段: order={}", segment.getOrder());
        continue;
    }
    // ... 添加到结果
}
```

#### 2.2 合并模式（merge）
```java
// 先过滤掉空文本片段
List<TextSegment> validSegments = new ArrayList<>();
for (TextSegment segment : segments) {
    if (segment.getText() != null && !segment.getText().trim().isEmpty()) {
        validSegments.add(segment);
    } else {
        log.debug("过滤空文本片段: order={}", segment.getOrder());
    }
}
```

**作用：** 在业务处理层再次过滤，确保不会有空文本进入TTS流程

---

### 第3层：TTS调用前（synthesizeParallel）**【最后防线】**

**位置：** `DocumentTTSServiceImpl.java`

**防护内容：**
```java
for (MergedSegment segment : segments) {
    // ✅ 最后一道防线：再次检查空文本
    String text = segment.getText();
    if (text == null || text.trim().isEmpty()) {
        log.warn("⚠️ 检测到空文本片段（最后防线），跳过TTS调用");
        continue;  // 跳过这个片段
    }
    
    // 创建TTS请求
    CompletableFuture<AudioSegment> future = CompletableFuture.supplyAsync(() -> {
        // ... TTS调用
    });
}
```

**作用：** 在TTS API调用前的最后一刻检查，100%确保不会传递空文本给TTS API

---

## 🔧 如何彻底重启服务

### 方法1：使用提供的批处理文件（推荐）

**步骤：**

1. **停止当前服务**
   ```
   在运行服务的终端按 Ctrl+C
   ```

2. **运行批处理文件**
   ```
   双击运行：d:\code\adminFlow\重新编译启动.bat
   ```

3. **等待启动完成**
   ```
   看到 "Started HmServiceApplication" 即可
   ```

---

### 方法2：手动命令行操作

**步骤：**

1. **停止当前服务**
   ```bash
   Ctrl+C
   ```

2. **进入项目目录**
   ```bash
   cd d:\code\adminFlow\hm-service
   ```

3. **清理旧的编译文件**
   ```bash
   mvn clean
   ```

4. **重新编译**
   ```bash
   mvn compile -DskipTests
   ```

5. **启动服务**
   ```bash
   mvn spring-boot:run
   ```

---

### 方法3：使用IDEA重启（最简单）

1. **停止运行**
   - 点击IDEA右上角的红色"停止"按钮

2. **重新构建项目**
   - 菜单：Build → Rebuild Project
   - 快捷键：Ctrl+Shift+F9

3. **重新运行**
   - 点击IDEA右上角的绿色"运行"按钮
   - 快捷键：Shift+F10

---

## 🧪 验证修复是否生效

### 验证步骤

#### 1. 查看启动日志

**启动时应该看到：**
```
使用独立模式，不合并相同音色的片段，原始片段数: X
```

**如果没有看到这行日志：**
- ❌ 代码没有生效
- 解决：重新编译（mvn clean compile）

---

#### 2. 上传测试文档

**使用Postman测试：**
- URL: `POST http://localhost:8080/tts/video/generate`
- 上传任何包含空行的Word文档

---

#### 3. 查看运行日志

**成功的日志应该包含：**

```
✅ 第1层防护日志：
跳过空文本片段: order=1
跳过空文本片段: order=3

✅ 第2层防护日志：
独立片段: 音色=zh_male_chunhoushumoyinnan_moon_bigtts, 文本=你好
独立片段: 音色=zh_female_qingxin, 文本=我是云舟
独立模式完成，输出片段数: 4（过滤后）

✅ 第3层防护日志（如果前两层漏了）：
⚠️ 检测到空文本片段（最后防线），跳过TTS调用

✅ TTS调用日志：
开始生成音频，音色: xxx, 文本长度: 2, 文本内容: [你好]
开始生成音频，音色: xxx, 文本长度: 4, 文本内容: [我是云舟]
```

**失败的日志（需要重新编译）：**
```
❌ 没有"独立模式"日志
❌ 出现错误：No readable text!
```

---

#### 4. 验证返回结果

**成功的返回：**
```json
{
  "success": true,
  "videoUrl": "/tts/videos/xxx.mp4",
  "dialogSegments": [
    {"text": "你好", ...},
    {"text": "我是云舟", ...},
    {"text": "很高兴认识你", ...},
    {"text": "我还喜欢游泳，滑冰", ...}
  ]
}
```

**关键验证：**
- ✅ HTTP状态码：200 OK（不是500）
- ✅ `success: true`
- ✅ 有 `videoUrl`
- ✅ `dialogSegments` 数量正确（对应有效文本行数）

---

## ⚠️ 常见问题排查

### 问题1：还是报"No readable text!"错误

**原因：** 代码没有生效，还在使用旧的class文件

**解决步骤：**

1. **确认是否真的重启了服务**
   ```bash
   # 查看进程（Windows）
   tasklist | findstr java
   
   # 如果有多个java进程，全部结束
   taskkill /F /IM java.exe
   ```

2. **强制清理并重新编译**
   ```bash
   cd d:\code\adminFlow\hm-service
   mvn clean
   mvn compile -DskipTests
   ```

3. **确认target目录被清理**
   ```bash
   # 删除target目录（如果mvn clean失败）
   rd /s /q target
   ```

4. **重新启动**
   ```bash
   mvn spring-boot:run
   ```

---

### 问题2：日志中没有"独立模式"字样

**原因：** 代码没有编译进去

**解决步骤：**

1. **检查文件是否保存**
   - 确认 `TextSegmentMerger.java` 已保存
   - 确认 `DocumentTTSServiceImpl.java` 已保存

2. **使用IDEA重新构建**
   - Build → Rebuild Project
   - 等待构建完成

3. **检查编译输出**
   ```bash
   # 查看是否有编译错误
   mvn compile
   ```

4. **重新启动服务**

---

### 问题3：编译失败

**可能的错误信息：**
```
[ERROR] 无法编译
[ERROR] 找不到符号
```

**解决步骤：**

1. **检查Java版本**
   ```bash
   java -version
   # 应该是Java 8或更高版本
   ```

2. **检查Maven配置**
   ```bash
   mvn -version
   ```

3. **清理Maven缓存**
   ```bash
   mvn dependency:purge-local-repository
   ```

4. **重新编译**
   ```bash
   mvn clean install -DskipTests
   ```

---

### 问题4：还是有1个请求失败

**日志特征：**
```
音频生成完成：20.8KB ✅
音频生成完成：22.1KB ✅
音频生成完成：18.3KB ✅
音频生成完成：21.5KB ✅
TTS合成失败：No readable text! ❌ ← 还有一个失败
```

**原因：** 文档中有5个片段，其中1个是空文本，但前4个片段成功了

**解决：** 
- 这说明防护已经生效了！
- 前4个有效片段成功生成
- 第5个空片段失败（预期行为）
- **修复后应该是：只有4个请求，全部成功，不会有第5个请求**

**如果还看到这个问题：**
- 说明第3层防线（synthesizeParallel）没有生效
- 需要确认代码是否真的编译进去了

---

## 📊 修复前后对比

### 修复前

```
文档：["你好", "", "我是云舟", "", "很高兴认识你"]

处理流程：
  片段1："你好" → TTS请求1 → 成功 ✅
  片段2："" → TTS请求2 → 失败 ❌ "No readable text!"
  → 整个流程中断 ❌
```

### 修复后（第1-2层生效）

```
文档：["你好", "", "我是云舟", "", "很高兴认识你"]

第1-2层过滤：
  片段1："你好" ✅
  片段2："" → 跳过 ✅
  片段3："我是云舟" ✅
  片段4："" → 跳过 ✅
  片段5："很高兴认识你" ✅

TTS请求：
  请求1："你好" → 成功 ✅
  请求2："我是云舟" → 成功 ✅
  请求3："很高兴认识你" → 成功 ✅
  → 整个流程成功 ✅
```

### 修复后（第3层兜底）

```
假设第1-2层漏了一个空文本：
  片段1："你好" ✅
  片段2："" ← 漏网之鱼
  片段3："我是云舟" ✅

第3层防护（TTS调用前）：
  片段1："你好" → 检查通过 → TTS请求 → 成功 ✅
  片段2："" → 检查不通过 → 跳过TTS请求 ✅
  片段3："我是云舟" → 检查通过 → TTS请求 → 成功 ✅
  → 整个流程成功 ✅
```

---

## ✅ 验收清单

### 必须验证项

- [ ] **服务已重启** - 使用 `mvn clean compile` 重新编译
- [ ] **日志显示"独立模式"** - 启动日志包含这个关键字
- [ ] **上传文档成功** - 不再报 "No readable text!" 错误
- [ ] **返回HTTP 200** - 不是500错误
- [ ] **生成视频成功** - 返回 `videoUrl`
- [ ] **字幕数量正确** - `dialogSegments` 对应有效文本行数

### 可选验证项

- [ ] 日志显示"跳过空文本片段"（第2层）
- [ ] 日志显示"检测到空文本片段（最后防线）"（第3层）
- [ ] 视频播放正常，字幕和语音对齐

---

## 🎯 快速测试步骤（5分钟）

### 1. 重启服务（2分钟）

```bash
# 停止服务
Ctrl+C

# 强制重新编译
cd d:\code\adminFlow\hm-service
mvn clean compile -DskipTests

# 启动服务
mvn spring-boot:run
```

### 2. 准备测试（30秒）

**创建 Word 文档：** `测试-空行.docx`

内容：
```
你好

我是云舟

很高兴认识你
```

### 3. 上传测试（1分钟）

**Postman：**
- URL: `POST http://localhost:8080/tts/video/generate`
- 上传文档
- 等待响应

### 4. 验证结果（1分钟）

**查看返回：**
```json
{
  "success": true,  ← ✅ 关键
  "videoUrl": "...",  ← ✅ 有值
  "dialogSegments": [...]  ← ✅ 3条（对应3行文本）
}
```

**查看日志：**
```
独立模式完成，输出片段数: 3（过滤后）  ← ✅ 关键
开始生成音频，音色: xxx, 文本内容: [你好]  ← ✅ 有内容
开始生成音频，音色: xxx, 文本内容: [我是云舟]  ← ✅ 有内容
开始生成音频，音色: xxx, 文本内容: [很高兴认识你]  ← ✅ 有内容
```

**如果看到以上内容 → ✅ 修复成功！**

---

## 📝 总结

### 修复内容

1. **第1层：Word解析器** - 源头过滤空段落和空Run
2. **第2层：文本合并器** - 业务层过滤空文本片段
3. **第3层：TTS调用前** - 最后防线，100%确保不传递空文本

### 修复效果

- ✅ **100%可靠** - 三层防护，任何一层生效都能防止错误
- ✅ **日志完整** - 每层都有日志，方便调试
- ✅ **性能不受影响** - 只是增加了简单的if判断

### 关键验证

**一句话验证：** 启动日志看到"独立模式"，上传文档返回HTTP 200，不再报"No readable text!"错误

---

**修复完成时间：** 2026-08-14  
**下一步：** 重启服务并测试验证

