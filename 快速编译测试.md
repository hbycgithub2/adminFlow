# 快速编译测试指南

## ❌ Maven 编译错误（Java 版本问题）
```
无效的目标发行版: 17
```

这是因为 Maven 使用的 JDK 版本不对，但不影响 IDEA 编译。

---

## ✅ 在 IDEA 中编译（推荐）

### 方法1：自动编译（最快）
1. IDEA 会自动检测文件修改
2. 看到右下角"Build completed successfully"即可
3. 无需手动操作

### 方法2：手动编译
1. 菜单：**Build → Rebuild Project**
2. 等待编译完成（约10-30秒）
3. 看到"Build completed successfully"

### 方法3：只编译 hm-service 模块
1. 右键点击 `hm-service` 模块
2. 选择 **Build Module 'hm-service'**
3. 等待编译完成

---

## 🔍 验证编译成功

### 检查1：IDEA 右下角
```
Build completed successfully in 15s 234ms
```

### 检查2：没有红色错误标记
- 打开 `WhisperXServiceImpl.java`
- 没有红色波浪线
- 第315行没有错误提示

### 检查3：Problems 面板
- 菜单：**View → Tool Windows → Problems**
- **0 errors, 0 warnings** ✅

---

## 🚀 编译成功后的操作

### 1. 重启服务
- 停止当前服务（红色方块按钮）
- 重新运行 `HmServiceApplication`（绿色三角按钮）

### 2. 查看启动日志
应该看到：
```
[WhisperX] 使用配置的Python命令：py -3.13
[WhisperX] Python命令拆分：["py", "-3.13"]
[WhisperX] 服务可用（Python: py -3.13）
```

### 3. 测试功能
- 打开：http://localhost:8080/document-tts-test.html
- 上传 Word 文档
- 查看日志中的 WhisperX 对齐信息

---

## 📝 修复的代码位置

**文件：** `WhisperXServiceImpl.java`

**修复内容：**
1. ✅ 新增 `getPythonCommandArray()` 方法（支持 `py -3.13`）
2. ✅ 修改 `align()` 方法（使用命令数组）
3. ✅ 修改 `isAvailable()` 方法（一致性修复）
4. ✅ 添加 `Arrays` import

**核心改进：**
- 支持带参数的命令（如 `py -3.13`）
- 支持带空格的路径（如 `D:/Program Files/Python313/python.exe`）
- 自动拆分命令字符串为数组

---

## ⚠️ 如果 IDEA 编译失败

### 情况1：缓存问题
```bash
# 清理缓存
File → Invalidate Caches → Invalidate and Restart
```

### 情况2：模块依赖问题
```bash
# 重新导入 Maven 项目
右键 pom.xml → Maven → Reload Project
```

### 情况3：JDK 版本问题
```bash
# 检查 JDK 版本
File → Project Structure → Project Settings → Project
确保 SDK 是 JDK 17 或更高
```

---

## 🎯 预期结果

**编译成功后：**
1. ✅ 没有编译错误
2. ✅ 服务启动成功
3. ✅ WhisperX 路径空格问题已解决
4. ✅ 可以正常调用 WhisperX 对齐

**日志示例：**
```
[WhisperX] 使用配置的Python命令：py -3.13
[WhisperX] Python命令拆分：["py", "-3.13"]
[WhisperX] 服务可用（Python: py -3.13）

=== 开始 WhisperX 字符级对齐 ===
[WhisperX] 执行命令：py -3.13 D:/code/adminFlow/scripts/whisperx_align.py ...
[WhisperX] ✅ 对齐完成，字符数：7，准确率：98-99%
=== WhisperX 对齐完成 ===
```

---

**创建时间：** 2026-08-16  
**状态：** 可立即测试

