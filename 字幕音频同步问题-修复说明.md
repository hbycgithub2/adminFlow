# 字幕音频同步问题 - 修复说明

## 🎯 一句话总结

**问题：** 字幕显示的文本和音频播放的内容不一致  
**原因：** 字幕基于原始文本，音频基于合并拆分后的文本  
**修复：** 让字幕直接基于音频片段构建，确保100%一一对应

---

## 📊 问题示例（最直观）

### 示例文档
```
第1行：你好（加粗）
第2行：我是云舟（普通）
第3行：很高兴认识你（加粗）
```

### 修复前（❌ 错误）
```
原始文档 → 字幕（基于原始文本）
  第1行 → 字幕1："你好"
  第2行 → 字幕2："我是云舟"
  第3行 → 字幕3："很高兴认识你"

原始文档 → TTS合并 → 音频
  第1行+第3行（相同音色）→ 音频1："你好很高兴认识你"
  第2行 → 音频2："我是云舟"

结果：3条字幕 vs 2段音频 → ❌ 完全对不上！
```

### 修复后（✅ 正确）
```
原始文档 → TTS合并 → 音频 → 字幕（基于音频）
  第1行+第3行 → 音频1 → 字幕1："你好很高兴认识你"
  第2行 → 音频2 → 字幕2："我是云舟"

结果：2条字幕 vs 2段音频 → ✅ 100%一一对应！
```

---

## 🔧 修复的核心改动

### 改动1：方法签名

**文件：** `DocumentTTSServiceImpl.java`

**修复前：**
```java
// 第172行
private List<DialogSegment> buildDialogSegments(
    List<TextSegment> originalSegments,  // ❌ 基于原始文本
    List<AudioSegment> audioSegments,
    VoiceConfig voiceConfig) {
    // ...
}
```

**修复后：**
```java
// 第172行
private List<DialogSegment> buildDialogSegments(
    List<AudioSegment> audioSegments,     // ✅ 只需要音频片段
    VoiceConfig voiceConfig) {
    // ...
}
```

**为什么这样改？**
- 移除`originalSegments`参数，避免使用原始文本
- 直接基于`audioSegments`构建字幕，确保100%对应

---

### 改动2：调用处

**文件：** `DocumentTTSServiceImpl.java`

**修复前：**
```java
// 第150行
List<DialogSegment> dialogSegments = buildDialogSegments(
    segments,         // ❌ 传入原始片段
    audioSegments, 
    voiceConfig
);
```

**修复后：**
```java
// 第150行
List<DialogSegment> dialogSegments = buildDialogSegments(
    audioSegments,    // ✅ 只传入音频片段
    voiceConfig
);
```

**为什么这样改？**
- 不再传入`segments`（原始片段）
- 只传入`audioSegments`（音频片段）

---

### 改动3：核心逻辑

**文件：** `DocumentTTSServiceImpl.java`

**修复前：**
```java
// buildDialogSegments方法内部
// 遍历originalSegments，基于原始文本构建字幕
for (TextSegment segment : originalSegments) {
    String text = segment.getText();  // ❌ 原始文本
    // ...
}
```

**修复后：**
```java
// buildDialogSegments方法内部
// 遍历audioSegments，基于TTS实际发音的文本构建字幕
for (AudioSegment audioSegment : audioSegments) {
    MergedSegment mergedSegment = audioSegment.getMergedSegment();
    String text = mergedSegment.getText();  // ✅ TTS实际发音的文本
    
    // 计算音频实际时长
    double segmentDuration = calculateAudioDuration(
        audioSegment.getAudioData(), 
        voiceConfig.getFormat(), 
        voiceConfig.getSampleRate()
    );
    
    // 添加停顿时间
    double pauseDuration = 0.0;
    if (audioSegment.getNeedPause() != null && audioSegment.getNeedPause()) {
        pauseDuration = (audioSegment.getPauseDuration() != null ? 
                      audioSegment.getPauseDuration() : 800) / 1000.0;
    }
    
    double totalDuration = segmentDuration + pauseDuration;
    
    // 保留isBold属性（从原始片段中提取）
    Boolean isBold = false;
    if (mergedSegment.getOriginalSegments() != null && 
        !mergedSegment.getOriginalSegments().isEmpty()) {
        isBold = mergedSegment.getOriginalSegments().get(0).getIsBold();
    }
    
    // 创建DialogSegment（字幕片段）
    DialogSegment dialogSegment = DialogSegment.builder()
            .index(i)
            .text(text)  // ✅ 使用TTS实际发音的文本
            .isBold(isBold)
            .startTime(currentTime)
            .duration(totalDuration)
            .voiceId(mergedSegment.getSpeaker())
            .charTimings(buildCharTimings(text, currentTime, segmentDuration))
            .build();
    
    dialogSegments.add(dialogSegment);
    currentTime += totalDuration;
}
```

**为什么这样改？**
1. **直接遍历audioSegments**：每个音频片段对应一个字幕片段
2. **字幕文本 = TTS实际发音文本**：从`mergedSegment.getText()`获取
3. **精确计算时长**：从音频数据计算实际时长，不是估算
4. **保留格式信息**：`isBold`属性从原始片段中提取

---

## 🎯 修复效果

### 效果1：字幕数量正确

| 文档类型 | 原始行数 | 修复前字幕数 | 修复后字幕数 | 实际音频片段数 |
|---------|---------|-------------|-------------|--------------|
| 示例1 | 3行 | 3条 ❌ | 2条 ✅ | 2段 |
| 示例2（全加粗） | 3行 | 3条 ❌ | 1条 ✅ | 1段 |
| 示例3（全普通） | 3行 | 3条 ❌ | 1条 ✅ | 1段 |
| 示例4（长文档） | 1行（1000字） | 1条 ❌ | 2条 ✅ | 2段（拆分） |

### 效果2：字幕文本正确

| 文档内容 | 修复前字幕1 | 修复后字幕1 | 实际音频1 |
|---------|-----------|-----------|----------|
| 你好（加粗）<br>我是云舟（普通）<br>很高兴认识你（加粗） | "你好" ❌ | "你好很高兴认识你" ✅ | "你好很高兴认识你" ✅ |

### 效果3：专业版编辑器体验

| 操作 | 修复前 | 修复后 |
|-----|--------|--------|
| 点击字幕1 | 播放"你好" ❌ | 播放"你好很高兴认识你" ✅ |
| 点击字幕2 | 播放"我是云舟" ✅ | 播放"我是云舟" ✅ |
| 点击字幕3 | 播放"很高兴认识你" ❌（音频里没有） | （不存在） ✅ |

---

## 📝 技术细节

### 数据流程

```
原始Word文档
  ↓
1. WordDocumentParser.parse()
  ↓
List<TextSegment> originalSegments（原始片段）
  - TextSegment1: text="你好", isBold=true
  - TextSegment2: text="我是云舟", isBold=false
  - TextSegment3: text="很高兴认识你", isBold=true
  ↓
2. TextSegmentMerger.merge()（合并相同音色）
  ↓
List<MergedSegment> mergedSegments（合并片段）
  - MergedSegment1: text="你好很高兴认识你", speaker=加粗音色
  - MergedSegment2: text="我是云舟", speaker=普通音色
  ↓
3. TextSegmentMerger.splitIfTooLong()（拆分过长片段）
  ↓
List<MergedSegment> finalSegments（最终片段）
  - MergedSegment1: text="你好很高兴认识你", speaker=加粗音色
  - MergedSegment2: text="我是云舟", speaker=普通音色
  ↓
4. VolcengineTTSService.generateSpeechBytes()（并发调用TTS API）
  ↓
List<AudioSegment> audioSegments（音频片段）← ✅ 这才是真实的音频！
  - AudioSegment1: audioData=..., mergedSegment=MergedSegment1
  - AudioSegment2: audioData=..., mergedSegment=MergedSegment2
  ↓
5. buildDialogSegments()（构建字幕片段）
  ↓
  ❌ 修复前：基于originalSegments（3个原始片段）
  ✅ 修复后：基于audioSegments（2个音频片段）
  ↓
List<DialogSegment> dialogSegments（字幕片段）
  - DialogSegment1: text="你好很高兴认识你", startTime=0.0, duration=5.2
  - DialogSegment2: text="我是云舟", startTime=5.2, duration=3.1
```

### 核心原则

```java
// ✅ 核心原则：字幕显示的文本 = TTS实际发音的文本
String text = audioSegment.getMergedSegment().getText();

// ✅ 核心原则：字幕数量 = 音频片段数量
for (AudioSegment audioSegment : audioSegments) {
    // 每个音频片段对应一个字幕片段
}

// ✅ 核心原则：字幕时长 = 音频时长 + 停顿时长
double segmentDuration = calculateAudioDuration(audioData, format, sampleRate);
double pauseDuration = audioSegment.getPauseDuration() / 1000.0;
double totalDuration = segmentDuration + pauseDuration;
```

---

## 🚀 如何验证修复成功

### 验证1：API响应
```json
// 上传文档后，检查dialogSegments字段
{
  "dialogSegments": [
    {
      "text": "你好很高兴认识你",  // ✅ 合并后的文本
      "isBold": true
    },
    {
      "text": "我是云舟",          // ✅ 独立的文本
      "isBold": false
    }
  ]
}

// ✅ 字幕数量 = 2（不是3）
// ✅ 第1条字幕是合并后的文本
```

### 验证2：专业版编辑器
```
打开 http://localhost:8082/subtitle-editor-pro.html
加载字幕数据

右侧字幕列表显示：
1. 你好很高兴认识你  ✅
2. 我是云舟          ✅

点击字幕1 → 播放"你好很高兴认识你" ✅
点击字幕2 → 播放"我是云舟" ✅
```

### 验证3：日志输出
```
开始构建对话片段，音频片段数: 2
字幕片段0: 文本=[你好很高兴认识你], ...  ✅
字幕片段1: 文本=[我是云舟], ...          ✅
构建了2个字幕片段，总时长: 8.30秒      ✅
```

---

## 📋 修复清单

### 修改的文件
- ✅ `DocumentTTSServiceImpl.java`（2处修改）
  - 第150行：调用处参数调整
  - 第172-240行：`buildDialogSegments`方法重写

### 创建的文档
- ✅ `字幕音频同步修复-完成报告.md`（详细报告）
- ✅ `字幕音频同步-快速测试指南.md`（5分钟测试）
- ✅ `字幕音频同步问题-修复说明.md`（本文档）

### 待测试项
- [ ] 简单文档测试（1-2段）
- [ ] 多音色文档测试（加粗+普通交替）
- [ ] 长文档测试（触发拆分，800字以上）
- [ ] 专业版实时预览测试
- [ ] 简单版编辑器测试

---

## 🎉 总结

**修复前的问题：**
- 字幕基于原始文本（3条）
- 音频基于合并后文本（2段）
- 结果：字幕和音频对不上

**修复后的效果：**
- 字幕直接基于音频片段（2条）
- 音频片段（2段）
- 结果：字幕和音频100%一一对应

**核心改动：**
- 移除`buildDialogSegments`的`originalSegments`参数
- 直接基于`audioSegments`构建字幕
- 字幕文本 = TTS实际发音文本

**修复状态：**
- ✅ 代码修改完成
- ✅ 编译检查通过
- ⏳ 待功能测试验证

---

**文档版本：** v1.0  
**创建时间：** 2026-08-14  
**修复人员：** Kiro

