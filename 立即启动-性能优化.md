# 🚀 立即启动 - 性能优化指南

## 📊 当前性能问题

**你的系统耗时：188秒（3分8秒）**

**瓶颈分析：**
```
TTS生成：20秒
WhisperX对齐：196秒 ⚠️ 主要瓶颈（7次Python启动）
音频合并：2秒
```

---

## ✅ 立即优化（5分钟内完成）

### 第1步：启动WhisperX HTTP服务（2分钟）

**打开CMD，执行：**
```cmd
cd D:\code\adminFlow\scripts
start_whisperx_server.bat
```

**等待看到（约30-60秒）：**
```
✅ Whisper模型加载完成
服务启动中...
监听地址：http://0.0.0.0:5000
```

**⚠️ 重要：保持这个CMD窗口开着！不要关闭！**

---

### 第2步：验证HTTP服务（30秒）

**新开一个CMD窗口，执行：**
```cmd
curl http://localhost:5000/health
```

**预期输出：**
```json
{
  "status": "healthy",
  "device": "cuda",
  "model_loaded": true
}
```

如果看到这个，说明HTTP服务启动成功！

---

### 第3步：重启Spring Boot（1分钟）

**在Spring Boot的CMD窗口按Ctrl+C停止，然后执行：**
```cmd
cd D:\code\adminFlow\hm-service
mvn spring-boot:run
```

**等待看到：**
```
[WhisperX] RestTemplate初始化完成
[WhisperX] 服务可用
```

---

### 第4步：测试性能提升（1分钟）

**执行测试请求：**
```cmd
curl -X POST http://localhost:8080/api/video/generate ^
     -H "Content-Type: application/json" ^
     -d "{\"text\":\"今天天气很好，适合户外活动。建议多喝水，注意防晒。\"}"
```

**观察日志：**
```
[WhisperX] === 开始批量对齐 ===
[WhisperX] ✅ 批量对齐完成，总耗时：14000 ms
```

**性能对比：**
- 之前：196秒（7个音频 × 28秒/个）
- 现在：14秒（批量处理）
- **性能提升：14倍！⭐**

---

## 🎯 预期性能提升

### 当前性能（问题状态）
```
总耗时：188秒
  ├─ TTS生成：20秒
  ├─ WhisperX对齐：196秒 ⚠️
  └─ 音频合并：2秒
```

### 优化后性能（HTTP服务）
```
总耗时：36秒
  ├─ TTS生成：20秒
  ├─ WhisperX批量对齐：14秒 ✅
  └─ 音频合并：2秒

性能提升：188秒 → 36秒（提升5.2倍）
```

---

## 📋 故障排查

### 问题1：HTTP服务启动失败

**症状：**
```
❌ 未找到Python 3.13！
```

**解决：**
```cmd
cd D:\code\adminFlow\scripts
setup_python311_whisperx.bat
```

---

### 问题2：端口被占用

**症状：**
```
OSError: [WinError 10048] 端口被占用
```

**解决：**
```cmd
# 查找占用5000端口的进程
netstat -ano | findstr ":5000"

# 终止进程（假设PID是12345）
taskkill /F /PID 12345

# 重新启动服务
start_whisperx_server.bat
```

---

### 问题3：Spring Boot未检测到HTTP服务

**症状：**
```
[WhisperX] HTTP服务不可用
[WhisperX] 回退到Python脚本模式
```

**检查步骤：**

1. **HTTP服务是否运行？**
   ```cmd
   curl http://localhost:5000/health
   ```

2. **配置是否正确？**
   ```yaml
   # application.yml
   whisperx:
     use:
       server: true  # ← 必须是true
     server:
       url: http://localhost:5000
   ```

3. **重启Spring Boot**
   ```cmd
   # 停止后重新启动
   cd D:\code\adminFlow\hm-service
   mvn spring-boot:run
   ```

---

## 📞 验证清单

- [ ] ✅ WhisperX HTTP服务正在运行（CMD窗口显示"服务启动中"）
- [ ] ✅ 健康检查返回正确（`curl http://localhost:5000/health`）
- [ ] ✅ Spring Boot检测到服务（日志显示"服务可用"）
- [ ] ✅ 批量对齐生效（日志显示"批量对齐完成，总耗时：~14000 ms"）
- [ ] ✅ 性能提升明显（总耗时从188秒降到36秒）

---

## 🎓 下一步优化（可选）

完成上述步骤后，如果你想进一步优化到7秒，请告知我，我会实施**混合TTS策略**：

- 简单文本（<200字，2个角色）：整句TTS → 7秒
- 复杂文本（>200字，多角色）：分段TTS → 36秒

---

**现在就开始第1步，5分钟内看到性能提升！** 🚀

**执行顺序：**
1. 启动WhisperX HTTP服务（保持窗口开着）
2. 验证健康检查
3. 重启Spring Boot
4. 测试性能提升
