# 三合一方案 - 快速修复清单

**优先级**: 🔥 高优先级（影响核心功能）  
**预计时间**: 1-2天  
**目标**: 修复关键问题，达到可测试状态

---

## 🔥 必须立即修复的问题（3个）

### 1. 实现SubtitleAlignmentService的recognizeOnly方法

**问题**: 当用户上传自定义MP3但不提供原始文本时，系统会抛出异常

**位置**: `SubtitleAlignmentServiceImpl.recognizeOnly()`

**修复方案**:
```java
private List<DialogSegment> recognizeOnly(byte[] audioData) throws Exception {
    log.info("[字幕对齐] 使用Whisper纯识别（兜底方案）");
    
    try {
        // 调用Whisper识别
        List<WordTimestamp> words = whisperService.recognize(audioData);
        
        if (words == null || words.isEmpty()) {
            throw new Exception("Whisper识别失败，未能生成字幕");
        }
        
        // 转换为DialogSegment（按句子分段）
        return convertWordsToDialogSegments(words);
        
    } catch (Exception e) {
        log.error("[字幕对齐] Whisper识别失败：{}", e.getMessage());
        throw new Exception("无法生成字幕：" + e.getMessage());
    }
}

private List<DialogSegment> convertWordsToDialogSegments(List<WordTimestamp> words) {
    List<DialogSegment> segments = new ArrayList<>();
    StringBuilder sentenceText = new StringBuilder();
    double startTime = 0.0;
    double endTime = 0.0;
    
    for (int i = 0; i < words.size(); i++) {
        WordTimestamp word = words.get(i);
        
        if (sentenceText.length() == 0) {
            startTime = word.getStartTime();
        }
        
        sentenceText.append(word.getWord());
        endTime = word.getEndTime();
        
        // 遇到句子结束标点或达到最大长度，创建新segment
        if (word.getWord().matches(".*[。？！.?!]") || i == words.size() - 1) {
            DialogSegment segment = new DialogSegment();
            segment.setText(sentenceText.toString());
            segment.setStartTime(startTime);
            segment.setDuration(endTime - startTime);
            
            segments.add(segment);
            
            sentenceText = new StringBuilder();
        }
    }
    
    return segments;
}
```

**预计时间**: 2小时

---

### 2. 添加文件大小限制

**问题**: 没有限制上传文件大小，可能导致内存溢出

**位置**: `application.yml` 和 Controller层

**修复方案**:

**步骤1**: 在`application.yml`中添加配置
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 20MB    # 单个文件最大20MB
      max-request-size: 20MB  # 请求最大20MB
```

**步骤2**: 在Controller中添加校验
```java
@PostMapping("/generate")
public ResponseEntity<AudioGenerateResponse> generateAudio(
        @RequestParam("file") MultipartFile file,
        ...) {
    
    // 检查文件大小
    if (file.getSize() > 20 * 1024 * 1024) {
        return ResponseEntity.badRequest().body(
            AudioGenerateResponse.fail("文件大小不能超过20MB")
        );
    }
    
    // 检查文件格式
    String filename = file.getOriginalFilename();
    if (filename == null || !filename.endsWith(".docx")) {
        return ResponseEntity.badRequest().body(
            AudioGenerateResponse.fail("只支持.docx格式")
        );
    }
    
    // 原有逻辑...
}
```

**预计时间**: 30分钟

---

### 3. 添加基本异常处理

**问题**: Controller的异常处理过于简单，错误信息不够友好

**位置**: 所有Controller

**修复方案**:

**步骤1**: 创建统一异常类
```java
// 文件：com/hmall/tts/exception/BusinessException.java
package com.hmall.tts.exception;

import lombok.Getter;

@Getter
public class BusinessException extends RuntimeException {
    private final String code;
    private final String message;
    
    public BusinessException(String code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }
    
    public static BusinessException audioError(String message) {
        return new BusinessException("AUDIO_ERROR", message);
    }
    
    public static BusinessException videoError(String message) {
        return new BusinessException("VIDEO_ERROR", message);
    }
    
    public static BusinessException subtitleError(String message) {
        return new BusinessException("SUBTITLE_ERROR", message);
    }
}
```

**步骤2**: 创建全局异常处理器
```java
// 文件：com/hmall/tts/exception/GlobalExceptionHandler.java
package com.hmall.tts.exception;

import lombok.extern.slf4j.Slf4j;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.multipart.MaxUploadSizeExceededException;

import java.util.HashMap;
import java.util.Map;

@Slf4j
@ControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<Map<String, Object>> handleBusinessException(BusinessException e) {
        log.error("业务异常：{} - {}", e.getCode(), e.getMessage());
        
        Map<String, Object> response = new HashMap<>();
        response.put("success", false);
        response.put("code", e.getCode());
        response.put("message", e.getMessage());
        
        return ResponseEntity.badRequest().body(response);
    }
    
    @ExceptionHandler(MaxUploadSizeExceededException.class)
    public ResponseEntity<Map<String, Object>> handleMaxUploadSizeExceeded(MaxUploadSizeExceededException e) {
        log.error("文件大小超出限制：{}", e.getMessage());
        
        Map<String, Object> response = new HashMap<>();
        response.put("success", false);
        response.put("code", "FILE_TOO_LARGE");
        response.put("message", "文件大小超过限制（最大20MB）");
        
        return ResponseEntity.status(HttpStatus.PAYLOAD_TOO_LARGE).body(response);
    }
    
    @ExceptionHandler(Exception.class)
    public ResponseEntity<Map<String, Object>> handleException(Exception e) {
        log.error("未知异常", e);
        
        Map<String, Object> response = new HashMap<>();
        response.put("success", false);
        response.put("code", "INTERNAL_ERROR");
        response.put("message", "系统内部错误：" + e.getMessage());
        
        return ResponseEntity.internalServerError().body(response);
    }
}
```

**步骤3**: 在Service中使用BusinessException
```java
@Service
public class AudioGeneratorServiceImpl implements AudioGeneratorService {
    
    @Override
    public AudioGenerateResponse generateAudioFromDocument(...) {
        try {
            // 原有逻辑...
            
            if (!result.getSuccess()) {
                throw BusinessException.audioError("音频生成失败：" + result.getMessage());
            }
            
        } catch (BusinessException e) {
            throw e; // 直接抛出业务异常
        } catch (Exception e) {
            throw BusinessException.audioError("音频生成异常：" + e.getMessage());
        }
    }
}
```

**预计时间**: 1小时

---

## ⚡ 可选的快速优化（2个）

### 4. 使用FFprobe精确检测音频时长

**问题**: 当前使用估算方法判断音频是否被编辑，可能不准确

**位置**: `SubtitleAlignmentServiceImpl.estimateAudioDuration()`

**修复方案**:
```java
private double getAccurateAudioDuration(byte[] audioData, String format) throws Exception {
    // 1. 保存临时文件
    Path tempFile = Files.createTempFile("audio_", "." + format);
    Files.write(tempFile, audioData);
    
    try {
        // 2. 调用FFprobe获取精确时长
        List<String> command = new ArrayList<>();
        command.add("ffprobe");
        command.add("-v");
        command.add("error");
        command.add("-show_entries");
        command.add("format=duration");
        command.add("-of");
        command.add("default=noprint_wrappers=1:nokey=1");
        command.add(tempFile.toString());
        
        ProcessBuilder pb = new ProcessBuilder(command);
        Process process = pb.start();
        
        BufferedReader reader = new BufferedReader(
            new InputStreamReader(process.getInputStream())
        );
        
        String durationStr = reader.readLine();
        process.waitFor();
        
        return Double.parseDouble(durationStr);
        
    } finally {
        // 3. 清理临时文件
        Files.deleteIfExists(tempFile);
    }
}

// 修改isAudioModified方法
@Override
public boolean isAudioModified(byte[] audioData, List<DialogSegment> originalSubtitles) {
    if (originalSubtitles == null || originalSubtitles.isEmpty()) {
        return true;
    }
    
    // 计算原始字幕的总时长
    double originalDuration = calculateTotalDuration(originalSubtitles);
    
    // 使用FFprobe精确获取音频时长
    double audioDuration;
    try {
        audioDuration = getAccurateAudioDuration(audioData, "mp3");
    } catch (Exception e) {
        log.warn("无法获取精确音频时长，使用估算方法：{}", e.getMessage());
        audioDuration = estimateAudioDuration(audioData);
    }
    
    double diff = Math.abs(audioDuration - originalDuration);
    
    log.debug("[字幕对齐] 时长比较：原始字幕={}秒，音频={}秒，差异={}秒",
            String.format("%.2f", originalDuration),
            String.format("%.2f", audioDuration),
            String.format("%.2f", diff));
    
    return diff > DURATION_TOLERANCE;
}
```

**预计时间**: 1小时

---

### 5. 添加简单的请求日志

**问题**: 没有记录请求日志，难以排查问题

**位置**: 所有Controller

**修复方案**:

创建简单的请求日志拦截器：
```java
// 文件：com/hmall/tts/interceptor/RequestLogInterceptor.java
package com.hmall.tts.interceptor;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;
import org.springframework.web.servlet.HandlerInterceptor;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@Slf4j
@Component
public class RequestLogInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) {
        
        String method = request.getMethod();
        String uri = request.getRequestURI();
        String clientIp = request.getRemoteAddr();
        
        log.info("请求开始 - {} {} from {}", method, uri, clientIp);
        
        request.setAttribute("startTime", System.currentTimeMillis());
        
        return true;
    }
    
    @Override
    public void afterCompletion(HttpServletRequest request, 
                               HttpServletResponse response, 
                               Object handler, 
                               Exception ex) {
        
        Long startTime = (Long) request.getAttribute("startTime");
        long duration = System.currentTimeMillis() - startTime;
        
        String method = request.getMethod();
        String uri = request.getRequestURI();
        int status = response.getStatus();
        
        log.info("请求完成 - {} {} - 状态:{} - 耗时:{}ms", 
                method, uri, status, duration);
    }
}

// 注册拦截器
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    
    @Autowired
    private RequestLogInterceptor requestLogInterceptor;
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(requestLogInterceptor)
                .addPathPatterns("/api/**");
    }
}
```

**预计时间**: 30分钟

---

## 📋 修复执行顺序

**推荐执行顺序**（按重要性和依赖关系）：

1. ✅ **修复2**: 添加文件大小限制（30分钟） - 最简单，立即生效
2. ✅ **修复3**: 添加异常处理（1小时） - 为其他修复提供基础
3. ✅ **修复1**: 实现recognizeOnly方法（2小时） - 完善核心功能
4. ⚡ **优化5**: 添加请求日志（30分钟） - 方便后续排查
5. ⚡ **优化4**: FFprobe精确检测（1小时） - 提升准确性

**总计时间**: 约5小时（1个工作日内完成）

---

## ✅ 修复后验证清单

修复完成后，执行以下验证：

### 验证1：文件大小限制
```bash
# 测试上传超大文件
curl -X POST http://localhost:8080/api/audio/generate \
  -F "file=@large_file.docx"  # 25MB文件

# 预期：返回400错误，提示"文件大小超过限制"
```

### 验证2：异常处理
```bash
# 测试各种异常场景
1. 上传空文件
2. 上传损坏的文件
3. 上传错误格式的文件

# 预期：返回友好的错误提示，包含错误码
```

### 验证3：recognizeOnly方法
```bash
# 测试上传自定义MP3（不提供原始文本）
curl -X POST http://localhost:8080/api/video/generate-from-audio \
  -F "audio=@my_recording.mp3"

# 预期：
# - 方式1：如果Whisper可用，成功生成视频
# - 方式2：如果Whisper不可用，返回友好的错误提示
```

### 验证4：请求日志
```bash
# 查看日志文件
tail -f logs/application.log

# 预期：看到请求开始、请求完成、耗时等信息
```

### 验证5：音频时长检测
```bash
# 测试编辑后的MP3
curl -X POST http://localhost:8080/api/video/generate-from-audio \
  -F "audio=@edited_audio.mp3" \
  -F "subtitles=..." 

# 预期：
# - 准确检测出音频被编辑
# - reAligned = true
# - 日志中显示精确的时长对比
```

---

## 🎯 完成标准

所有修复完成后，系统应该达到：

✅ **功能完整性**: 90%  
✅ **代码质量**: 85%  
✅ **错误处理**: 90%  
✅ **可测试性**: 80%  

**可以进入测试阶段** ✅

---

**创建时间**: 2026-08-16  
**预计完成**: 2026-08-17  
**负责人**: 开发团队
