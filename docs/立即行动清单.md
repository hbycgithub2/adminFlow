# 立即行动清单 - Manual模式性能优化 + WhisperX自动启动

> **状态：** ✅ 代码完成，准备测试  
> **时间：** 2026-08-17 00:20  
> **预计测试时间：** 15分钟

---

## ⚡ 快速开始（3步骤）

### **Step 1：重启后端服务（1分钟）**

```bash
# 停止当前服务（Ctrl+C）

# 重新启动（二选一）
# 方式1：IDEA启动（推荐）
右键 HmServiceApplication.java → Run

# 方式2：命令行启动
cd d:\code\adminFlow\hm-service
mvn spring-boot:run
```

**观察启动日志：**
```log
✅ 应该看到：[WhisperX Server] 🚀 开始启动 HTTP 服务
✅ 应该看到：[WhisperX Server] ✅ HTTP 服务启动成功！
✅ 启动时间：约23秒（+18秒模型加载）
```

---

### **Step 2：验证WhisperX服务（30秒）**

**浏览器访问：**
```
http://localhost:5000/health
```

**预期返回：**
```json
{
  "status": "healthy",
  "uptime_seconds": 25.8
}
```

**如果失败：**
- 检查5000端口是否被占用：`netstat -ano | findstr :5000`
- 检查后端日志错误信息

---

### **Step 3：测试Manual模式（3分钟）**

1. **打开前端页面：**
   ```
   http://localhost:8080/video-generator-test.html
   ```

2. **点击"手动模式"** → 切换到Manual模式

3. **点击"选择Word文档"** → 上传测试文档（任意Word文档）

4. **点击"生成音频"** → 观察进度和时间

**预期结果：**
```
✅ 耗时：2-3秒（之前30秒）← 关键指标！
✅ 日志：skipAlignment=true
✅ 日志：⚡ 跳过批量对齐
✅ 音频可以播放
```

**如果耗时>10秒：**
- 检查后端日志是否有"跳过批量对齐"
- 如果没有，说明代码修改未生效

---

## 📋 完整测试清单（15分钟）

### **✅ 测试1：WhisperX自动启动（1分钟）**
- [ ] 重启服务成功
- [ ] 看到"✅ HTTP 服务启动成功！"
- [ ] 访问 http://localhost:5000/health 返回healthy

### **✅ 测试2：Manual模式生成音频（3分钟）**
- [ ] 切换到Manual模式
- [ ] 上传Word文档
- [ ] 点击"生成音频"
- [ ] 耗时2-3秒（关键！）
- [ ] 音频可以播放

### **✅ 测试3：Manual模式上传视频（5分钟）**
- [ ] 下载刚生成的音频
- [ ] 上传音频
- [ ] 点击"生成视频"
- [ ] 耗时38-40秒
- [ ] 字幕精确对齐（98-99%）

### **✅ 测试4：Auto模式生成视频（5分钟）**
- [ ] 切换到Auto模式
- [ ] 上传Word文档
- [ ] 点击"生成视频"
- [ ] 耗时38-40秒
- [ ] 字幕精确对齐（98-99%）

---

## 🎯 核心验证点（3个关键指标）

### **1. Manual模式生成音频速度（最重要⭐⭐⭐）**
```
优化前：30秒
优化后：2-3秒
提升：15倍
```

**如何验证：**
- 打开浏览器控制台（F12）
- 观察Network标签中的请求耗时
- 或观察前端显示的"耗时：X秒"

### **2. 最终视频字幕精度（必须保证⭐⭐⭐）**
```
优化前：98-99%
优化后：98-99%
影响：无
```

**如何验证：**
- 播放生成的视频
- 暂停在任意字
- 检查该字的音频是否正在发音
- 重复检查5-10个位置

### **3. Auto模式不受影响（必须保证⭐⭐⭐）**
```
优化前：38-40秒
优化后：38-40秒
影响：无
```

**如何验证：**
- 生成Auto模式视频
- 检查耗时是否38-40秒
- 检查字幕精度是否98-99%

---

## 📊 后端日志关键词

**测试时重点看这些日志：**

### **Manual模式生成音频（应该跳过对齐）**
```log
✅ 必须看到："⚡ Manual模式优化：skipAlignment=true"
✅ 必须看到："⚡ 跳过批量对齐（Manual模式优化）"
✅ 必须看到："耗时: 2xxx ms"
❌ 不应看到："开始批量对齐"
```

### **Manual模式上传视频（不应该跳过对齐）**
```log
✅ 必须看到："[WhisperX] === 开始批量对齐 ==="
✅ 必须看到："✅ 批量对齐完成，总耗时：28xxx ms"
✅ 必须看到："耗时: 38xxx ms"
```

### **Auto模式生成视频（不应该跳过对齐）**
```log
✅ 必须看到："跳过对齐: false"
✅ 必须看到："[WhisperX] === 开始批量对齐 ==="
✅ 必须看到："耗时: 38xxx ms"
```

---

## ⚠️ 常见问题快速修复

### **问题1：WhisperX服务启动失败**
```bash
# 检查端口占用
netstat -ano | findstr :5000

# 如果被占用，杀死进程
taskkill /PID <PID> /F

# 重启服务
```

### **问题2：Manual模式仍然很慢（>10秒）**
```bash
# 检查代码是否正确
grep -n "skipAlignment = true" AudioGeneratorServiceImpl.java

# 应该看到：
boolean skipAlignment = true;
```

### **问题3：视频字幕不对齐**
```bash
# 检查WhisperX服务是否正常
curl http://localhost:5000/health

# 检查后端日志是否有"开始批量对齐"
```

---

## ✅ 成功标准

**全部测试通过的标准：**

1. ✅ WhisperX服务自动启动成功
2. ✅ Manual模式生成音频：2-3秒（提速15倍）
3. ✅ Manual模式上传视频：38-40秒，字幕98-99%准确
4. ✅ Auto模式生成视频：38-40秒，字幕98-99%准确
5. ✅ 后端日志显示正确的skipAlignment值
6. ✅ 无任何报错

**如果达到以上标准：🎉 优化成功！**

---

## 📞 需要帮助？

如果测试过程中遇到问题：

1. **查看完整测试指南：**
   ```
   docs/测试指南-Manual模式性能优化.md
   ```

2. **查看实施报告：**
   ```
   docs/实施完成报告-Manual模式性能优化.md
   ```

3. **查看技术方案：**
   ```
   docs/Manual模式性能优化方案.md
   docs/WhisperX-HTTP服务自动启动方案.md
   ```

---

## 🎬 测试完成后

**如果测试通过：**
1. ✅ 标记任务完成
2. ✅ 提交代码到版本控制
3. ✅ 更新用户文档

**如果测试失败：**
1. 📋 记录失败现象
2. 📋 检查后端日志
3. 📋 根据"常见问题快速修复"排查

---

**准备开始测试？现在就重启服务吧！** 🚀

---

**创建时间：** 2026-08-17 00:20  
**预计完成时间：** 2026-08-17 00:35  
**总耗时：** 15分钟
