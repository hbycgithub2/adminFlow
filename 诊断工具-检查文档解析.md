# 诊断工具 - 检查Word文档解析结果

**目的：** 查看Word文档被解析成了什么内容，找出空文本的来源

---

## 🔍 临时诊断代码

### 步骤1：在WordDocumentParser中添加详细日志

在 `WordDocumentParser.java` 的 `parse()` 方法开始处添加：

```java
public List<TextSegment> parse(InputStream inputStream, VoiceConfig voiceConfig) throws Exception {
    log.info("========== 开始解析Word文档 ==========");
    log.info("boldVoice={}, normalVoice={}", voiceConfig.getBoldVoice(), voiceConfig.getNormalVoice());
    
    List<TextSegment> segments = new ArrayList<>();
    
    try (XWPFDocument document = new XWPFDocument(inputStream)) {
        int order = 0;
        int paragraphIndex = 0;
        
        // 遍历所有段落
        for (XWPFParagraph paragraph : document.getParagraphs()) {
            paragraphIndex++;
            String paragraphText = paragraph.getText();
            
            // ✅ 添加这行：打印每个段落
            log.info("段落{}: 原始文本=[{}], 长度={}, isEmpty={}", 
                    paragraphIndex, paragraphText, paragraphText.length(), 
                    paragraphText.trim().isEmpty());
            
            // 跳过空段落
            if (paragraphText.trim().isEmpty()) {
                log.info("  → 跳过空段落");
                continue;
            }
            
            // ... 后续代码
```

### 步骤2：在TextSegmentMerger中添加详细日志

在 `mergeNoMerge()` 方法开始处添加：

```java
public List<MergedSegment> mergeNoMerge(List<TextSegment> segments) {
    log.info("========== 使用独立模式处理片段 ==========");
    log.info("输入片段数: {}", segments.size());
    
    // 打印所有输入片段
    for (int i = 0; i < segments.size(); i++) {
        TextSegment seg = segments.get(i);
        log.info("输入片段{}: text=[{}], length={}, speaker={}, isBold={}", 
                i, seg.getText(), seg.getText() != null ? seg.getText().length() : 0, 
                seg.getSpeaker(), seg.getIsBold());
    }
    
    List<MergedSegment> result = new ArrayList<>();
    // ... 后续代码
```

---

## 🚨 但是！我有更快的方法

不需要修改代码，直接看你的Word文档！

**请你做以下操作：**

### 操作1：打开你上传的Word文档

1. 打开那个报错的Word文档
2. 按 `Ctrl+A` 全选
3. 看看到底有多少行内容
4. 检查是否有：
   - 只包含空格的行
   - 只包含特殊字符的行（如制表符、换行符）
   - 看不见但存在的行

### 操作2：告诉我文档的准确内容

**请按照这个格式告诉我：**

```
第1行：你好（加粗）
第2行：（空行）
第3行：我是云舟（普通）
第4行：（空行）
第5行：很高兴认识你（加粗）
第6行：（空行）
第7行：我还喜欢游泳，滑冰（加粗）
第8行：？？？（这里是什么？）
```

---

## 🔍 我的猜测

从日志看，有5个TTS请求（4个成功，1个失败），说明Word文档被解析出了5个片段。

**可能的情况：**

1. **文档末尾有隐藏的空行**
   ```
   第1行：你好
   第2行：（空行）
   第3行：我是云舟
   第4行：（空行）
   第5行：很高兴认识你
   第6行：（空行）
   第7行：我还喜欢游泳，滑冰
   第8行：（隐藏的空格或换行符）← 这个！
   ```

2. **文档中有只包含空格的行**
   ```
   某一行：[空格][空格][空格]  ← 看起来是空行，实际有空格
   ```

3. **文档中有特殊字符**
   ```
   某一行：[制表符]  ← 看不见但存在
   ```

---

## 💡 快速解决方案

### 方案1：重新创建文档（最简单）

1. 新建一个Word文档
2. 只输入以下内容（**不要复制粘贴，手动输入**）：
   ```
   你好
   
   我是云舟
   
   很高兴认识你
   
   我还喜欢游泳，滑冰
   ```
3. 第1、3、4行加粗
4. 保存为 `test-new.docx`
5. 重新上传测试

### 方案2：清理当前文档

1. 打开当前文档
2. 按 `Ctrl+End` 跳到文档末尾
3. 查看最后一行是否有内容
4. 如果有隐藏内容，全部删除
5. 按 `Ctrl+H` 打开查找替换
6. 查找：`^p^p`（两个段落标记）
7. 替换为：`^p`（一个段落标记）
8. 点击"全部替换"
9. 保存文档
10. 重新上传测试

---

## 🎯 终极解决方案（如果上面都不行）

我们直接在代码中添加更强的防护 - **在Word解析器中就把所有可疑内容过滤掉**。

但是**请先告诉我你的文档内容**，我需要知道到底是什么导致了第5个空文本片段。

---

**现在请你：**

1. 打开你上传的Word文档
2. 检查文档内容
3. 告诉我**准确的每一行内容**（包括空行）
4. 或者，直接重新创建一个简单的文档测试

我需要知道问题的根源才能彻底解决！🔍

