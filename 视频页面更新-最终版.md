# 视频生成页面更新 - 最终版

## ✅ 更新完成

**更新时间：** 2026-08-21  
**更新内容：** 在原有"生成视频"按钮上方添加"生成音频"按钮

---

## 🎯 界面布局

```
┌─────────────────────────────────┐
│   [🎵 生成音频（试听）]  ← 新增  │
│                                 │
│   ┌───────────────────────┐    │
│   │ ✅ 音频生成成功！      │    │
│   │ [音频播放器]          │    │ ← 点击"生成音频"后显示
│   │ [💾 下载音频]         │    │
│   └───────────────────────┘    │
│                                 │
│   [🎬 生成视频]  ← 保留原功能   │
└─────────────────────────────────┘
```

---

## 💡 使用方式

### 方式1：直接生成视频（原有功能，不变）

```
上传Word文档 → 配置音色 → 点击"生成视频" → 完成
```

**适用场景：** 
- 已经确认音色选择正确
- 不需要预先试听

### 方式2：先试听音频，再生成视频（新增功能）

```
上传Word文档 
  ↓
配置音色
  ↓
点击"🎵 生成音频（试听）"
  ↓
试听音频，确认音色正确
  ↓
点击"🎬 生成视频"
  ↓
完成
```

**适用场景：**
- 不确定音色选择是否正确
- 想先听听效果再决定
- 需要单独下载音频

---

## 🎨 详细功能

### 1. "生成音频（试听）"按钮

**位置：** "生成视频"按钮上方  
**颜色：** 绿色（区别于紫色的视频按钮）

**点击后：**
1. 调用文档TTS API生成音频
2. 显示音频播放器
3. 显示下载按钮
4. 耗时约10-30秒

### 2. 音频播放器区域

**显示时机：** 音频生成成功后

**包含内容：**
- ✅ 成功提示
- 🎧 HTML5音频播放器（可播放、暂停、拖动进度）
- 💾 下载按钮（下载MP3文件）

### 3. "生成视频"按钮

**功能：** 完全保持不变

**点击后：**
1. 调用视频生成API
2. 生成完整视频（包含音频）
3. 显示视频预览
4. 耗时约30秒-3分钟

---

## 📊 API调用

### 生成音频
```
前端: generateAudioOnly()
  ↓
API: POST /api/document-tts/generate
  ↓
返回: { audioUrl, duration, dialogSegments }
  ↓
显示: 音频播放器 + 下载按钮
```

### 生成视频
```
前端: generateVideo()（原有函数，不变）
  ↓
API: POST /api/video-generator/generate
  ↓
返回: { videoUrl, audioUrl, dialogSegments }
  ↓
显示: 视频预览
```

---

## 🔑 技术细节

### 新增HTML（在"生成视频"按钮上方）

```html
<!-- 新增：生成音频按钮 -->
<button onclick="generateAudioOnly()" style="background: green; margin-bottom: 15px;">
    🎵 生成音频（试听）
</button>

<!-- 音频结果区域（初始隐藏） -->
<div id="autoAudioResult" style="display: none;">
    ✅ 音频生成成功！
    <audio id="autoAudioPlayer" controls></audio>
    <a id="autoDownloadAudioLink" download>
        <button>💾 下载音频</button>
    </a>
</div>

<!-- 原有：生成视频按钮 -->
<button onclick="generateVideo()">
    🎬 生成视频
</button>
```

### 新增JavaScript函数

```javascript
// 自动模式 - 只生成音频（用于试听）
async function generateAudioOnly() {
    // 1. 验证文件
    // 2. 构建FormData（音色配置）
    // 3. 调用 /api/document-tts/generate
    // 4. 显示音频播放器和下载按钮
}
```

**关键点：**
- 只调用文档TTS API，不调用视频生成API
- 显示音频播放器后，用户可以选择继续生成视频或停止

---

## ✅ 优势

### 1. 保留原有功能
- "生成视频"按钮完全不变
- 习惯直接生成视频的用户不受影响

### 2. 提供新选择
- 想要预先试听的用户可以先点"生成音频"
- 音频和视频生成是独立的，互不影响

### 3. 节省时间
- 如果音色错了，只需重新生成音频（20秒）
- 避免重新生成视频（3分钟）

### 4. 音频可单独使用
- 可以下载MP3文件
- 可以用于其他用途（播客、配音等）

---

## 🧪 测试步骤

1. 打开：`http://localhost:8080/video-generator-test.html`
2. 选择"⚡ 自动模式"
3. 上传Word文档
4. 配置音色（启用多音色模式）

**测试路径A（直接生成视频）：**
5. 直接点击"🎬 生成视频"
6. 验证：能正常生成视频

**测试路径B（先试听音频）：**
5. 点击"🎵 生成音频（试听）"
6. 验证：显示音频播放器
7. 验证：能播放音频
8. 验证：能下载MP3
9. 点击"🎬 生成视频"
10. 验证：能正常生成视频

---

## 📝 修改文件

**文件：** `video-generator-test.html`

**修改内容：**
1. 在"生成视频"按钮上方添加"生成音频"按钮
2. 添加音频结果显示区域
3. 添加 `generateAudioOnly()` 函数

**代码量：**
- HTML：约20行
- JavaScript：约60行

---

## 🎉 总结

**核心理念：** 不破坏原有功能，提供额外选择

**用户体验：**
- 老用户：不受影响，继续使用"生成视频"按钮
- 新用户：可以先试听音频，确认无误再生成视频

**技术实现：**
- 代码简洁，逻辑清晰
- 完全向后兼容
- 易于维护和扩展

---

**状态：** ✅ 更新完成，可以测试
