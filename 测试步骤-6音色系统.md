# 6音色系统测试步骤

## ✅ 修改完成

**修改文件：** `WordDocumentParser.java`  
**修改内容：**
1. 新增：提取文字颜色 `String colorRGB = run.getColor()`
2. 修改：使用 `voiceConfig.getVoiceId(colorRGB, isBold)` 智能选择音色
3. 新增：调试日志输出颜色信息

**代码状态：** ✅ 无语法错误

---

## 📋 测试步骤

### 步骤1：准备测试Word文档

创建一个Word文档（`测试6音色.docx`），包含以下内容：

```
第一行：黑色加粗文字测试（应该用"加粗音色"）

第二行：黑色普通文字测试（应该用"非加粗音色"）

第三行：蓝色文字测试（颜色RGB=0070C0，应该用"蓝色音色"）

第四行：红色文字测试（颜色RGB=C00000，应该用"红色音色"）

第五行：绿色文字测试（颜色RGB=00B050，应该用"绿色音色"）

第六行：紫色文字测试（颜色RGB=7030A0，应该用"紫色音色"）
```

**Word文档设置：**
- 第一行：黑色 + 加粗
- 第二行：黑色 + 不加粗
- 第三行：蓝色（RGB: 0070C0） + 不加粗
- 第四行：红色（RGB: C00000） + 不加粗
- 第五行：绿色（RGB: 00B050） + 不加粗
- 第六行：紫色（RGB: 7030A0） + 不加粗

**如何设置文字颜色：**
1. 选中文字
2. 点击"字体颜色"按钮
3. 选择"其他颜色"
4. 选择"自定义"标签页
5. 输入RGB值：
   - 蓝色：R=0, G=112, B=192（十六进制：0070C0）
   - 红色：R=192, G=0, B=0（十六进制：C00000）
   - 绿色：R=0, G=176, B=80（十六进制：00B050）
   - 紫色：R=112, G=48, B=160（十六进制：7030A0）

---

### 步骤2：启动后端服务

```bash
cd d:\code\adminFlow\hm-service
mvn spring-boot:run
```

或者在IDE中直接运行 `HmServiceApplication`

---

### 步骤3：打开前端测试页面

浏览器打开：`http://localhost:8080/document-tts-test.html`

---

### 步骤4：配置6种音色

**启用多音色模式：**
✅ 勾选"启用多音色模式"

**选择6种不同的音色：**
1. **加粗音色**：选择 **云舟**（zh_male_m191_uranus_bigtts）- 沉稳男声
2. **非加粗音色**：选择 **薇薇**（zh_female_vv_uranus_bigtts）- 温柔女声
3. **蓝色音色**：选择 **佳佳**（zh_female_m191_uranus_bigtts）- 温柔女声
4. **红色音色**：选择 **欢小鹿**（zh_female_xiaolu_uranus_bigtts）- 活泼女声
5. **绿色音色**：选择 **儒雅才俊 2.0**（ICL_uranus_zh_male_ruyacaijun_tob）- 儒雅男声
6. **紫色音色**：选择 **温柔文雅 2.0**（ICL_uranus_zh_female_wenrouwenya_tob）- 古风女声

---

### 步骤5：上传Word文档

点击"选择Word文档"按钮，选择 `测试6音色.docx`

---

### 步骤6：生成音频

点击"生成音频"按钮

---

### 步骤7：查看后端日志

**日志关键字：** `解析文本片段`

**预期日志输出：**

```log
2026-08-21 14:00:00.123  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='黑色加粗文字测试', isBold=true, colorRGB=null, speaker=zh_male_m191_uranus_bigtts, paragraphId=1

2026-08-21 14:00:00.124  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='黑色普通文字测试', isBold=false, colorRGB=null, speaker=zh_female_vv_uranus_bigtts, paragraphId=2

2026-08-21 14:00:00.125  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='蓝色文字测试', isBold=false, colorRGB=0070C0, speaker=zh_female_m191_uranus_bigtts, paragraphId=3

2026-08-21 14:00:00.126  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='红色文字测试', isBold=false, colorRGB=C00000, speaker=zh_female_xiaolu_uranus_bigtts, paragraphId=4

2026-08-21 14:00:00.127  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='绿色文字测试', isBold=false, colorRGB=00B050, speaker=ICL_uranus_zh_male_ruyacaijun_tob, paragraphId=5

2026-08-21 14:00:00.128  DEBUG --- [nio-8080-exec-1] c.h.t.v.docx.WordDocumentParser  : 解析文本片段: text='紫色文字测试', isBold=false, colorRGB=7030A0, speaker=ICL_uranus_zh_female_wenrouwenya_tob, paragraphId=6
```

**关键验证点：**
- ✅ 第1行：`colorRGB=null, speaker=zh_male_m191_uranus_bigtts`（云舟）
- ✅ 第2行：`colorRGB=null, speaker=zh_female_vv_uranus_bigtts`（薇薇）
- ✅ 第3行：`colorRGB=0070C0, speaker=zh_female_m191_uranus_bigtts`（佳佳）
- ✅ 第4行：`colorRGB=C00000, speaker=zh_female_xiaolu_uranus_bigtts`（欢小鹿）
- ✅ 第5行：`colorRGB=00B050, speaker=ICL_uranus_zh_male_ruyacaijun_tob`（儒雅才俊2.0）
- ✅ 第6行：`colorRGB=7030A0, speaker=ICL_uranus_zh_female_wenrouwenya_tob`（温柔文雅2.0）

---

### 步骤8：播放音频验证

播放生成的音频，验证每行文字使用了对应的音色：

1. **第一行**：应该听到**沉稳男声**（云舟）
2. **第二行**：应该听到**温柔女声**（薇薇）
3. **第三行**：应该听到**温柔女声**（佳佳）
4. **第四行**：应该听到**活泼女声**（欢小鹿）
5. **第五行**：应该听到**儒雅男声**（儒雅才俊2.0）
6. **第六行**：应该听到**古风女声**（温柔文雅2.0）

---

## ✅ 预期结果

### 成功标志

1. ✅ 后端日志显示每种颜色都提取到了正确的RGB值
2. ✅ 每种颜色都匹配到了对应的音色ID
3. ✅ 播放音频时能听到6种不同的声音
4. ✅ 每种颜色文字使用的音色与前端选择的一致

### 如果失败

**问题1：日志中 `colorRGB` 全部为 `null`**
- **原因：** Word文档颜色设置不正确
- **解决：** 检查Word文档，确保文字确实设置了颜色（不是黑色）

**问题2：日志中 `colorRGB` 有值，但 `speaker` 不对**
- **原因：** VoiceConfig中的颜色音色字段为null
- **解决：** 检查前端是否启用了多音色模式，是否选择了颜色音色

**问题3：日志中 `speaker` 正确，但音频听起来都是同一个声音**
- **原因：** TTS API调用问题
- **解决：** 检查火山引擎TTS配置，确认音色ID有效

---

## 🔧 调试技巧

### 开启DEBUG日志

在 `application.yml` 中添加：

```yaml
logging:
  level:
    com.hmall.tts.volcengine.docx: DEBUG
    com.hmall.tts.volcengine.service: DEBUG
```

### 查看完整请求参数

在浏览器控制台（F12）查看Network标签，找到 `/tts/document/generate` 请求，查看Request Payload：

```json
{
  "boldVoice": "zh_male_m191_uranus_bigtts",
  "normalVoice": "zh_female_vv_uranus_bigtts",
  "multiVoiceMode": true,
  "blueVoice": "zh_female_m191_uranus_bigtts",
  "redVoice": "zh_female_xiaolu_uranus_bigtts",
  "greenVoice": "ICL_uranus_zh_male_ruyacaijun_tob",
  "purpleVoice": "ICL_uranus_zh_female_wenrouwenya_tob",
  "format": "mp3",
  "sampleRate": 24000
}
```

确认6个音色参数都已发送。

---

## 📌 常见问题

### Q1：Word文档中的颜色怎么设置？

**A：** 
1. 选中文字
2. 点击"字体颜色"按钮旁边的下拉箭头
3. 选择"其他颜色"
4. 在"自定义"标签页中输入RGB值
5. 点击"确定"

### Q2：如何确认Word文档的颜色是正确的？

**A：** 
1. 选中文字
2. 右键 → 字体
3. 在"字体"对话框中点击"颜色"旁边的下拉箭头
4. 选择"其他颜色"
5. 查看RGB值是否匹配

### Q3：支持哪些颜色？

**A：** 当前只支持以下4种颜色：
- 蓝色：0070C0
- 红色：C00000
- 绿色：00B050
- 紫色：7030A0

如果使用其他颜色，会自动降级到基础模式（根据是否加粗选择音色）。

### Q4：黑色文字会使用颜色音色吗？

**A：** 不会。黑色文字（`colorRGB=null` 或 `000000`）始终根据是否加粗来选择音色：
- 黑色加粗 → boldVoice
- 黑色不加粗 → normalVoice

### Q5：可以同时使用加粗和颜色吗？

**A：** 可以，但**颜色优先级更高**。例如：
- 蓝色加粗文字 → 使用blueVoice（而不是boldVoice）
- 如果blueVoice未设置，则降级到boldVoice

---

## 🎓 技术原理

### 音色选择优先级

```
1. 启用多音色模式？
   ├─ 是 → 2
   └─ 否 → 使用基础模式（boldVoice/normalVoice）

2. 文字有颜色吗？
   ├─ 是 → 3
   └─ 否 → 使用基础模式（boldVoice/normalVoice）

3. 颜色匹配4种预设颜色吗？
   ├─ 是 → 使用对应颜色音色（blueVoice/redVoice/greenVoice/purpleVoice）
   └─ 否 → 使用基础模式（boldVoice/normalVoice）

4. 颜色音色有值吗？
   ├─ 是 → 使用颜色音色 ✅
   └─ 否 → 使用基础模式（boldVoice/normalVoice）
```

### 降级策略

系统设计了完善的降级策略，确保任何情况下都能正常工作：

1. **未启用多音色模式** → 降级到2音色模式
2. **颜色不匹配** → 降级到基础模式
3. **颜色音色未设置** → 降级到基础模式
4. **文字为黑色** → 使用基础模式

---

**创建时间：** 2026-08-21  
**作者：** Kiro  
**状态：** 待测试
