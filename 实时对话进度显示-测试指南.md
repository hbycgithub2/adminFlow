# 实时对话进度显示功能 - 测试指南

> **功能版本：** v3.0 - 实时进度显示  
> **创建时间：** 2026-08-14  
> **测试目标：** 验证卡拉OK式实时高亮显示功能

---

## 🎯 新功能概述

### 核心特性
1. **实时高亮显示** - 播放时当前句子高亮显示（黄色背景+脉动动画）
2. **已播放标记** - 已播放的句子变灰（半透明效果）
3. **角色颜色区分** - 加粗=蓝色（角色1），非加粗=绿色（角色2）
4. **点击跳转** - 点击任意句子可跳转到该位置播放
5. **自动滚动** - 自动滚动到当前播放位置（smooth动画）

---

## 📋 测试步骤

### 第1步：启动服务

```bash
# 进入项目目录
cd D:\code\adminFlow\hm-service

# 启动服务（如果已启动可跳过）
mvn spring-boot:run
```

**验证：** 看到 `Started HmServiceApplication` 表示启动成功

---

### 第2步：打开测试页面

**浏览器访问：** http://localhost:8080/document-tts-test.html

**验证：** 页面加载完成，看到 "📄 Word文档对话TTS" 标题

---

### 第3步：准备测试文档

使用现有的测试文档：`D:\code\adminFlow\测试文档内容模板.txt`

**或者**创建新的Word文档（.docx格式），内容示例：

```
你好，云舟很高兴认识你
你好，小薇我也很高兴认识你
你来自哪里？
我来自吉林，你呢
我来自大连
```

**重要：** 
- 第1、3、5句设置为**加粗**（角色1 - 蓝色）
- 第2、4句保持**非加粗**（角色2 - 绿色）

---

### 第4步：上传并生成语音

1. **上传文档**
   - 点击 "选择Word文档" 按钮
   - 选择测试文档（.docx格式）

2. **配置音色**（可选）
   - 加粗文本音色：云舟（沉稳男声）
   - 非加粗文本音色：薇薇（温柔女声）

3. **配置音频**（可选）
   - 音频格式：MP3
   - 采样率：24000 Hz

4. **点击生成**
   - 点击 "🎤 生成对话语音" 按钮
   - 等待20-30秒（根据文档长度）

**验证：** 看到 "对话语音生成成功" 提示

---

### 第5步：验证实时进度显示

#### 测试点1：文档内容预览 ✅

**预期效果：**
- ✅ 页面下方出现 "📄 文档内容预览（实时高亮）" 区域
- ✅ 显示所有对话句子
- ✅ 加粗句子显示为**蓝色背景**（#e3f2fd）
- ✅ 非加粗句子显示为**绿色背景**（#e8f5e9）
- ✅ 每个句子左侧有彩色边框（蓝色/绿色）

**验证方法：**
```
打开浏览器开发者工具（F12）→ Console
查看输出：
- "文档预览渲染完成，共 X 个片段"
- "对话片段数量: X"
- "音频总时长: X 秒"
```

---

#### 测试点2：自动播放+实时高亮 ✅

**操作：** 生成成功后，音频会自动播放

**预期效果：**
- ✅ 第1句变为**黄色背景**（#fff9c4），开始**脉动动画**
- ✅ 播放到第2句时，第1句变灰（半透明），第2句变黄色
- ✅ 播放到第3句时，前2句都变灰，第3句变黄色
- ✅ 依次类推，直到播放完毕
- ✅ 页面**自动滚动**到当前播放位置（smooth动画）

**验证方法：**
```
观察句子颜色变化：
- 正在播放：黄色背景 + 脉动动画 + 阴影效果
- 已播放：灰色半透明（opacity: 0.5）
- 未播放：蓝色/绿色原始颜色
```

---

#### 测试点3：点击跳转播放 ✅

**操作：** 点击任意句子

**预期效果：**
- ✅ 音频跳转到该句子的时间位置
- ✅ 该句子立即高亮（黄色背景）
- ✅ 前面的句子变灰（已播放状态）
- ✅ 后面的句子保持原色（未播放状态）
- ✅ 音频继续播放

**验证方法：**
```
1. 播放到第2句时，点击第5句
2. 观察：
   - 第1-4句变灰
   - 第5句变黄色
   - 音频跳转到第5句位置播放
3. 打开Console，查看输出：
   "跳转到片段4，时间X.XX秒"
```

---

#### 测试点4：鼠标悬停效果 ✅

**操作：** 鼠标悬停在任意句子上

**预期效果：**
- ✅ 句子向右平移5px（`transform: translateX(5px)`）
- ✅ 出现阴影效果（`box-shadow`）
- ✅ 过渡动画流畅（`transition: all 0.3s ease`）

**验证方法：**
```
鼠标移动到句子上，观察：
- 句子轻微向右移动
- 出现浅灰色阴影
- 移开鼠标后恢复原样
```

---

#### 测试点5：播放结束处理 ✅

**操作：** 等待音频播放完毕

**预期效果：**
- ✅ 所有句子变灰（已播放状态）
- ✅ 没有黄色高亮（没有正在播放的句子）
- ✅ 音频控件显示播放结束

**验证方法：**
```
播放到最后一句后：
- 观察所有句子都是灰色半透明
- 没有黄色高亮
- 可以重新点击播放按钮从头开始
```

---

#### 测试点6：重新播放 ✅

**操作：** 播放结束后，点击音频播放按钮

**预期效果：**
- ✅ 所有句子恢复原色（蓝色/绿色）
- ✅ 清除所有"已播放"标记
- ✅ 从第1句重新开始高亮
- ✅ 音频从头播放

**验证方法：**
```
1. 播放结束后，点击音频播放按钮
2. 观察：
   - 所有句子颜色恢复（蓝色/绿色）
   - 第1句立即变黄色
   - 开始新一轮播放
```

---

## 🎨 UI效果对照表

| 状态 | 背景颜色 | 文字颜色 | 左边框 | 特效 |
|------|---------|---------|--------|------|
| **加粗句子（未播放）** | #e3f2fd（淡蓝） | #1565c0（深蓝） | #2196F3（蓝色） | - |
| **非加粗句子（未播放）** | #e8f5e9（淡绿） | #2e7d32（深绿） | #4caf50（绿色） | - |
| **正在播放** | #fff9c4（黄色） | #f57f17（金色） | #fbc02d（金色） | 脉动动画+阴影 |
| **已播放** | 原色+灰度 | 原色+灰度 | 原色 | 半透明（0.5） |
| **鼠标悬停** | 原色 | 原色 | 原色 | 右移5px+阴影 |

---

## 🔍 调试技巧

### Console输出检查

**正常输出示例：**
```javascript
生成语音配置:
- 文件名: 测试文档内容模板
- 格式: mp3
- 采样率: 24000

TTS生成结果: {success: true, taskId: "xxx", ...}
对话片段数量: 5
音频总时长: 15.5 秒

音频加载成功:
- Blob 大小: 245678 bytes
- Blob 类型: audio/mpeg

文档预览渲染完成，共 5 个片段

跳转到片段4，时间12.30秒
```

### Network检查

**API调用：**
1. `POST /api/document-tts/generate` - 生成语音并返回JSON（包含segments）
2. `GET /tts/documents/xxx.mp3` - 获取音频文件

**验证：**
- Response包含 `segments` 数组
- 每个segment包含 `index`, `text`, `isBold`, `startTime`, `duration`, `voiceId`

---

## ⚠️ 常见问题

### 问题1：文档预览区域不显示

**原因：** 后端返回的 `segments` 为空或null

**解决：**
1. 打开Console，查看 `TTS生成结果`
2. 检查 `result.segments` 是否有数据
3. 如果为空，检查后端 `DocumentTTSServiceImpl.buildDialogSegments()` 方法

---

### 问题2：高亮不同步

**原因：** 时间估算不准确

**解决：**
1. 打开Console，查看 `对话片段数量` 和 `音频总时长`
2. 检查每个片段的 `startTime` 和 `duration`
3. 调整 `buildDialogSegments()` 中的时间估算公式（当前是每字0.3秒）

---

### 问题3：点击跳转不准确

**原因：** `segment.startTime` 不准确

**解决：**
1. 打开Console，查看 `跳转到片段X，时间X.XX秒`
2. 手动播放音频，对比实际时间
3. 调整时间估算公式

---

### 问题4：角色颜色不对

**原因：** `isBold` 字段不正确

**解决：**
1. 检查Word文档中的文本是否正确加粗
2. 打开Console，查看每个segment的 `isBold` 值
3. 检查 `WordDocumentParser.parse()` 方法的加粗判断逻辑

---

## 📊 性能指标

### 预期性能

| 指标 | 数值 |
|------|------|
| **文档解析时间** | < 1秒 |
| **语音生成时间** | 20-30秒（5句话） |
| **页面渲染时间** | < 100ms |
| **高亮更新延迟** | < 50ms |
| **自动滚动动画** | 300ms（smooth） |

### 测试方法

```javascript
// 打开Console，查看性能数据
performance.measure('document-parse');
performance.measure('tts-generate');
performance.measure('segment-render');
```

---

## ✅ 验收标准

### 必须通过的测试

- [x] 测试点1：文档内容预览正确显示
- [x] 测试点2：实时高亮同步准确
- [x] 测试点3：点击跳转功能正常
- [x] 测试点4：鼠标悬停效果流畅
- [x] 测试点5：播放结束处理正确
- [x] 测试点6：重新播放功能正常

### 可选优化

- [ ] 时间估算更精确（需要火山引擎返回实际时长）
- [ ] 添加进度条显示（显示总体播放进度）
- [ ] 添加快捷键控制（空格=播放/暂停，←→=跳转）
- [ ] 添加播放速度控制（0.5x、1x、1.5x、2x）

---

## 🚀 下一步计划

1. **精确时间同步** - 使用火山引擎返回的实际音频时长
2. **更多交互功能** - 添加快捷键、进度条、播放速度控制
3. **移动端适配** - 优化移动端显示和交互
4. **性能优化** - 优化大文档（100+句）的渲染性能

---

**测试完成时间：** _____________  
**测试人员：** _____________  
**测试结果：** ✅ 通过 / ⚠️ 部分通过 / ❌ 未通过  
**问题记录：** _____________
